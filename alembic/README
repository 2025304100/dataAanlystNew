Alembic 迁移目录（WP0.2）
==========================

本目录托管机会中心改造项目的版本化数据库迁移脚本。

## 目录结构
- `env.py`：Alembic 运行时入口，从 `app/core/config.py` 的 `settings.database_url`
  读取数据库连接（支持 SQLite / MySQL 双库），并将 `app.models.*` 的
  `Base.metadata` 作为 `target_metadata`。
- `script.py.mako`：新建迁移脚本的模板（`alembic revision -m "..."` 使用）。
- `versions/`：存放具体迁移修订文件。

## 双轨迁移策略（重要）
本项目采用「Alembic 版本化迁移」与「init_db.py 轻量兼容迁移」并行的双轨策略：

1. **Alembic 版本化迁移**（本目录）
   - 用于新表创建、新列添加等可重放的版本化变更。
   - 通过 `alembic revision --autogenerate -m "..."` 生成修订，
     再 `alembic upgrade head` 应用。
   - 适合开发期 schema 演进与生产环境显式升级。

2. **轻量兼容迁移**（`app/db/init_db.py` 中的 `_ensure_sqlite_*_columns`
   与 `_ensure_mysql_indicator_version_columns`）
   - 用于运行时对已存在表做幂等的 schema 补丁（ALTER TABLE ADD COLUMN）。
   - 适配旧库升级场景：`Base.metadata.create_all` 不会 ALTER 已存在表，
     这些函数在启动时补齐缺失列。
   - 必须保留，不得删除。

两条轨道并行运行：新变更优先走 Alembic，兼容补丁继续由 init_db.py 在启动时执行。

## 常用命令
```bash
# 生成新修订（基于模型差异）
alembic revision --autogenerate -m "add xxx table"

# 应用到最新版本
alembic upgrade head

# 回滚一个版本
alembic downgrade -1

# 查看当前版本
alembic current
```

## 注意事项
- 迁移必须同时兼容 SQLite 和 MySQL（参见 `app/db/dialect.py`）。
- 不要在迁移脚本中写死数据库选择。
- 不要在迁移脚本或日志中泄露数据库凭据。
