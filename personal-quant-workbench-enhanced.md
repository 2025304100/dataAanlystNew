# 个人量化研究工作台增强建议

## 1. 优化方向判断

现有方案已经把研究、预测、回测、复盘的闭环搭出来了，但如果你的核心诉求是：

- 个人便捷使用
- 快速做预测分析和量化打分
- 快速筛出可操作的股票 / ETF
- 能结合总仓位金额直接给出买卖建议和阶段上限

那还需要把系统从“研究平台”进一步收紧成“研究 + 决策辅助平台”。

当前方案最值得补强的不是再加更多花哨模块，而是补下面 4 个关键能力：

1. `股质评分` 和 `交易时点评分` 分开
2. 把 `机会扫描` 直接联动到 `仓位建议`
3. 加入 `组合级资金管理和阶段上限`
4. 让输出结果从“看法”升级为“可执行计划”

## 2. 建议重新定义核心目标

建议把系统目标明确成下面这句：

> 帮助个人投资者在限定风险和仓位规则下，快速识别高质量标的、判断所处阶段、生成可执行的交易计划。

这句话很重要，因为它会直接影响 MVP 的优先级。

不是优先做一个“万能分析系统”，而是优先做一个：

- 能筛
- 能打分
- 能排队
- 能提示怎么买
- 能提示什么时候不能再买

的个人工作台。

## 3. 核心能力优化

### 3.1 增加“双评分体系”

现在文档里更偏“综合趋势/风险评分”，建议拆成两个分数：

#### A. 股质评分 `Quality Score`

用于回答：这个标的本身值不值得长期放进观察池或核心池。

建议维度：

- 趋势质量
- 波动健康度
- 流动性质量
- 相对强度
- 板块归属和赛道强度
- 基本事件稳定性

适合输出：

- `A / B / C / D` 四档
- `0-100` 分
- 标签：`核心观察` / `普通观察` / `仅交易型` / `淘汰`

#### B. 时点评分 `Timing Score`

用于回答：当前是不是合适的介入窗口。

建议维度：

- 启动信号
- 动量斜率
- 量价配合
- 回调结构
- 事件催化强度
- 过热程度

适合输出：

- `0-100` 分
- 阶段：`启动 / 加速 / 过热 / 退潮`
- 动作：`可开仓 / 可低吸 / 仅持有 / 禁止追高 / 减仓 / 退出`

这样会比单一综合分更贴近真实决策流程。

### 3.2 增加“组合设置 + 仓位引擎”

这是你这次提出的最关键增强点，建议作为核心模块，而不是附属参数。

用户先设置：

- 总资金金额
- 计划投入比例
- 保留现金比例
- 单标最大仓位
- 单赛道最大仓位
- 股票总仓上限
- ETF 总仓上限
- 单笔最大亏损比例
- 同时持仓数量上限

系统据此自动计算：

- 当前还能新开几笔
- 每个候选标的的建议仓位
- 某只票是否超过阶段允许上限
- 某个赛道是否已经过度集中

建议新增一个核心公式：

```text
建议仓位 = min(
  单标仓位上限,
  阶段仓位上限,
  赛道剩余仓位,
  资产类别剩余仓位,
  风险预算仓位
)
```

其中：

```text
风险预算仓位 = 可承受亏损金额 / 止损幅度
```

例如：

- 总资金 `500000`
- 单笔最大亏损 `1%`
- 可承受亏损金额 `5000`
- 预设止损 `5%`

则风险预算仓位约为：

```text
5000 / 5% = 100000
```

系统就能直接提示这笔交易不应超过 `10` 万。

### 3.3 增加“阶段上限规则”

你提到“提示买卖点和阶段上限”，这个非常合理，建议直接制度化。

可以先用简单但实用的规则：

- `启动`
  - 允许试仓
  - 单标建议 `5%-10%`
  - 允许后续加仓
- `加速`
  - 允许持有和小幅加仓
  - 单标建议 `10%-20%`
  - 若偏离买点过大则禁止追高
- `过热`
  - 禁止新追
  - 以减仓和锁盈为主
  - 单标上限自动下调
- `退潮`
  - 以退出或观察为主
  - 原则上不新增仓位

ETF 和个股还可以分开：

- ETF 可适度放宽单标仓位上限
- 个股应更严格控制集中度和止损

### 3.4 增加“交易计划输出”

现在的方案里输出的是预测结果，建议升级为交易计划卡片。

每个候选标的至少输出：

- 当前股质评分
- 当前时点评分
- 当前阶段
- 建议动作
- 建议买入区间
- 失效位 / 止损位
- 第一目标位
- 建议仓位
- 是否允许加仓
- 所属赛道当前是否超配

这样用户打开后不只是看到“看好”，而是能直接知道：

- 能不能买
- 买多少
- 买错了怎么办
- 涨上去之后还能不能加

## 4. 机会扫描模块优化

### 4.1 从“榜单”升级为“可执行候选池”

建议扫描结果不要只按总分排序，而是给出 3 个榜单：

- `高股质候选`
- `高时点候选`
- `组合可执行候选`

其中“组合可执行候选”最实用，它要同时满足：

- 分数达标
- 没有超过单标上限
- 没有超过赛道上限
- 当前阶段允许介入
- 流动性合格

### 4.2 增加预设扫描模板

建议默认内置几套模板，而不是让用户每次自己拼条件：

- `稳健ETF轮动`
- `强趋势个股`
- `启动低位候选`
- `事件驱动观察`
- `过热减仓监控`

这样个人使用会顺手很多。

## 5. 评分框架建议

### 5.1 适合个人使用的简化总分

为了便于快速落地，建议不要一开始就上太复杂模型，可以先用可解释权重法。

#### 股质评分

```text
Quality Score
= 25% 趋势质量
+ 20% 相对强弱
+ 15% 波动健康度
+ 15% 流动性
+ 15% 板块/赛道强度
+ 10% 事件稳定性
```

#### 时点评分

```text
Timing Score
= 30% 启动/突破信号
+ 20% 动量
+ 15% 量价配合
+ 15% 回调结构
+ 10% 事件催化
- 10% 过热惩罚
```

#### 执行优先级分

```text
Execution Priority
= 40% Timing Score
+ 30% Quality Score
+ 20% 组合仓位适配度
+ 10% 流动性适配度
```

这样最终扫描排序会更像“今天该先看谁”。

## 6. 数据模型补充建议

原方案中的实体已经不错，但如果要支持你的资金和买卖建议诉求，建议补这几张表：

- `portfolios`
- `portfolio_rules`
- `positions`
- `trade_setups`
- `trade_signals`
- `allocation_snapshots`

各表职责建议如下：

- `portfolios`
  - 保存总资金、可用资金、账户类型
- `portfolio_rules`
  - 保存单标上限、单赛道上限、股票/ETF 上限、风险预算
- `positions`
  - 当前持仓、成本、浮盈亏、归属赛道、资产类别
- `trade_setups`
  - 某只标的在某次分析时生成的买点、止损、目标位、建议仓位
- `trade_signals`
  - 每次系统给出的动作提示，如开仓、加仓、减仓、退出
- `allocation_snapshots`
  - 每次扫描或交易前后组合暴露情况的快照

## 7. 页面设计补充建议

建议在原来的页面结构上补两个高价值视图。

### 7.1 组合驾驶舱

展示：

- 总资金
- 已用仓位
- 股票 / ETF 仓位分布
- 赛道集中度
- 当前允许新增仓位
- 高优先级候选

### 7.2 候选交易卡片

每个候选卡片直接显示：

- 股质分
- 时点分
- 阶段
- 建议仓位
- 买入区间
- 止损位
- 目标位
- 风险收益比

这样会比单纯榜单更贴近实际操作。

## 8. MVP 优先级建议调整

如果目标是尽快做出“个人真会每天打开用”的版本，我建议优先级稍微重排。

### 第一阶段 MVP

- 数据接入与本地存储
- 标的池管理
- 股质评分
- 时点评分
- 单标的详情分析
- 机会扫描
- 组合设置和仓位规则
- 交易计划输出

### 第二阶段

- 公式版本化和参数对比
- 多公式对比
- 基础回测
- 复盘记录

### 第三阶段

- AI 消息面增强
- 自动预警
- 每日扫描报告
- 更高级参数实验

原因很简单：

对个人用户来说，先解决“今天看什么、买多少、还能不能加”比“复杂回测体系”更有日常价值。

## 9. 我认为最合理的产品主线

如果我们把产品做得更聚焦，我建议主线就是：

```text
数据更新
-> 股质评分
-> 时点评分
-> 机会扫描
-> 组合约束过滤
-> 生成交易计划
-> 执行记录
-> 复盘修正
```

这个链路会比原始方案更贴近你的真实诉求，也更适合个人用户长期使用。

## 10. 建议直接纳入方案的新增需求

建议把下面这些明确写入正式需求：

- 支持设置总资金和组合规则
- 支持按股票 / ETF 分别设置仓位上限
- 支持按阶段输出建议仓位区间
- 支持输出买入区间、止损位、目标位
- 支持检测赛道过度集中
- 支持根据风险预算自动计算建议仓位
- 支持生成“组合可执行候选池”
- 支持区分股质评分和时点评分

## 11. 结论

你的方向是合理的，而且我建议进一步聚焦：

- 少一点“全能平台”思路
- 多一点“个人交易助手”思路

真正高价值的不是系统能分析很多，而是它能在你准备下单前，明确回答下面几个问题：

- 这只票值不值得看
- 现在是不是买点
- 该买多少
- 买了以后最多加到多少
- 什么时候该停手

如果按这个方向完善，产品会更利于落地，也更容易做成你每天都会打开的工具。
