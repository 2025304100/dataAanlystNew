# 个人量化研究工作台改良计划方案

## 1. 改良目标

当前项目已经有个人量化工作台的基本方向，下一步最重要的不是继续堆功能，而是把系统从“能展示分析结果”改良成“能稳定产出可解释、可执行、可复盘的交易辅助决策”。

本阶段改良聚焦三件事：

- 数据是否可靠
- 分数是否可信
- 建议是否可执行

改良后的系统应该能回答：

- 今天数据能不能用
- 哪些股票 / ETF 值得优先看
- 这只标的为什么高分或低分
- 现在是不是合适买点
- 在我的总资金和仓位规则下应该买多少
- 已有持仓是否还能加仓或需要减仓

## 2. 总体原则

### 2.1 先可信，再智能

如果行情数据缺失、过期或覆盖不足，再复杂的评分模型也没有意义。

第一优先级必须是数据完整度、更新时间、异常提示和失败重试。

### 2.2 先可解释，再自动化

个人使用场景下，系统不能只给一个分数。它需要告诉用户：

- 为什么高分
- 风险在哪里
- 哪些因子贡献最大
- 哪些条件导致禁止买入

### 2.3 先辅助决策，再追求预测

系统不应该包装成确定性预测器，而应该定位为交易决策助手。

最关键的是让用户每天快速知道：

- 今天看什么
- 哪些能买
- 买多少
- 什么时候不能追
- 已有持仓该不该减

## 3. 当前最急问题

### 3.1 数据覆盖与新鲜度不足

测试中发现数据健康存在明显风险：

- 行情覆盖率偏低
- 大量标的行情超过 7 天未更新
- 最近机会挖掘任务不是完成状态

影响：

- 股质评分不可信
- 时点评分不可信
- 机会扫描结果可能误导
- 买卖点和仓位建议缺乏基础

### 3.2 缺少评分解释层

如果系统只输出总分，用户很难判断是否应该相信系统。

需要补充：

- 分项得分
- 因子贡献
- 风险扣分
- 阶段判断原因
- 禁止买入原因

### 3.3 交易建议还不够闭环

仅有分数和阶段不够，个人使用需要明确交易计划。

至少需要：

- 买入区间
- 止损位 / 失效位
- 目标位
- 建议仓位
- 阶段仓位上限
- 风险收益比
- 是否允许加仓

### 3.4 持仓与组合约束不足

没有当前持仓，系统无法判断：

- 是否已经重仓
- 是否赛道集中
- 是否超过股票 / ETF 配置上限
- 是否还能开新仓
- 某个候选是否适合买入

## 4. 改良路线图

## P0：数据可信与基础可用

目标：让系统每天打开时，先知道数据是否可用，避免基于脏数据做判断。

建议周期：1 到 2 周。

### 4.1 数据健康中心

需要展示：

- 行情覆盖率
- 最新交易日
- 过期标的数量
- 缺失 K 线标的
- 最近数据更新任务状态
- 最近机会扫描任务状态
- 数据异常风险提示

验收标准：

- 用户能一眼看到今天数据是否可用
- 系统能标出哪些标的缺数据
- 数据异常时，扫描和评分页面有明显提示

### 4.2 数据更新任务改良

需要补充：

- 批量更新进度
- 失败重试
- 单标的补拉
- 按资产类型更新：股票 / ETF / 指数 / 板块
- 更新日志
- 更新结果摘要

验收标准：

- 可以手动触发全量或增量更新
- 更新失败时能看到失败原因
- 可以单独修复某个标的的数据

### 4.3 数据有效性保护

评分和扫描前增加保护规则：

- 行情缺失时不给高置信度评分
- 行情过期时标记低可信
- 覆盖率过低时提示扫描结果不完整
- 关键字段异常时跳过该标的

验收标准：

- 缺数据标的不会被误排到高优先级
- 扫描结果能展示可信度

## P1：评分解释与机会筛选升级

目标：让用户不仅看到分数，还能理解分数，并快速定位真正值得看的候选。

建议周期：2 到 3 周。

### 4.4 股质评分解释

股质评分拆成分项：

- 趋势质量
- 相对强弱
- 波动健康度
- 流动性
- 赛道强度
- 事件稳定性

每个分项展示：

- 当前分数
- 权重
- 对总分贡献
- 简短解释

验收标准：

- 用户能知道一只票为什么是 A 档或 C 档
- 用户能看到主要加分项和扣分项

### 4.5 时点评分解释

时点评分拆成分项：

- 启动 / 突破信号
- 动量
- 量价配合
- 回调结构
- 事件催化
- 过热惩罚

同时展示阶段判断依据：

- 为什么是启动
- 为什么是加速
- 为什么判定过热
- 为什么进入退潮

验收标准：

- 用户能理解当前是不是买点
- 系统能明确说明禁止追高或减仓原因

### 4.6 机会扫描改良

扫描结果从单一榜单升级为多视角候选池：

- 高股质候选
- 高时点候选
- 组合可执行候选
- 过热风险候选
- 低可信度候选

每个候选展示：

- 股质分
- 时点分
- 执行优先级
- 当前阶段
- 主要加分原因
- 主要风险原因
- 数据可信度

验收标准：

- 用户可以快速知道今天优先看哪些标的
- 扫描结果不会把缺数据标的当作强机会

## P2：交易计划与仓位闭环

目标：把分析结果变成可执行的买卖计划。

建议周期：2 到 4 周。

### 4.7 持仓与组合管理

先做手动持仓录入，不急于对接券商。

需要支持：

- 标的代码
- 持仓数量
- 成本价
- 当前价
- 持仓市值
- 浮盈亏
- 所属赛道
- 股票 / ETF 类型

组合层展示：

- 总资金
- 已用仓位
- 可用现金
- 股票仓位
- ETF 仓位
- 单赛道集中度
- 单标集中度

验收标准：

- 系统能知道当前组合暴露情况
- 系统能判断某个候选是否还能买

### 4.8 仓位规则引擎

需要支持配置：

- 总资金
- 计划投入比例
- 保留现金比例
- 单标最大仓位
- 单赛道最大仓位
- 股票总仓上限
- ETF 总仓上限
- 单笔最大亏损比例
- 同时持仓数量上限

建议仓位计算：

```text
建议仓位 = min(
  单标仓位上限,
  阶段仓位上限,
  赛道剩余仓位,
  资产类别剩余仓位,
  风险预算仓位
)
```

验收标准：

- 系统能给出建议买入金额
- 系统能说明为什么建议这么多
- 超配时系统能明确禁止新增

### 4.9 交易计划卡片

每个候选标的生成交易计划：

- 标的名称和代码
- 股质评分
- 时点评分
- 当前阶段
- 建议动作
- 买入区间
- 止损位 / 失效位
- 第一目标位
- 建议仓位
- 风险收益比
- 是否允许加仓
- 禁止条件

验收标准：

- 用户不需要自己二次计算仓位
- 用户能看到一条完整操作计划

## P3：验证、复盘与长期迭代

目标：让系统能自我校验，避免长期依赖主观感觉。

建议周期：持续迭代。

### 4.10 轻量信号验证

先不做复杂回测，先做信号表现统计：

- 信号出现后 3 日收益
- 信号出现后 5 日收益
- 信号出现后 10 日收益
- 信号出现后 20 日收益
- 胜率
- 平均收益
- 最大回撤

验收标准：

- 用户能知道某类信号历史上是否有效
- 用户能比较不同信号和参数的表现

### 4.11 复盘闭环

复盘记录需要绑定：

- 当时评分
- 当时阶段
- 当时交易计划
- 实际执行动作
- 最终结果
- 用户主观备注

验收标准：

- 可以回看当时为什么买
- 可以统计是否按系统执行
- 可以总结哪类交易最容易出错

### 4.12 备份与导出

个人长期使用必须保证数据安全。

需要支持：

- 一键备份 SQLite 数据库
- 导出复盘记录
- 导出扫描结果
- 导出持仓和交易计划
- 恢复备份

验收标准：

- 用户可以迁移数据
- 数据损坏或误操作后有恢复路径

## 5. 推荐开发顺序

建议按以下顺序推进：

1. 数据健康中心
2. 数据更新与补拉
3. 评分可信度保护
4. 股质评分解释
5. 时点评分解释
6. 机会扫描多榜单
7. 手动持仓录入
8. 仓位规则引擎
9. 交易计划卡片
10. 轻量信号验证
11. 复盘闭环
12. 备份与导出

## 6. 立即启动版本

建议下一个版本命名为：

```text
MVP+ 数据可信与决策可用版
```

版本目标：

- 用户每天打开系统后，先知道数据是否可信
- 用户能看到哪些标的值得优先关注
- 用户能理解系统为什么给出这个评分
- 用户能得到能否买、买多少、为什么的决策辅助

## 7. 第一阶段任务拆解

第一阶段只聚焦 P0，目标是把数据底座稳定住。

任务清单：

- 完善 `/api/v1/system/data-health`
- 增加数据健康前端视图
- 增加缺失标的列表
- 增加过期行情列表
- 增加单标补拉接口
- 增加批量更新任务状态
- 在评分和扫描页面展示数据可信度

验收口径：

- 今天数据是不是新的
- 哪些标的缺 K 线
- 哪些标的超过 7 天未更新
- 机会扫描结果是否可信
- 是否需要先更新数据再看结论

## 8. 风险与注意事项

### 8.1 不要过早复杂化模型

初期不要急着引入复杂机器学习模型。先用可解释规则和权重法，把数据质量、评分解释和交易计划做扎实。

### 8.2 不要把系统包装成确定性预测器

系统应该输出概率、风险和计划，而不是绝对判断。

建议所有交易相关页面保留风险提示：

- 数据可能延迟
- 分数仅代表模型判断
- 买卖计划需用户最终确认

### 8.3 默认预设要好用

个人工具是否能长期使用，很大程度取决于默认配置。

建议内置：

- 稳健 ETF 轮动
- 强趋势个股
- 启动低位候选
- 过热减仓监控
- 低可信数据过滤

## 9. 结论

项目下一步最该做的是：

```text
数据可信 -> 评分可解释 -> 交易计划可执行
```

这条路线能把当前项目从“研究原型”推进到“个人每天能用的决策工具”。

不建议现在优先做复杂回测、AI 消息面或更多页面。先把数据、评分、仓位和交易计划这条主线做稳，系统的实际价值会提升得最快。
