# 个人量化研究工作台 MVP 需求文档

## 1. 文档目标

本文档用于统一《个人量化研究工作台需求清单》《个人量化研究工作台方案》以及增强建议中的有效内容，形成一版可直接用于产品设计、数据库设计和工程开发的正式 MVP 需求。

本版 MVP 的目标不是构建复杂全面的量化平台，而是优先落地一套适合个人日常使用的：

- 预测分析工具
- 量化股质打分工具
- 股票 / ETF 机会筛选工具
- 组合仓位与交易计划辅助工具

## 2. 产品定位

本系统定位为个人量化研究与交易决策助手，服务于中低频、波段型交易场景，重点支持股票与 ETF 的日常研究、筛选、评分、仓位控制与复盘。

系统需要帮助用户高效回答以下问题：

- 这只标的值不值得关注
- 当前是不是合适的介入时点
- 在我的总资金和仓位规则下，应该买多少
- 当前阶段最多能加到多少
- 某个赛道或资产类别是否已经过度集中
- 后续应该持有、加仓、减仓还是退出

## 3. MVP 核心目标

MVP 必须优先实现以下能力：

- 统一管理股票 / ETF 自选池和观察池
- 接入并本地保存基础行情数据
- 对单标的输出股质评分和时点评分
- 对大范围标的进行机会扫描和优先级排序
- 根据组合规则输出建议仓位和交易计划
- 展示阶段判断、买入区间、止损位、目标位
- 保存预测结果、交易设定和复盘记录，支持后续追踪

MVP 暂不追求：

- 高频交易
- 复杂机器学习模型
- 多市场全覆盖
- 自动实盘交易

## 4. 产品主线

系统的核心工作流定义为：

```text
数据更新
-> 因子计算
-> 股质评分
-> 时点评分
-> 机会扫描
-> 组合规则过滤
-> 生成交易计划
-> 执行记录
-> 复盘修正
```

这条主线是 MVP 的设计中心，其他功能均围绕该主线服务。

## 5. MVP 范围

### 5.1 必做范围

- 标的池管理
- 数据抓取与本地存储
- 因子基础管理
- 股质评分
- 时点评分
- 单标的详情分析
- 机会扫描
- 组合设置与仓位规则
- 交易计划输出
- 基础复盘备注

### 5.2 延后范围

- 复杂回测对比体系
- 大规模参数实验
- AI 消息面增强
- 自动预警
- 多市场扩展

## 6. 用户对象与使用场景

目标用户为个人投资者，主要使用场景包括：

- 收盘后更新数据，查看候选股票和 ETF
- 在观察池中快速定位高质量、好时点标的
- 根据总资金和仓位规则确定是否开仓、买多少
- 对已有持仓判断是否还能加仓，是否已进入过热阶段
- 记录买入逻辑和后续复盘结论

## 7. 关键业务概念

### 7.1 股质评分 `Quality Score`

用于评估标的本身是否值得中短期持续关注。

输出形式：

- `0-100` 分
- `A / B / C / D` 分档
- 标签：`核心观察` / `普通观察` / `仅交易型` / `淘汰`

### 7.2 时点评分 `Timing Score`

用于评估当前是否为较优的交易时点。

输出形式：

- `0-100` 分
- 当前阶段：`启动 / 加速 / 过热 / 退潮`
- 当前动作：`可开仓 / 可低吸 / 仅持有 / 禁止追高 / 减仓 / 退出`

### 7.3 组合规则

用于约束系统给出的建议仓位与交易动作。

至少包括：

- 总资金金额
- 计划投入比例
- 保留现金比例
- 单标最大仓位
- 单赛道最大仓位
- 股票总仓上限
- ETF 总仓上限
- 单笔最大亏损比例
- 同时持仓数量上限

### 7.4 交易计划

系统针对候选标的生成的可执行建议，至少包括：

- 当前股质评分
- 当前时点评分
- 阶段判断
- 建议动作
- 建议买入区间
- 止损位 / 失效位
- 第一目标位
- 建议仓位
- 是否允许加仓
- 所属赛道是否超配

## 8. 功能需求

### 8.1 标的池管理

系统需要支持：

- 添加、删除、搜索自选标的
- 区分股票与 ETF
- 按市场、行业、赛道分类
- 管理观察池、交易池、淘汰池
- 保存自定义标的池

### 8.2 数据接入与更新

系统需要支持：

- 使用 `AKShare` 作为第一阶段数据源
- 抓取股票、ETF、指数、板块日线数据
- 本地保存原始抓取结果
- 手动刷新数据
- 记录最近更新时间和更新状态
- 为未来切换 `Tushare` 或其他数据源预留接口层

MVP 数据更新建议：

- 日线行情：每日收盘后更新一次
- 指数 / 板块：每日更新一次
- 因子、评分、扫描结果：行情更新后自动重算

### 8.3 因子管理

系统需要支持：

- 新增因子
- 编辑因子名称、说明、分类、方向
- 设置因子状态：`draft / testing / active / deprecated`
- 查看因子输入字段和计算方式
- 启用与停用因子

MVP 阶段对因子版本化只要求基础可追踪，不要求复杂编辑器。

### 8.4 评分体系

系统必须实现双评分体系。

#### 股质评分维度

建议至少包含：

- 趋势质量
- 相对强弱
- 波动健康度
- 流动性
- 板块 / 赛道强度
- 事件稳定性

建议初版权重：

```text
Quality Score
= 25% 趋势质量
+ 20% 相对强弱
+ 15% 波动健康度
+ 15% 流动性
+ 15% 板块/赛道强度
+ 10% 事件稳定性
```

#### 时点评分维度

建议至少包含：

- 启动 / 突破信号
- 动量
- 量价配合
- 回调结构
- 事件催化
- 过热惩罚

建议初版权重：

```text
Timing Score
= 30% 启动/突破信号
+ 20% 动量
+ 15% 量价配合
+ 15% 回调结构
+ 10% 事件催化
- 10% 过热惩罚
```

### 8.5 阶段判断与动作映射

系统必须对候选标的输出阶段判断，并映射为具体动作建议。

建议规则：

- `启动`
  - 可试仓
  - 允许后续加仓
- `加速`
  - 可持有
  - 可小幅加仓
  - 若偏离买点过大则禁止追高
- `过热`
  - 禁止新追
  - 以减仓和锁盈为主
- `退潮`
  - 原则上不新增仓位
  - 以退出或观察为主

### 8.6 机会扫描

系统需要支持：

- 按范围扫描：
  - `ETF`
  - `上证A股`
  - `深证A股`
  - `创业板`
  - `科创板`
- 多选扫描范围
- 设置过滤条件：
  - 成交额
  - 成交量
  - 价格区间
  - 上市天数
  - 波动率
  - 是否剔除 ST、新股、停牌
- 保存扫描预设

扫描结果至少提供 3 类榜单：

- `高股质候选`
- `高时点候选`
- `组合可执行候选`

其中“组合可执行候选”必须同时考虑：

- 分数是否达标
- 当前阶段是否允许介入
- 是否超过单标上限
- 是否超过赛道上限
- 是否超过资产类别上限
- 流动性是否合格

### 8.7 组合设置与仓位规则

系统必须提供组合设置页面或面板，支持配置：

- 总资金
- 可投资资金比例
- 现金保留比例
- 单标最大仓位
- 单赛道最大仓位
- 股票总仓上限
- ETF 总仓上限
- 单笔最大亏损比例
- 同时持仓数量上限

系统必须自动计算：

- 当前已使用仓位
- 当前可新增仓位
- 某候选标的建议仓位
- 某赛道是否超配
- 当前还能新开几笔

建议仓位计算原则：

```text
建议仓位 = min(
  单标仓位上限,
  阶段仓位上限,
  赛道剩余仓位,
  资产类别剩余仓位,
  风险预算仓位
)
```

其中：

```text
风险预算仓位 = 可承受亏损金额 / 止损幅度
```

### 8.8 阶段仓位上限

系统必须支持按阶段对建议仓位做约束。

建议初版规则：

- `启动`
  - 单标建议 `5%-10%`
- `加速`
  - 单标建议 `10%-20%`
- `过热`
  - 自动下调上限
  - 原则上不建议新增
- `退潮`
  - 原则上不建议持有新增仓位

系统应允许后续将股票与 ETF 的阶段上限分开配置。

### 8.9 单标的详情分析

系统需要展示：

- K 线图
- 关键均线和成交量
- 股质评分
- 时点评分
- 各维度得分
- 当前阶段与动作建议
- 建议买入区间
- 止损位 / 失效位
- 第一目标位
- 建议仓位
- 备注与复盘记录

### 8.10 交易计划输出

系统必须将分析结果生成结构化交易计划卡片。

每个交易计划至少包含：

- 标的名称和代码
- 资产类型
- 股质评分
- 时点评分
- 执行优先级
- 阶段
- 建议动作
- 买入区间
- 止损位
- 目标位
- 建议仓位
- 风险收益比
- 所属赛道
- 是否超配

### 8.11 复盘与日志

MVP 需要支持基础复盘：

- 记录买入或观察理由
- 记录主观判断
- 记录是否按系统执行
- 记录结果和后续总结
- 支持按时间和标的查询

## 9. 页面需求

### 9.1 总览页

展示：

- 自选标的摘要
- 组合仓位概览
- 高优先级候选
- 当前重点风险提示

### 9.2 标的详情页

展示单标的完整分析、图表和交易计划。

### 9.3 机会扫描页

布局建议：

- 左侧：范围选择和过滤条件
- 中间：候选榜单
- 右侧：候选交易卡片与摘要

### 9.4 组合驾驶舱

展示：

- 总资金
- 已用仓位
- 股票 / ETF 仓位分布
- 赛道集中度
- 当前可新增仓位
- 当前组合可执行候选

### 9.5 因子中心

展示因子列表、分类、状态和说明。

### 9.6 复盘页

展示历史记录，并支持筛选与补充总结。

## 10. 数据模型要求

MVP 建议至少包含以下核心实体：

- `symbols`
- `watchlists`
- `daily_bars`
- `factors`
- `factor_values`
- `scores`
- `scan_presets`
- `scan_runs`
- `scan_results`
- `portfolios`
- `portfolio_rules`
- `positions`
- `trade_setups`
- `trade_signals`
- `allocation_snapshots`
- `journal_entries`

核心说明：

- `scores`
  - 保存股质评分、时点评分、阶段、动作建议
- `trade_setups`
  - 保存买入区间、止损位、目标位、建议仓位
- `portfolio_rules`
  - 保存仓位、风险和资产类别约束
- `allocation_snapshots`
  - 保存每次扫描或输出时的组合暴露状态

## 11. 非功能需求

### 11.1 易用性

- 页面结构必须清晰
- 核心结论必须一眼可见
- 尽量减少重复输入和重复点击
- 默认预设要适合个人快速使用

### 11.2 性能

- 单标的详情页加载应快速
- 机会扫描结果应可缓存
- 评分计算应支持增量更新

### 11.3 可扩展性

- 数据源调用需统一封装
- 因子与评分逻辑需解耦
- 仓位规则需可配置
- 后续可扩展到回测、消息面和自动预警

### 11.4 可追溯性

- 每次评分结果应保留生成时间
- 每次交易计划应保留当时的评分和组合上下文
- 每次扫描应保留条件和结果快照

## 12. 技术约束

MVP 技术栈约束如下：

- 后端：`FastAPI`
- 数据处理：`Pandas`
- 指标研究：`vectorbt`
- 图表：`Plotly`
- 前端：`React`
- 存储：`SQLite + Parquet`
- 第一阶段数据源：`AKShare`

回测能力保留接口，但不作为 MVP 核心实现目标。

## 13. MVP 验收标准

MVP 完成后，至少应满足以下使用结果：

- 用户可以导入并维护股票 / ETF 自选池
- 用户可以更新基础行情数据
- 系统可以对单个标的输出股质评分和时点评分
- 系统可以判断标的阶段并给出动作建议
- 系统可以按范围扫描候选标的并输出榜单
- 用户可以设置总资金和仓位规则
- 系统可以计算建议仓位和是否超配
- 系统可以生成结构化交易计划
- 用户可以记录基础复盘内容

## 14. 后续阶段建议

MVP 之后建议按以下顺序扩展：

1. 公式版本化和参数对比
2. 基础回测与对比
3. AI 消息面摘要和事件标签
4. 自动预警和每日扫描报告
5. 更复杂的参数实验和模型优化

## 15. 结论

本版 MVP 需求的核心不是做“大而全”，而是优先做“个人真能每天打开使用”的量化工作台。

其最核心的交付结果是：系统能把研究结果转化为带有仓位、阶段和风险约束的交易计划，帮助用户更快、更稳地完成股票 / ETF 的机会筛选与决策支持。
