# 组合交易大改造需求报告

> 报告版本：v1.0  
> 生成时间：2026-08-05  
> 基于原型：组合交易.html / 组合工作台.html / 新建组合弹窗.html / 无组合状态.html / 标的详情抽屉.html / 组合列表抽屉.html / 应用主框架.html  
> 对照实现版本：当前项目 `frontend/src/components/Trading.tsx` 与 `PortfolioWorkbench.tsx`

---

## 一、整体改造概述

### 1.1 改造目标

将当前"组合交易 + 组合工作台"两个独立子页面的散列布局，重构为统一信息架构的双主Tab体系：

| 维度 | 当前现状（改造前） | 目标设计（改造后） |
|---|---|---|
| **信息架构** | `组合工作台` 与 `组合交易` 两个松散子页面，无明确主Tab切换 | 顶部一级导航：`组合工作台` ↔ `组合交易`，各自含二级子Tab |
| **组合工作台** | 散列卡片结构：暴露分析 / 规则 / 候选 / 持仓录入 | 4个二级Tab：`账户总览` / `组合成员` / `策略规则（因子/条件）` / `回测中心` |
| **组合交易** | 纵向堆叠：绩效 → 自动交易 → 回测 → 下单+持仓 | 上半区：下单面板(35%) + 当前持仓(65%)；下半区：`绩效` / `自动交易` / `回测` 三Tab |
| **状态管理** | 无空状态引导，直接展示空div | 明确的「无组合」空状态 + 创建首个组合引导 |
| **组合选择器** | 散落在各处（header、modal等） | 统一Header顶部组合选择Dropdown + 状态徽章 + 组合列表抽屉 |
| **新增标的交互** | 详情modal内操作 | 标的详情右侧Drawer + 组合列表Drawer |

### 1.2 页面级变更速览

| 原型页面 | 对应改造内容 | 优先级 |
|---|---|---|
| `无组合状态.html` | 新增：无组合空状态引导页 | P0 |
| `组合交易.html` | 重构：交易页上半区（下单+持仓左右布局）+ 下半区三Tab | P0 |
| `组合工作台.html` | 重构：工作台4个子Tab（总览/成员/策略/回测） | P0 |
| `新建组合弹窗.html` | 扩展：创建时在工作台上叠加Modal + 后台内容预览可见 | P1 |
| `标的详情抽屉.html` | 新增：标的右侧抽屉替代原有详情点击 | P1 |
| `组合列表抽屉.html` | 新增：组合列表排名抽屉（排名/收益/回撤/切换） | P1 |
| `应用主框架.html` | 变更：统一Header/Nav结构（全局Tab样式规范） | P2 |

---

## 二、删除功能清单（DELETE）

> ⚠️ **删除原则**：仅删除原型中**不存在**且**不在新版信息架构**中的功能；如功能仍在其他页面有对应模块，标记为"迁移"而非删除。

### 2.1 现有 Trading.tsx 删除项

| # | 删除模块 | 原位置 | 说明 | 替代/迁移去向 |
|---|---|---|---|---|
| D1 | 账户汇总6列大Band | `Trading.tsx` 顶部 section.band.account-band | 原型改为4列/5列精简版（总资产、可用现金、持仓市值、累计收益、最大回撤，无6列网格） | 迁移到「组合工作台-账户总览」顶部5列 + 「组合交易-无顶栏」 |
| D2 | **绩效面板完整区**（独立卡片） | `Trading.tsx` P1-2 调用处 | 原交易页顶部直接渲染 `PortfolioPerformancePanel`（含日期选择+归因+复盘Tabs） | 迁移到「组合交易-下半区绩效Tab」（简化为5指标卡+净值曲线，无日期RangePicker） |
| D3 | **自动交易面板完整区**（独立卡片） | `Trading.tsx` P2-3 调用处 | 原交易页直接堆叠 `AutoTradePanel`（含dry_run、成员状态、diff对比） | 迁移到「组合交易-下半区自动交易Tab」（重构为策略列表+独立开关，非执行面板） |
| D4 | **回测面板完整区**（独立卡片） | `Trading.tsx` P2-2 调用处 | 原交易页直接堆叠 `PortfolioBacktestPanel`（含新旧引擎对比、来源、成员校验等） | 迁移到「组合工作台-回测中心」（保留完整功能）；交易页仅保留「回测结果历史表」 |
| D5 | 下单面板中的「快捷数量按钮 25%/33%/50%/全部」 | `Trading.tsx` `.trading-quick-qty` | 原型下单面板仅保留 数量/价格/预估金额，无快捷百分比按钮 | 删除（非P0必需，后续可按需加回） |
| D6 | 下单面板中的「4项持仓信息卡」（持仓量/成本/权重/当前价） | `Trading.tsx` `.trading-symbol-info` | 原型下单面板无持仓展示区块，选择标的后直接进入数量/价格录入 | 迁移到「标的详情抽屉」（更丰富的K线/因子/持仓一体化） |
| D7 | 订单预览（orderPreview）剩余现金提示 | `Trading.tsx` 未渲染，仅 `useMemo` 中计算 | 原型仅有「预估金额」只读项，不展示剩余现金 | 简化为仅展示 预估金额 = 数量×价格 |

### 2.2 现有 PortfolioWorkbench.tsx 删除项

| # | 删除模块 | 原位置 | 说明 | 替代/迁移去向 |
|---|---|---|---|---|
| D8 | **手动录入持仓表单**（代码/数量/成本3输入框+保存） | `PortfolioWorkbench.tsx` 持仓录入表单 | 原型工作台无录入入口，改为「组合成员页内 加标的→直接创建持仓」 | 迁移到「组合工作台-组合成员-Tab内的操作列」 |
| D9 | **暴露分析 + 组合规则** 两卡片（独立左右列） | `PortfolioWorkbench.tsx` P2区域 | 原型将暴露/规则 归属于「策略规则Tab」内的子模块，而非工作台首页独立卡片 | 迁移到「策略规则Tab-条件模型与风控」卡片组 |
| D10 | **备份/恢复 数据管理区** | `PortfolioWorkbench.tsx` P3区域（backup/restore modal） | 原型所有页面均无备份/恢复入口 | 删除并下沉到「设置-数据管理」（不在本次改造范围，可按需迁移） |
| D11 | 候选标的大列表（直接在workbench首页渲染） | `PortfolioWorkbench.tsx` candidates列表 | 原型将候选池作为「组合成员Tab底部-候选标的池子表」，非首页内容 | 迁移到「组合工作台-组合成员Tab底部」 |

### 2.3 组件级删除/改名

| # | 组件/路由 | 变更类型 | 说明 |
|---|---|---|---|
| D12 | `PortfolioBacktestPanel.tsx` 在 Trading 内的调用 | 删除调用 | 保留组件本体，改在「工作台-回测中心Tab」调用 |
| D13 | `AutoTradePanel.tsx` 交易页版本 | 删除调用 / 新增精简版 | 原完整面板保留（工作台策略Tab可用）；交易页「自动交易Tab」改为策略开关列表视图（新增独立组件） |

---

## 三、变更功能清单（MODIFY / CHANGE）

### 3.1 组合交易 Trading.html 变更（P0）

#### M1：上半区主布局 —— 下单面板(35%) + 当前持仓(65%) 左右分栏

| 对比维度 | 改造前 | 改造后（原型） |
|---|---|---|
| **布局** | 下单面板在右侧（约30%），持仓+绩效在左侧主区（纵向堆叠） | 左35%下单卡 + 右65%持仓卡，**同一水平线对齐**，无上下堆叠 |
| **下单面板结构** | 标题→当前价→持仓4格→数量/价格→快捷百分比→买/卖按钮 | 标的代码搜索框→买卖方向Segmented→数量→价格→预估金额（只读）→买入/卖出双按钮 |
| **搜索标的** | 无搜索框，依赖左侧机会中心/组合成员点击带入 | **新增搜索框**：输入代码/名称即可触发选中（SymbolSelect组件） |
| **买卖方向** | 依赖 ctx.side（上下文切换） | 独立 Segmented / Toggle 切换 买入↔卖出（选中高亮） |
| **按钮** | 单一 buy/sell 按钮（随上下文变化） | 双按钮并列：主按钮「买入/下单」 + Outline「卖出」（红边） |
| **持仓表列** | 代码/名称/持仓量/现价/市值/盈亏额/盈亏%/权重/操作 | **简化7列**：代码/名称/持仓量/现价/市值/盈亏%/操作（交易） |
| **持仓表操作** | 详情按钮 + 点击行弹详情 | 操作列仅「交易」文字链 → 点击后**自动回填到下单面板**（代码、方向按持仓判断、价格填现价） |

#### M2：下半区三Tab（绩效 / 自动交易 / 回测）

| Tab | 改造前 | 改造后（原型） |
|---|---|---|
| **绩效** | 完整 `PortfolioPerformancePanel`（日期RangePicker + 指标6卡 + 归因/复盘Tabs） | **精简5指标卡**：累计收益/年化收益/最大回撤/夏普比率/胜率 + 净值曲线图（含组合+基准两条） |
| **自动交易** | 完整 `AutoTradePanel`（dry_run开关、候选数、执行按钮、卖出/买入两表） | **策略开关列表视图**：4个策略卡（均线跟踪、动量突破、网格交易、止盈止损），每行 最后运行时间 + 状态（运行中/已暂停）+ Switch开关 |
| **回测** | 完整 `PortfolioBacktestPanel`（日期、名称、新旧对比、成员校验等全量功能） | **简化回测结果历史表**：顶部筛选（回测区间下拉、初始资金）+ 运行回测按钮；表格列：策略名/回测区间/初始资金/最终权益/年化收益/最大回撤/夏普比率 |

### 3.2 组合工作台 Workbench.html 变更（P0）

#### M3：新增工作台4个子Tab

| Tab（原型新） | 原位置/迁移来源 | 关键变更 |
|---|---|---|
| **①账户总览** | 原 Trading 顶部 Band + Workbench 首页卡片合并 | 新增5列资产卡（总资产/可用现金/持仓市值/累计收益/最大回撤）→ 下方「自动接管配置卡」+ 次组合卡（左右）→ 下方「风险规则面板」全宽列表 |
| **②组合成员** | 原 `PortfolioMembersPanel` + 持仓表合并 | 新增操作栏：导出CSV/手动触发再平衡/偏离过滤Switch/搜索/添加标的；表新增列：成本价、实际占比、**策略目标占比+偏离差**、浮动盈亏+盈亏%；偏离>2%或待调出行**高亮黄底**；底部新增「候选标的池」子表 |
| **③策略规则（因子/条件）** | 原 Workbench 规则卡 + 能力门控 + 因子配置合并 | 顶部总控卡（接管系统配置+总开关）→ 左右两列：**左=因子模型配置**（选股池/调仓周期/权重法+激活因子芯片）、**右=条件模型与风控**（条件触发、止盈止损、熔断阈值等） |
| **④回测中心** | 原 Trading 内 `PortfolioBacktestPanel` 全量迁入 | 功能不变（保留7.4全套：来源说明/成员校验/快照折叠/新旧引擎对比），仅调整到工作台4级Tab位置 |

#### M4：账户总览Tab关键变更

| 模块 | 变更点 |
|---|---|
| 5列资产卡 | 总资产、可用现金（含现金占比%）、持仓市值（含持仓只数）、累计收益（+年化副标）、最大回撤（+夏普比副标） |
| 自动接管配置卡 | 左：当前模型/执行模式/风险规则/调仓周期；右上：Toggle自动接管；右下：详细配置（跳策略Tab）+ 暂停接管（灰按钮） |
| 次组合卡（新增概念） | 支持多组合关联展示：次组合名+子标题、执行模式、当前模型、风险规则、接管状态Toggle |
| 风险规则列表面板 | 8项规则列表，按分类配色（蓝=因子规则/绿=基础风控/黄=参数类/红=熔断），每项前色点+规则名 |

#### M5：组合成员Tab关键变更（偏离分析新增）

| 变更 | 说明 | 数据需求 |
|---|---|---|
| 策略目标占比列 | 每个成员有「策略目标权重」，与「实际占比」并列展示 | 需 Position 或 PortfolioMember 新增 `target_weight_pct` 字段 |
| 偏离差值 | `实际 - 目标`，正偏离（实际>目标）红蓝字 / 负偏离蓝字 / 待调出（目标0）黄字警示 | 后端计算差值返回 + 前端条件着色 |
| 偏离过滤开关 | 「仅显示目标偏离 > 2% 的标的」Switch，默认关闭 | 前端过滤即可（简单） |
| 再平衡触发按钮 | 「手动触发再平衡」Action Bar按钮 | 后端需新增API：`POST /portfolios/{id}/rebalance` 返回调仓建议清单 |
| 候选标的池子表 | 独立表格：代码/名称/信号分/目标权重/所属因子（标签）/加入时间/操作（加入组合） | 新增 `portfolio_candidates` 数据接口（或复用 workbench candidates 扩充字段） |
| 高亮行-待调出 | 目标=0%的行：整行浅黄底 + 操作列改为「一键清仓」按钮（黄底醒目） | 前端行渲染判定 target_weight === 0 |

### 3.3 新建组合弹窗变更（P1）

| 对比维度 | 改造前 | 改造后（原型） |
|---|---|---|
| **显示效果** | 纯Modal覆盖全屏，背景遮罩全黑 | Modal居中弹出，**底层组合工作台内容半透明可见**（真实预览已创建空组合后的结构） |
| **字段** | 名称+资金+比例+货币+是否默认+自动交易 | 简化3字段：组合名称 / 初始资金 / 账户类型下拉（普通账户/信用账户/模拟账户） |
| **账户类型** | 仅 account_type = simulated/normal | 原型三选项：普通 / 信用 / 模拟（需检查 `Portfolio.account_type` 当前枚举定义是否支持 信用=credit） |
| **背景预览** | 无 | 底层显示空工作台+4子Tab，给用户「创建后去哪」的预期感 |

### 3.4 顶部 Header / Nav 统一变更（P2）

| 元素 | 改造前 | 改造后（原型） |
|---|---|---|
| 左：标题+组合选择 | 标题分散 | 标题「组合交易」固定 + 组合Dropdown（显示当前组合名+徽章，实盘/模拟） |
| 右：操作按钮 | 多处分散 | 刷新按钮（ghost） + 新建组合（primary solid） + 组合列表按钮（secondary + 奖杯icon） |
| 一级Nav | 无统一Tab样式 | 40px高 underline风格：组合工作台 / 组合交易，选中=青蓝色下划线+字色 |
| 运行状态徽章 | 无 | 工作台可显示「运行正常」绿色徽章 + 「自动接管：全自动运行」橙色闪电胶囊 |

---

## 四、新增功能清单（ADD / NEW）

### 4.1 页面级新增（P0-P1）

| # | 新增模块 | 原型页面 | 优先级 | 详细说明 |
|---|---|---|---|---|
| A1 | **无组合空状态页** | 无组合状态.html | P0 | 无任何组合时，主区中央展示：钱包图标+标题「暂无组合」+说明+大号主按钮「创建首个组合」 |
| A2 | **组合工作台-账户总览Tab** | 组合工作台.html L536-694 | P0 | 新增该Tab容器（原workbench首页无Tab概念，现为第1子Tab） |
| A3 | **组合工作台-策略规则Tab** | 组合工作台.html L940+ | P0 | 因子模型配置 + 条件风控模型 双栏布局（新模块） |
| A4 | **组合工作台-回测中心Tab** | 组合工作台.html（迁移定位） | P0 | 回测功能迁入工作台4子Tab（结构新） |
| A5 | **组合交易-下半区三Tab容器** | 组合交易.html L382-527 | P0 | 绩效/自动交易/回测 三Tab切换（交易页原无此容器，原三模块垂直堆叠） |
| A6 | **交易页-自动交易Tab策略开关列表** | 组合交易.html L419-471 | P0 | 新增 策略卡列表视图（替代原dry_run执行面板）：4策略+每策略独立Switch+运行状态 |
| A7 | **交易页-回测Tab简化版** | 组合交易.html L473-525 | P1 | 仅回测结果历史列表 + 顶部筛选（区间、初始资金）/运行按钮，无完整7.4复杂功能 |
| A8 | **候选标的池子表** | 组合工作台.html L878-936 | P1 | 组合成员Tab底部新增独立表格：信号分/目标权重/因子标签/加入组合按钮 |
| A9 | **风险规则列表面板** | 组合工作台.html L654-693 | P1 | 账户总览Tab底 8项分类色点规则列表（新增概念，原只有阈值数字） |
| A10 | **次组合关联卡** | 组合工作台.html L620-650 | P2 | 同一工作台下显示次组合概览卡（支持多组合联动概念） |

### 4.2 抽屉/模态级新增（P1）

| # | 新增组件 | 原型页面 | 优先级 | 详细说明 |
|---|---|---|---|---|
| A11 | **组合列表抽屉 Drawer** | 组合列表抽屉.html | P1 | 点击Header右侧「组合列表」按钮，右滑Drawer 560px宽：总览Tab（3排名徽章+组合表）/ 绩效对比Tab（组合对比）；组合表列：排名(冠亚季军徽章)/组合名+类型标签/累计收益/最大回撤/夏普/创建时间/操作（切换/详情） |
| A12 | **标的详情抽屉 Drawer** | 标的详情抽屉.html | P1 | 点击持仓行/成员行任意位置 → 右滑Drawer 480px宽：头部（代码+名称+行情Tag+星标）→ K线迷你图 → 持仓摘要4卡（现价/持仓量/成本价/市值+盈亏）→ 因子得分雷达/条形 → 操作按钮组（调仓/平仓/加自选） |
| A13 | **新建组合 Modal背景预览** | 新建组合弹窗.html | P1 | 创建Modal显示时，底层**不空遮罩**，而是显示空工作台预览结构（给用户预期），是交互体验新增 |

### 4.3 交互级新增

| # | 交互功能 | 关联位置 | 优先级 | 说明 |
|---|---|---|---|---|
| A14 | **持仓行→自动回填下单面板** | 组合交易.html 持仓表「交易」按钮 | P0 | 点击行操作「交易」→ 自动把代码写入搜索框、价格填现价、方向：若有持仓默认选卖出（方便减仓），否则默认买入 |
| A15 | **偏离过滤Toggle** | 组合工作台.html 成员Tab操作栏 | P0 | 开关一键筛选出偏离>2%的标的，聚焦再平衡操作 |
| A16 | **再平衡触发Action** | 组合工作台.html 成员Tab「手动触发再平衡」按钮 | P1 | 点击后后端批量生成调仓建议，弹确认框展示 买/卖清单后执行 |
| A17 | **候选→一键加入组合** | 候选标的池「加入组合」按钮 | P1 | 候选行一键晋升为 PortfolioMember（状态active），并写入target_weight |
| A18 | **策略独立Switch启停** | 组合交易.html 自动交易Tab每行 | P0 | 原只有组合级auto_trade_enabled（0/1）；新增**单策略启停**（需后端支持 策略级 enabled 字段） |
| A19 | **组合列表快速切换** | 组合列表抽屉 操作列「切换」按钮 | P1 | 一键切换 `ctx.portfolioId` 并刷新所有相关请求 |
| A20 | **一键清仓（待调出行）** | 组合成员Tab target=0 的高亮行 | P1 | 偏离行 目标=0%，操作列突出黄按钮，点击后生成卖出全量市价单 |

### 4.4 因子模型新配置项（策略规则Tab专属）

| # | 新增配置 | UI形式 | 优先级 | 数据支持 |
|---|---|---|---|---|
| A21 | 选股池（Universe）下拉 | 沪深300/中证500/中证800/自定义板块 | P1 | 新字段 PortfolioRule 或 FactorModelConfig：`universe_key` |
| A22 | 调仓周期（Rebalance）下拉 | 每日/每周五/每月末/按信号触发 | P1 | 新字段：`rebalance_frequency` |
| A23 | 权重分配法（Weighting）下拉 | 等权/市值加权/风险平价/最小方差 | P1 | 新字段：`weighting_method` |
| A24 | 激活因子芯片组 | 动量/价值/低波/质量/成长，每因子可配置权重% | P1 | 新字段：`factor_weights_json`（含激活+权重） |

---

## 五、数据模型与后端支持评估

### 5.1 现有数据支持情况 ✅ / ⚠️ / ❌

| 功能需求 | 涉及模型/表 | 现有字段支持 | 评估 | 缺失项 |
|---|---|---|---|---|
| 5列资产卡（总资产/现金/市值/累计收益/最大回撤） | Portfolio + Position + EquitySnapshot | `total_capital`（初始）；需动态计算总资产=现金+市值；累计收益/最大回撤/夏普 已由 `compute_portfolio_performance` 返回 | ✅ 基本支持 | 当前累计收益在 PerformanceResult.stats 中；需确保在工作台Tab首屏就可快速获取（可能需要缓存/汇总字段） |
| 持仓表7列（代码/名称/持仓/现价/市值/盈亏%） | Position + Symbol | `quantity, latest_price, market_value, unrealized_pnl_pct` 全有 | ✅ 完整支持 | 无缺失 |
| 下单面板（搜索标的/数量/价格） | SimOrder / SimTrade | 提交流程已有 `submitSimOrder` | ✅ 流程支持 | 新增**搜索Symbol**前端AutoComplete即可（后端已有 `getSymbols` 接口） |
| 绩效5指标卡 + 净值曲线 | `compute_portfolio_performance` 返回 stats | `total_return_pct, sharpe_ratio, win_rate, max_drawdown_pct`；`equity_curve` + `benchmark_curve` 已返回 | ✅ 完整支持 | 交易页的绩效Tab**去掉日期选择器**，默认取最近1年即可，无需改后端 |
| 回测历史结果表 | BacktestRun | 字段`total_return, max_drawdown, sharpe_ratio, trade_count`完整 | ⚠️ 部分支持 | 缺「策略名称」「回测区间」的直接展示字段，需要从BacktestRun `start_date/end_date/run_name` 取；另外交易页Tab下的回测历史列表**需新增 仅该组合下历史run的列表API**（当前是先run再get单个详情） |
| **策略开关列表**（4策略独立启停） | 无现有模型 | ❌ **完全缺失** | ❌ 需新增 | 详见5.2新增表设计 |
| **目标占比 + 偏离分析** | Position / PortfolioMember | Position无`target_weight_pct`；PortfolioMember也无 | ❌ 需加字段 | 详见5.2 |
| **候选标的池**（信号分/目标权重/因子标签） | WorkbenchCandidate 部分 | 现有 workbench.candidates 仅有基础项 | ⚠️ 部分支持 | 缺 `signal_score`, `target_weight`, `factor_tag`, `added_at`；可复用或扩展 |
| 因子模型4配置（选股池/调仓/权重/因子权重） | PortfolioRule | 当前只有「最大仓位」类阈值字段 | ❌ 因子配置缺失 | 详见5.2 |
| 风险规则列表面板（8条规则条目） | 现有模型 | 无规则条目表，只有数值阈值 | ❌ 需新增 | 详见5.2 |
| 组合列表排名（切换/对比） | Portfolio 表 | 基础组合数据 + 各自绩效可单独查 | ⚠️ 可拼接 | 缺**批量多组合绩效聚合**API；列表页抽屉可先并行查N次，后续优化 |
| 标的详情抽屉（K线+因子） | Symbol.bars + Score.factor_* | 已有 `bars`、`factor_scores_json`、`quality_score`、`timing_score`等 | ✅ 基本支持 | 现有 `loadSymbolDetail` 可复用，抽屉只是新UI容器 |
| 再平衡触发（调仓建议） | 无现有API | ❌ 缺「推荐调仓清单」计算 | ❌ 需新增服务 | `compute_rebalance_plan(portfolio_id)` → 返回买卖清单 |

### 5.2 必需新增的数据模型/字段

#### 🔴 必需P0级新增

| 新增项 | 类型 | 说明 |
|---|---|---|
| **PortfolioMember.target_weight_pct** | 新增字段 Float | 成员目标权重（0~100%），支撑偏离分析。**P0必需**，否则组合成员Tab 策略目标占比列和偏离高亮全无法实现 |
| **Position.target_weight_pct** | 新增字段 Float（冗余/同步） | 若Member/Position两张表都需要展示，建议至少一张表有；交易页读Position，工作台读Member |
| **`auto_trade_strategies` 新增表** | 新表 | 支持策略独立开关：columns = `id, portfolio_id, strategy_key (均线跟踪/momentum/grid/tp_sl), strategy_name, enabled (0/1), last_run_at, last_run_status, params_json, created_at, updated_at` + `(portfolio_id, strategy_key)` 唯一索引。**否则 A18 无法落地** |

#### 🟡 P1级新增

| 新增项 | 类型 | 说明 |
|---|---|---|
| **PortfolioRule扩展字段** | 加列 | `universe_key`(varchar 32) / `rebalance_frequency`(varchar 16) / `weighting_method`(varchar 32) / `factor_weights_json`(Text) —— 支撑A21-A24因子配置 |
| **`portfolio_rule_items` 新表**（风险规则条目） | 新表 | 列：`id, portfolio_id, rule_key, rule_name, rule_category (factor/basic/param/fuse), severity, enabled, params_json, created_at`。支撑「风险规则列表面板」8条规则展示和可管理 |
| **`portfolio_candidates` 表**（候选池） | 新表 | 列：`id, portfolio_id, symbol_id, signal_score, target_weight_pct, factor_tag(varchar 32), added_at, status(watching/pending/joined), source_type`。替代 workbench 原 candidates 的弱结构，保证「加入组合」是正式原子操作 |
| **组合再平衡建议 API** | 新Service+Route | `POST /portfolios/{id}/rebalance/plan` 返回 `{ sells:[{symbol_id, qty, reason}], buys:[{symbol_id, qty, reason}], deviation_summary }`；`POST /portfolios/{id}/rebalance/execute` 批量下单 |
| **BacktestRun历史列表 API** | 新Route | `GET /portfolios/{id}/backtest/runs?status=finished&limit=50` 分页返回复合视图（策略名/区间/初始资金/最终权益/年化/回撤/夏普），支撑交易页「回测Tab简化历史表」 |
| **多组合绩效聚合 API** | 新Route（可选优化） | `GET /portfolios/summary?ids=1,2,3` → 批量返回 各组合累计收益/回撤/夏普，加速组合列表抽屉渲染（不做的话前端N次请求也可行） |

### 5.3 后端API变更汇总

| API | 方法 | 变更类型 | 说明 |
|---|---|---|---|
| `/portfolios/{id}` | GET | MODIFY | 响应中 需附加 target_weight_pct（成员/持仓）；因子规则扩展字段；已启用策略列表及状态 |
| `/portfolios/{id}/performance` | GET | NONE | 无需修改，已支撑5指标+双曲线 |
| `/portfolios/{id}/strategies` | GET | **ADD** | 返回该组合下所有策略的 `strategy_key / name / enabled / last_run_at / status`（交易页自动交易Tab首屏） |
| `/portfolios/{id}/strategies/{key}` | PATCH | **ADD** | 切换单策略 enabled 状态（Switch Toggle调用） |
| `/portfolios/{id}/members` | GET/POST | MODIFY | GET时返回 `target_weight_pct` + `deviation_pct`（后端直接计算好）；创建成员时可接收 target_weight_pct 参数 |
| `/portfolios/{id}/positions` | GET/PUT | MODIFY | 同member，持仓返回时附加目标权重及偏离 |
| `/portfolios/{id}/rebalance/plan` | POST | **ADD** | 计算调仓建议清单（P1） |
| `/portfolios/{id}/rebalance/execute` | POST | **ADD** | 执行调仓（P1） |
| `/portfolios/{id}/candidates` | GET/POST | **ADD** | 候选标的池CRUD；`POST /candidates/{id}/promote` → 转为正式member+持仓建议 |
| `/portfolios/{id}/backtest/runs` | GET | **ADD** | 历史回测结果列表（简化表） |
| `/portfolios` 批量绩效 | GET | MODIFY/OPTIONAL | 可选新增 summary=1 查询参数 |

---

## 六、前端组件 / 基础功能支持评估

### 6.1 现有组件复用与改造

| 现有组件 | 改造 / 复用情况 | 工作量评估 |
|---|---|---|
| `Trading.tsx` | **大重构**：布局完全重排（35%+65%），删除面板堆叠，新增下半区三Tab容器；下单面板重写（新增搜索、买卖方向、双按钮）；持仓表列简化 | 🔴 高 |
| `PortfolioWorkbench.tsx` | **大重构**：改4子Tab结构；首页=账户总览（5卡+接管卡+次组合卡+风险规则）；持仓录入表迁移/删除；暴露/规则卡迁入策略Tab | 🔴 高 |
| `PortfolioPerformancePanel.tsx` | **双版本**：交易页使用简化视图（无日期picker、无归因/复盘Tabs），工作台可保留完整视图；因此**抽离子组件**（PerformanceSummaryCards + EquityChart） | 🟡 中 |
| `AutoTradePanel.tsx` | **新增衍生组件**：原完整面板保留（策略规则Tab或回测中心保留复杂执行功能）；交易页自动交易Tab新增组件 `AutoTradeStrategyList`（策略卡+Switch），调用新策略API | 🟡 中 |
| `PortfolioBacktestPanel.tsx` | **迁移位置**：从Trading移到工作台回测中心Tab，功能不变（7.4全保留）；交易页新增精简 `BacktestHistoryTable` 组件调新历史列表API | 🟡 中 |
| `PortfolioMembersPanel.tsx` | **大扩展**：新增ActionBar（导出/再平衡/偏离过滤/搜索/添加）；表新增5列+偏离高亮；底部新增候选池子表 | 🟡 中 |
| `AppContext / useApp` | **状态扩展**：新增当前Tab（workbench/trading）、子Tab索引、策略列表状态；需确保 `portfolioId` 变更时所有子Tab联动刷新 | 🟡 中 |
| i18n翻译文件 | **新增Keys**：新增Tab名（账户总览/组合成员/策略规则/回测中心）、新术语（偏离、接管、因子芯片、次组合、再平衡、待调出等） | 🟢 低 |
| CSS / workbench.css 主题 | **适配新设计**：新增Drawer样式、Tab胶囊、次组合卡、偏离高亮、5指标卡网格、Candidate子表等；设计色板为青色主色+深色底 | 🟡 中 |

### 6.2 新增组件清单（前端）

| 组件名 | 用途 | 依赖AntD |
|---|---|---|
| `PortfolioEmptyState.tsx` | 无组合空状态引导 | Card + Button + Icon |
| `SymbolSearchSelect.tsx` | 下单面板代码搜索（可复用） | Select + AutoComplete（AntD） |
| `OrderPanel.tsx` | 交易页左35%下单面板（重构提取独立组件） | Input/Segmented/InputNumber/Button |
| `PositionCompactTable.tsx` | 交易页右65%持仓简表（7列） | Table（AntD） |
| `TradingBottomTabs.tsx` | 交易页下半区三Tab容器 | Tabs（AntD） |
| `PerformanceSummaryMini.tsx` | 交易页绩效Tab：5指标+净值曲线（精简版） | Statistic + ECharts（复用现有） |
| `AutoTradeStrategyList.tsx` | 交易页自动交易Tab：策略+开关列表 | List/Card/Switch |
| `BacktestHistoryTable.tsx` | 交易页回测Tab：历史回测列表 | Table + Select + InputNumber |
| `WorkbenchSubTabs.tsx` | 工作台4子Tab容器（账户总览/成员/策略/回测） | Tabs |
| `AccountOverviewTab.tsx` | 工作台-账户总览Tab：5卡+接管卡+次组合+风险规则 | Row/Col/Grid + Statistic/Typography |
| `StrategyRulesTab.tsx` | 工作台-策略规则Tab：因子模型+条件风控双栏 | Form/Select/Tag/Chip |
| `MemberDeviationTable.tsx` | 工作台-组合成员Tab扩展版（偏离列+高亮） | Table + Switch + Search |
| `CandidatePoolTable.tsx` | 成员Tab底部候选标的池表格 | Table + Tag |
| `RiskRuleList.tsx` | 账户总览Tab底部8项风险规则列表 | List/Avatar/Tag |
| `PortfolioListDrawer.tsx` | 组合列表抽屉 | Drawer + Table + Badge/Rank |
| `SymbolDetailDrawer.tsx` | 标的详情抽屉 | Drawer + ECharts迷你图 + Descriptions |
| `StrategyToggleSwitch.tsx` | 策略级独立开关（带运行状态色） | Switch + Tag/Tooltip |

### 6.3 技术可行性 & 风险评估

| 风险项 | 风险等级 | 说明 | 缓解方案 |
|---|---|---|---|
| **数据缺失：偏离与目标权重** | 🔴 高 | 组合成员Tab是P0核心，缺 target_weight 全失效 | 先加字段并给默认值（按现有position_pct回填=目标，保证初次打开有数据） |
| **数据缺失：策略开关** | 🔴 高 | 交易页自动交易Tab P0必需独立4策略开关 | 前端先本地mock开关状态（localStorage），后段表+API后续补；或先展示静态UI+禁用开关（不阻塞主流程） |
| **布局重构引发回归** | 🟡 中 | Trading/Workbench 两组件改动量大，可能影响子组件渲染 | 保留旧路由+feature flag（如 `?redesign=1`）切换，灰度发布 |
| **回测中心Tab性能** | 🟡 中 | 回测面板功能复杂，迁移后首屏加载慢 | 与原实现一致，4子Tab采用 **lazy loading + Suspense**，非首屏Tab不加载 |
| **Drawer性能影响** | 🟢 低 | 新增2大Drawer（列表/详情） | AntD Drawer原生支持 destroyOnClose + unmountOnExit，不常驻 |
| **i18n遗漏** | 🟢 低 | 新术语多，翻译Key补全不及时 | 先中文字面（不翻译包）上线，后补全 zh / en Keys |

---

## 七、实施优先级划分（建议分批）

### 7.1 P0 —— 第一批：核心结构落地（不可缺，阻断交付）

**目标**：完成信息架构改造 + 两主Tab骨架 + 首屏可使用
1. **D1-D4, D8-D11** —— 删除 Trading/Workbench 原堆叠结构中不需要的调用与卡片
2. **M1, M2（三Tab容器）** —— 交易页左右布局（下单35%+持仓65%）+ 下半区三Tab空壳
3. **M3（①账户总览 ②组合成员 两Tab）** —— 工作台4Tab中先做前两个P0 Tab
4. **M4（5列资产卡+自动接管配置卡）** —— 账户总览Tab核心两卡
5. **M5（偏离分析基本版：目标占比列+偏离值）** —— 组合成员Tab扩展列（需后端先加 `target_weight_pct` 字段）
6. **A1（无组合空状态）** —— 空数据场景体验
7. **A14（持仓行回填下单面板）** + **A15（偏离过滤Toggle）**
8. **A18（策略开关列表UI）** —— 交易页自动交易Tab策略列表，先做前端Switch UI，后端缺API时可本地mock或禁用
9. 数据基础：**PortfolioMember/Position 加 target_weight_pct 字段 + 回填默认值脚本**

### 7.2 P1 —— 第二批：功能闭环（不阻塞主流程但需完成）

**目标**：工作台后两Tab落地 + 新交互闭环 + 抽屉体系
1. **M3（③策略规则Tab ④回测中心Tab）** —— 因子配置+风控 双栏；回测面板迁移
2. **M5高级版（偏离高亮行/一键清仓/候选池子表 A8）** —— 组合成员Tab剩余功能
3. **A9（风险规则列表 8项）** —— 账户总览Tab底（新增 `portfolio_rule_items` 表）
4. **A11（组合列表抽屉）** + **A19（快速切换组合）**
5. **A12（标的详情抽屉）** —— 替代原详情跳转
6. **A16（再平衡计划+执行API）** + **A17（候选加入组合）** + **A20（一键清仓）**
7. **A7（交易页回测Tab简化历史表）**
8. 数据基础：**`auto_trade_strategies` 表 + 单策略启停API；`portfolio_candidates` 表；因子模型扩展字段**

### 7.3 P2 —— 第三批：体验完善（可迭代上线后补）

1. **M6（一级Nav/Header统一视觉规范）** 应用主框架全局一致性
2. **A10（次组合关联卡）** —— 多组合联动概念
3. **A21-A24（因子配置高级版）** 完整4项配置+权重芯片编辑弹窗
4. **组合绩效批量聚合API优化**（组合列表抽屉加载速度优化）
5. 完整i18n Key补全 + 多语言回测
6. 回归测试（全部旧功能无阻断）

---

## 八、当前项目数据 & 功能**是否支持**快速结论

| 问题 | 结论 | 备注 |
|---|---|---|
| **组合、持仓、下单、绩效计算、回测引擎**核心链路是否完整？ | ✅ **完整支持** | 后端服务 `portfolios.py` + `compute_portfolio_performance` + `run_auto_trade` + Backtest引擎均已就绪，业务逻辑无需重写 |
| **新设计中新增的"目标权重/偏离分析"** 当前是否有数据？ | ❌ **无字段** | 必须扩表 `target_weight_pct` + 回填脚本（P0级阻塞项） |
| **新设计中"策略独立开关（4策略）"** 当前是否支持？ | ❌ **组合级只有auto_trade_enabled总开关** | 无策略级粒度；要么后端新表（最佳），要么前端临时mock先上线（可接受） |
| **工作台4子Tab、交易页3子Tab** 前端组件框架能否支撑？ | ✅ **AntD Tabs完全支持** | 仅工作量问题，无技术障碍 |
| **候选标的池、风险规则列表、因子配置项** 新数据是否都要新表？ | ⚠️ **部分可复用** | 风险规则列表可先硬编码8项静态展示（不阻塞UI）；候选池可复用workbench candidates加字段；因子配置可先存在 `PortfolioRule` 扩展列 |
| **新增Drawer（组合列表、标的详情）** 是否需要新业务逻辑？ | ⚠️ **部分需要** | 组合列表需批量绩效聚合API（可先N次查单个，优化后置）；标的详情复用 `loadSymbolDetail` 即可 |
| **现有测试文件**会受影响吗？ | 🔴 **影响较大需升级** | 6个相关test：`Trading.test.tsx` / `PortfolioWorkbench.test.tsx`（含子test）/ 绩效/自动交易/回测/成员 Panel 测试。重构后测试必须更新（Mock新数据+断言新结构） |

---

## 九、推荐下一步行动

1. **确认优先级划分**（P0→P1→P2）是否符合预期，特别确认「策略开关」是否允许P0前端mock上线
2. **P0数据先行**：先执行数据库变更：
   - ALTER TABLE portfolio_members ADD COLUMN target_weight_pct FLOAT DEFAULT NULL;
   - ALTER TABLE positions ADD COLUMN target_weight_pct FLOAT DEFAULT NULL;
   - 回填脚本：target_weight_pct = position_pct（首次近似值）
3. **确认P0范围是否包括 组合成员Tab的偏离过滤+高亮黄底**（如果数据字段到位，前端渲染很简单）
4. **确认新建组合账户类型**是否新增「信用账户=credit」枚举（后端Portfolio.account_type现仅 simulated/normal？需检查）
5. **开发模式建议**：采用 feature flag（URL参数 `?redesign=1` 或 localStorage）切换新旧版本，便于并行开发和逐步回归
