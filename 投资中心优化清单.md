## 投资中心优化计划需求文档

### 1. 背景

当前系统已经具备个人投资决策的核心雏形：标的搜索、K 线展示、股质评分、时点评分、机会挖掘、交易计划、仓位约束、模拟交易、消息面和复盘记录。

现阶段最需要优化的不是继续堆叠复杂功能，而是把“系统给出的建议”变成“用户可以调整、验证、执行、复盘”的工作流。投资中心应从只读分析页升级为个人决策工作台。

核心目标：

- 快速判断股票/ETF 是否值得关注。
- 明确当前阶段、买入区间、止损、目标和仓位上限。
- 支持用户按自己的风险偏好调整交易计划。
- 用轻量回测验证规则是否可靠。
- 保留规则、参数和交易计划的可追溯记录。

---

### 2. 产品定位

投资中心不是专业机构级量化平台，而是个人投资辅助系统。

它应该优先解决以下问题：

1. 这只股票/ETF 当前值不值得看？
2. 如果买，合理买区在哪里？
3. 最大应该买多少？
4. 错了在哪里止损？
5. 对了在哪里止盈或减仓？
6. 这个买卖规则过去是否大致有效？
7. 当前建议是系统默认结论，还是我手动调整后的计划？

因此，本轮优化重点是“可执行性”和“可验证性”，高级图表、复杂归因、多标的对比等能力后置。

---

### 3. 当前系统判断

#### 3.1 已具备能力

- 投资中心已能展示单标的分析视图。
- 后端已有交易计划生成逻辑。
- 系统已有股质评分、时点评分、阶段判断和动作建议。
- 已有机会挖掘和评分排序能力。
- 已有模拟账户、模拟订单和模拟成交表。
- 已有信号规则和历史相似信号统计。
- 前端已有 K 线、MACD、RSI、BOLL、ATR 等指标计算能力。
- 技术指标已有基础展示，但缺少统一的信号汇总、可配置阈值和新增指标扩展。
- 已有仓位规则和资金预算相关逻辑。

#### 3.2 主要短板

- 交易计划多数为只读，用户不能直接调整关键参数。
- 风险偏好和预警阈值存在硬编码，不能体现个人交易风格。
- 交易计划区域信息密度高，阅读和操作成本偏高。
- 历史相似信号统计不等于完整回测，无法模拟资金、持仓和买卖执行。
- 信号规则只有配置能力，缺少多套规则管理和结果验证闭环。
- 技术指标目前偏展示，尚未形成“指标信号 -> 规则条件 -> 回测验证 -> 交易计划”的闭环。
- InvestmentCenter 组件过大，后续迭代维护成本偏高。

---

### 4. 优先级总览

| 优先级 | 模块 | 目标 | 说明 |
| --- | --- | --- | --- |
| P0 | 交易计划可编辑 | 让系统建议可被用户调整 | 立刻提升个人决策可用性 |
| P0 | 风险/预警参数可配置 | 让仓位和提醒符合个人风格 | 避免硬编码误导 |
| P0 | 交易计划 Tab 化 | 降低投资中心信息噪音 | 为后续功能扩展留空间 |
| P1 | 轻量回测 | 验证规则过去是否有效 | 不做复杂机构级回测 |
| P1 | 规则表单与多规则管理 | 支持不同交易风格 | 如稳健趋势、短线反弹、ETF轮动 |
| P1 | 交易成本建模 | 提升模拟和回测可信度 | 佣金、印花税、滑点 |
| P1 | 技术指标增强 | 丰富买卖点判断依据 | 新增 KDJ/成交量确认/相对强弱等指标 |
| P2 | 分批计划编辑 | 细化执行计划 | 在主计划可编辑后再做 |
| P2 | 组件拆分 | 降低维护成本 | 配合功能改造推进 |
| P3 | 高级分析和图表交互 | 增强体验 | 多标的对比、归因、基准对比等 |

---

## 5. P0：投资中心可执行化

### 5.1 交易计划参数可编辑

#### 目标

把交易计划从“系统只读建议”升级为“可调整的个人计划”。

#### 范围

交易计划区支持编辑以下字段：

- 买入区间下限 `entry_min`
- 买入区间上限 `entry_max`
- 止损价 `stop_loss`
- 目标价 `target_price`
- 建议仓位比例 `recommended_position_pct`
- 计划投入金额 `recommended_position_amount`

#### 前端要求

- 在交易计划区域增加“编辑计划”按钮。
- 点击后进入编辑模式。
- 编辑模式下关键数值变为输入框。
- 提供“重新计算”和“取消”按钮。
- 修改后实时提示风险收益变化。
- 用户覆盖过的字段需要有明显标识，例如“手动调整”。

#### 后端要求

- `POST /trade-setups/generate` 支持 `overrides` 参数。
- 后端仍生成默认计划，但允许使用用户覆盖值重新计算。
- 返回结果中标记字段来源：
  - `system`：系统计算
  - `manual`：用户覆盖
- 保存交易计划时保留覆盖参数，方便复盘。

#### 验收标准

- 用户可以修改买入区间、止损、目标和仓位。
- 点击重新计算后，风险收益、分批计划、计划金额同步变化。
- 刷新页面后，已保存的手动计划仍可看到。
- 系统默认计划和手动调整计划能区分。

---

### 5.2 风险仪表盘参数可调

#### 目标

让风险判断符合用户自己的风险承受能力。

#### 范围

支持配置：

- ATR 止损倍数：`1x / 1.5x / 2x / 3x`
- 单标最大仓位比例
- 单赛道最大仓位比例
- 单次交易最大亏损比例
- 最大可用仓位比例

#### 前端要求

- 在风险仪表盘附近增加风险参数设置入口。
- 参数变化后，风险卡片实时重算。
- 默认值沿用当前系统逻辑。
- 支持保存到 localStorage 或后端配置表。

#### 验收标准

- 修改 ATR 倍数后，止损距离和风险提示同步变化。
- 修改单标仓位阈值后，仓位集中度判断同步变化。
- 刷新后保留用户配置。

---

### 5.3 价格预警阈值自定义

#### 目标

避免固定阈值导致提醒过多或过少。

#### 范围

支持配置：

- RSI 超买阈值
- RSI 超卖阈值
- 距离止损价提醒阈值
- 距离目标价提醒阈值
- 是否启用各类提醒

#### 前端要求

- 增加预警设置面板。
- 每类预警有开关和阈值输入。
- 当前触发的预警应展示触发依据。

#### 验收标准

- 用户关闭某类预警后，该类提醒不再出现。
- 用户修改阈值后，提醒立即按新规则刷新。
- 预警文案说明触发原因。

---

### 5.4 交易计划区域 Tab 化

#### 目标

降低交易计划区域的信息密度，让用户更快找到重点。

#### 建议 Tab

1. **计划概览**
   - 买入区间
   - 止损
   - 目标
   - 建议仓位
   - 风险收益比

2. **收益预演**
   - 悲观情景
   - 基准情景
   - 乐观情景

3. **分批执行**
   - 分批节点
   - 每批仓位
   - 触发条件

4. **未来计划**
   - 通用/短期/中期/长期/自定义场景
   - 回撤买入计划

#### 验收标准

- 默认展示“计划概览”。
- 用户能快速切换各 Tab。
- 移动端不出现内容挤压和横向溢出。

---

## 6. P1：轻量规则验证与回测

### 6.1 轻量回测引擎

#### 目标

验证当前评分和买卖规则在历史数据中是否大致有效。

#### 第一版边界

第一版只做日线级轻量回测，不做分钟级、不做复杂撮合、不做高频策略。

支持：

- 单标的回测
- 候选池批量回测
- 日线买卖执行
- 固定止盈/止损
- 最大持有天数
- 固定仓位或按风险预算反算仓位

暂不支持：

- 分钟级回测
- 复杂挂单撮合
- 多账户组合回测
- 高频交易
- 高级绩效归因

#### 后端建议

新增 `app/services/backtest.py`。

核心输入：

- 标的范围
- 开始日期
- 结束日期
- 买入规则
- 卖出规则
- 仓位规则
- 交易成本参数

核心输出：

- 交易流水
- 每日权益曲线
- 总收益率
- 最大回撤
- 胜率
- 盈亏比
- 平均持有天数
- 交易次数

#### 数据表建议

可新增：

- `backtest_runs`
- `backtest_trades`
- `backtest_equity_curve`

也可第一版先复用轻量结构，避免过度设计。

#### 验收标准

- 用户能选择一只标的并运行回测。
- 回测能输出交易明细和核心指标。
- 回测结果可用于判断规则是否值得继续使用。

---

### 6.2 规则表单

#### 目标

让用户不用写代码，也能配置买卖规则。

#### 第一版规则字段

买入条件：

- 质量分大于 N
- 时点分大于 N
- 阶段属于 start/accel/cooldown/overheat
- 动作为 open/hold/buy_dip/reduce/exit
- RSI 小于 N
- 价格站上 MA20

卖出条件：

- 止盈比例
- 止损比例
- 最大持有天数
- RSI 大于 N
- 跌破 MA20

仓位规则：

- 固定金额
- 固定仓位比例
- 按风险预算反算

#### 实现建议

- 第一版用 Ant Design Form，不做拖拽规则编辑器。
- 条件之间先支持 AND。
- OR 和复杂嵌套逻辑后续再做。

#### 验收标准

- 用户能创建一套规则。
- 用户能用该规则跑轻量回测。
- 回测结果能关联到规则。

---

### 6.3 回测结果展示

#### 第一版展示

- 核心指标卡片
- 权益曲线
- 交易流水表
- 按标的汇总收益

#### 暂缓展示

- 月度收益热力图
- 行业归因
- 阶段归因
- 基准超额收益

#### 验收标准

- 用户能看懂这套规则过去赚不赚钱、回撤多大、交易频率多高。
- 用户能定位每一笔买入和卖出。

---

### 6.4 交易成本建模

#### 目标

让模拟交易和回测结果更接近真实交易。

#### 参数

- 佣金费率
- 最低佣金
- 印花税
- 滑点比例或固定滑点

#### 验收标准

- 回测结果扣除交易成本。
- 成交记录显示费用。
- 用户可以修改交易成本参数。

---

### 6.5 技术指标增强

#### 目标

在现有 MACD、RSI、BOLL、ATR 的基础上，增加更适合个人买卖点判断的技术指标，并把指标从“图表展示”升级为“信号解释”和“规则条件”。

#### 第一批新增指标

优先新增：

- KDJ：用于辅助判断短线超买、超卖和拐点。
- 成交量确认：放量突破、缩量回踩、异常放量。
- 相对强弱：标的相对沪深300、中证500或同类 ETF 的阶段强弱。

后续可选：

- CCI：辅助识别趋势加速和极端偏离。
- OBV：观察量价累积方向。
- 52 周高低位：判断价格所处长期区间。

#### 前端要求

- 技术指标面板支持选择显示哪些指标。
- 新增指标要有简短信号解释，例如“短线超卖”“放量突破”“弱于基准”。
- 指标阈值可配置，默认值使用常见参数。
- 指标信号应能在投资中心和回测规则中复用。

#### 后端要求

- 如果指标只用于展示，可先由前端计算。
- 如果指标参与回测或机会扫描，应在后端提供一致计算逻辑，避免前后端结果不一致。
- 回测规则需要支持技术指标条件，例如 KDJ 金叉、放量突破、相对强弱大于 N。

#### 验收标准

- 投资中心能展示新增技术指标及其信号解释。
- 用户可以配置关键阈值。
- 至少 KDJ 和成交量确认可以作为回测规则条件。
- 指标信号不会替代股质评分和时点评分，而是作为买卖点确认依据。

---

## 7. P1：规则管理

### 7.1 多规则组合管理

#### 目标

支持用户保存多套不同交易风格。

示例：

- 稳健趋势规则
- 短线反弹规则
- ETF 轮动规则
- 高股质回调规则

#### 要求

- 规则可新增、复制、编辑、删除。
- 每套规则有名称、描述、适用资产类型。
- 每套规则可单独回测。
- 可设置一套默认规则用于投资中心。

#### 验收标准

- 用户可以在投资中心切换规则。
- 切换规则后，交易计划和预警结果可重新计算。
- 回测结果能追溯到对应规则。

---

### 7.2 规则导入导出

#### 目标

方便备份和迁移。

#### 要求

- 支持导出 JSON。
- 支持从 JSON 导入。
- 导入时校验字段合法性。

#### 验收标准

- 导出的规则能再次导入并正常使用。
- 非法 JSON 不会破坏现有配置。

---

## 8. P2：执行计划增强

### 8.1 分批计划编辑

#### 目标

让用户细化执行节奏。

#### 范围

支持编辑：

- 分批名称
- 每批仓位比例
- 触发条件
- 是否启用该批次

#### 验收标准

- 每批仓位总和不能超过计划仓位。
- 修改分批后，总计划金额同步变化。
- 保存后可在复盘中查看当时的分批计划。

---

### 8.2 未来买入计划增强

#### 目标

让预设场景也可以微调。

#### 范围

支持调整：

- 观察天数
- 回撤比例
- 仓位缩放比例
- MA 附近买入带宽

#### 验收标准

- 通用、短期、中期、长期、自定义场景都可微调。
- 修改后图表上的未来买入区间同步刷新。

---

## 9. P2：工程维护优化

### 9.1 InvestmentCenter 组件拆分

#### 目标

降低后续维护成本。

#### 建议拆分

- `ICSearch`
- `ICChart`
- `ICRiskDashboard`
- `ICTradePlan`
- `ICTradePlanTabs`
- `ICPriceAlerts`
- `ICRuleSelector`
- `ICBacktestPanel`

#### 原则

- 不为拆分而拆分。
- 优先在实现交易计划可编辑和 Tab 化时顺手拆。
- 保持现有交互不回退。

#### 验收标准

- 主组件明显变薄。
- 核心状态边界清晰。
- 构建通过，现有功能不受影响。

---

## 10. P3：高级增强

以下能力有价值，但不建议当前阶段优先做：

- 多标的对比模式。
- 图表趋势线绘制。
- 搜索历史和收藏管理弹窗。
- 回测绩效归因。
- 基准指数对比。
- 回测结果缓存。
- 回测异步任务队列。
- 分钟级数据和日内回测。

这些功能应等 P0/P1 稳定后再逐步推进。

---

## 11. 推荐实施路线

### 阶段 A：投资中心可执行化

优先级：最高

范围：

- 交易计划参数可编辑。
- 风险参数可调。
- 价格预警阈值自定义。
- 交易计划 Tab 化。

交付结果：

- 用户可以基于系统建议生成自己的交易计划。
- 投资中心从只读分析页升级为可操作决策页。

---

### 阶段 B：轻量回测验证

优先级：高

范围：

- 日线级轻量回测。
- 规则表单。
- 基础回测结果展示。
- 交易成本建模。
- 技术指标增强。

交付结果：

- 用户能验证某套规则过去是否有效。
- 系统建议不再只是当前评分，而有历史表现作为参考。

---

### 阶段 C：多规则和执行增强

优先级：中

范围：

- 多规则管理。
- 规则导入导出。
- 分批计划编辑。
- 未来买入计划增强。

交付结果：

- 用户能沉淀自己的交易方法。
- 不同市场风格可使用不同规则。

---

### 阶段 D：高级分析和体验增强

优先级：低

范围：

- 多标的对比。
- 绩效归因。
- 基准对比。
- 图表高级交互。
- 回测缓存和异步任务。

交付结果：

- 系统从个人决策工具逐步增强为更完整的个人量化工作台。

---

## 12. 第一批建议开发任务

建议下一步直接从以下任务开始：

1. 交易计划 Tab 化。
2. 交易计划参数可编辑。
3. 风险参数设置面板。
4. 预警阈值设置面板。

这四项完成后，投资中心的实用性会明显提升，并且不会过早引入复杂回测架构。

---

## 13. 非目标

当前阶段不做：

- 高频交易。
- 分钟级回测。
- 自动实盘下单。
- 复杂拖拽式策略编排。
- 机构级组合归因。
- 多账户资金管理。

系统仍以个人辅助决策为边界，所有结果只作为投资参考，不作为自动交易指令。




