E2E 需先启动后端 `python -m uvicorn app.main:app --port 8000` 与前端 `npm run dev`，再执行 `pytest tests/e2e/ -v -m e2e`；性能测试通过 `pytest tests/performance/test_discovery_5500_sla.py -m performance --no-header` 运行，开发环境默认对慢测试 xfail，需加 `--hardware-capability=prod` 强制真实执行。