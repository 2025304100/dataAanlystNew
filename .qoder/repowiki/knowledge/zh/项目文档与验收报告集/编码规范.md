- 方案类文档统一以 `<模块名>-<主题>-plan.md` 或 `<主题>-final-plan.md` 命名，并在标题中标注版本或日期以便追溯。
- 验收与回归类文档采用章节编号（0~N）+ 子节（X.Y）+ 带 `[ ]` 复选框的检查项格式，便于手工逐项打勾。
- 数据库迁移相关文档使用独立 JSON 文件（如 `migration-baseline-YYYY-MM-DD.json`）记录表级数量快照，并在 notes 中声明仅用于估算、不得写入业务逻辑。
- 面向同一功能的多个阶段产出（计划、评审标准、测试用例、最终方案、验收报告）集中在同一目录并按语义后缀区分，形成完整交付链。