可通过设置环境变量 `ALEMBIC_DATABASE_URL` 覆盖默认数据库连接；SQLite 模式下会自动把相对路径转为绝对路径并在首次打开时创建父目录；迁移必须同时兼容 SQLite 和 MySQL，禁止在迁移脚本中写死数据库选择。常用命令：`alembic revision --autogenerate -m "..."`、`alembic upgrade head`、`alembic downgrade -1`、`alembic current`。