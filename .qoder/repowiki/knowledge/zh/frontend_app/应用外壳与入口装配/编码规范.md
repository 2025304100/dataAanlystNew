- 通过 `ctx.activeTab` 在 App 壳层内以条件渲染方式切换一级视图，新增 Tab 需同步更新导航按钮与渲染分支。
- 全局上下文使用 `useApp()` 从 `./context/AppContext` 获取，避免在子组件中直接 import 状态。
- 用户可见文案统一通过 `t(...)` 调用 i18n，不硬编码字符串。
- 入口文件统一使用 `ReactDOM.createRoot` + `React.StrictMode` 挂载，并通过 Antd `ConfigProvider` 集中配置主题色 `#0f766e`。