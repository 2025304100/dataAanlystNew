- 测试按 pytest marker 分类（whitebox/blackbox/e2e），CI 各 job 通过 `-m <marker>` 选择执行范围。
- CI job 统一使用 actions/checkout@v4、actions/setup-python@v5、actions/setup-node@v4 标准 action 版本，保持环境一致性。
- 需要网络服务的 job 采用“后台启动服务 + sleep + health 探测”的启动模式，确保下游测试开始前服务可用。