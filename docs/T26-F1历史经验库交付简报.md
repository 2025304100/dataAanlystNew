# T26 · F1 历史经验库交付简报

**状态**：✅ 完成（2026-09-18 17:11，PROGRESS T26 done）
**DoD**：`pytest tests/services/factors/experience/ -q` → **57 passed, exit 0**

## 交付物

| 层 | 文件 | 内容 |
|---|---|---|
| 表 | `alembic/versions/2026_09_18_0058_wps_0023_058_f1_experience_tables.py` | 4 表新建（显式 InnoDB、无 FK 照设计 §3.12、索引名与 ORM 一致） |
| ORM | `app/models/factor_experience.py` | 主表 19 列 + Tags / Metrics / FieldDeps 3 子表（自动扫描注册） |
| 服务 | `app/services/factors/experience/{generalization,fingerprint,service}.py` | 参数泛化、三层指纹、存回/抽取/相关 |
| DTO | `app/schemas/factor_experience.py` | 3 接口请求/响应契约 |
| 路由 | `app/api/routes/factor_experience.py` + `app/api/router.py` 挂载 | POST `/api/v1/factor-experience`、GET `…/sample`、GET `…/related` |
| 测试 | `tests/services/factors/experience/`（5 文件 57 用例） | 泛化 / 指纹 / 存回 / 抽取 / HTTP 全覆盖 |

## 核心口径（与文档 §3.12 严格对齐）

- **泛化**：只泛化 **Call 参数位置**的数值常量（窗口类）；算术操作数是结构常数
  保留字面（文档示例 `mean(close,20)/mean(close,60)-1` → 模板保留 `-1`）。
- **指纹**：三层 = 结构哈希（数值归一）+ 算子/字段统计 + 语义桶，全确定性；
  同构不同参数 → 同指纹 → 存回幂等返回 `duplicate`。
- **抽取**：负样本（`is_negative_sample=1`）与 archived 永不参与；字段硬过滤 →
  场景打分（`2×env + 2×pool + success_rate + max(0, avg_icir)`）→ 加权不放回 → 实例化。
- **数值纪律**：metrics / avg_icir 全过 `db_numeric.to_db_float`，NaN→None 不归 0。

## 验证链（全绿）

1. DoD 57 passed（首跑 11 failed → 修泛化规则 + 改异构造数后全绿）
2. 迁移双向验证：scratch 库 upgrade head（4 表 + 7 索引 + 19 列对齐 ORM）→ downgrade -1 干净消失
3. 真实库应用：alembic `wps_0023_055 → head`（056 TD6 幂等 + 058 新表）
4. `verify_schema_drift`：4 表无漂移、InnoDB（首跑 charset WARN 已修——迁移不显式声明 charset）
5. 真实库写入试探：NaN→None、四表同落、抽取命中、清理零残留
6. C5 红线静态扫描：`mining/**` 对 F1 零引用（挖掘只经 HTTP）
7. 防波及：`import app.main` OK、3 路由在位；g1 + mining 抽查 26 passed

## 遗留 / 观察

- `strategy_execution_snapshots` FK `RESTRICT vs NO ACTION` 漂移为**既有债**
  （A 类 FK 补齐批 P1 范围，非本卡引入）。
- related 接口本期实现不调用（F2 二期启用）；管理接口（列表/详情/归档）属 M2。
