# 因子挖掘实验向导详细设计

> 版本：v2.0（遗传算法范式重构版）  
> 日期：2026-09-08  
> 定位：在现有"设置 → 因子中心"内增加因子挖掘实验向导，采用遗传算法范式（初始种群→逐代进化→收敛验证），不新增独立桌面应用；不负责因子模型训练

## 1. 产品目标

用户通过一个连续向导完成一次可复现的因子挖掘实验，采用**遗传算法范式**：

```text
候选池 → 时间与目标 → 字段与校验 → 进化参数 → 进化执行与结果
```

核心流程：**初始种群生成 → 逐代进化（选择→变异→交叉→评估）→ 收敛判断 → 最终全量验证 → 结果输出**。

每一步保存草稿；只有当前步骤校验通过才能进入下一步。关闭页面不会丢失配置。最终配置形成不可变实验快照，后续结果可以复现。资源确认不再单独占用步骤，在提交挖掘实验时以弹窗完成。

**与枚举范式的区别：** 候选因子不是一次性生成全部，而是生成初始种群后逐代进化优化；每代用短样本快速评估，最终Top因子用全量验证；进化过程中的历史最优因子全部保留。

## 2. 步骤条交互

采用横向步骤条：已完成为蓝色、当前步骤为蓝色并显示进度、阻断步骤为红色、未到步骤为灰色。每一步显示标题、简短状态和错误数量。

步骤不可随意跳过；允许返回修改。修改上游步骤后，下游结果自动标记“需重新校验”，不能继续使用旧校验结果。

当前生效步骤：

1. 候选股票池
2. 时间与目标
3. 字段与校验
4. 进化参数
5. 进化执行与结果

原设计的 8 步映射：原 Step 1 保持为新 Step 1；原 Step 2 与 Step 5 合并为新 Step 2；原 Step 3 与 Step 4 合并为新 Step 3；原 Step 6 为新 Step 4（进化参数）；原 Step 7 降为提交时资源确认弹窗；原 Step 8 与训练结果总览合并为新 Step 5（进化执行与结果）。模板的"直接开始"只能跳过已完成的填写页面，不能绕过候选池快照、字段校验、最终校验或资源确认。

## 3. Step 1：候选股票池

### 3.1 入口选择：两种方式互斥

进入候选池步骤后显示顶部 Tab：“条件筛选”（默认）和“导入股票池”。两种方式仍然严格互斥；切换 Tab 前必须确认清空当前方式的未保存配置，切换后页面内容完全替换，不同时展示或合并两套条件。方式一旦保存为候选池即写入 `source_type`，之后不可切换；改变方式必须新建候选池。

**方式 A：外部导入**

- 提供下载导入模板，支持 CSV 和 Excel；模板至少包含 `symbol`（股票代码，必填）、`name`（可选）、`market`（可选）、`note`（可选）。
- 股票代码是唯一匹配键，导入文件中的名称和市场只用于校验提示，不覆盖系统主数据。
- 导入后通过本系统 `symbols`/资产主数据匹配，返回成功、未匹配、重复、格式错误、非股票资产和已停牌等逐行结果。
- 未匹配代码不能进入候选池；用户可下载错误明细，修正后重新导入。
- 导入方式不提供板块、PE、ST 等二次筛选控件；如需按条件筛选，应返回入口选择“方式 B”。

**方式 B：条件过滤**

- 使用本系统行情、估值、财报、资产标签和交易状态数据构建范围，不要求用户导入代码。
- 支持指数成分（沪深 300、中证 500、创业板指、科创 50 等）、市场板块、自定义条件组合和已保存条件复制。
- 过滤条件只作用于当前生成的候选池，不修改 symbols、行情或财报主数据。

方式选择一经创建即写入候选池的 `source_type`（`import`/`filter`），后续不可切换；需要改变方式必须新建候选池。

### 3.2 条件过滤页：分层布局与交互

首期训练范围固定为 A 股（上交所、深交所、北交所及其板块）；不提供地域筛选。后续扩展港股、美股等市场时，再增加独立的市场/地域能力和对应数据门禁，不能通过本期配置绕过。

条件过滤页采用“左侧常用区间 + 右侧分类条件”的双栏布局；候选结果、已选条件和规则排除统计固定在页面底部，不把所有控件堆在同一平面。

**左侧：常用区间选项卡**

左栏仅服务于有连续数值或时间范围的条件，提供可一键应用、可取消的预设。用户点击预设后，右侧对应分类自动展开并回填为可编辑的最小值、最大值或天数；手工修改后预设状态显示为“自定义”，不得再误显示为已选预设。预设不可覆盖用户未确认的其他条件。

| 选项卡 | 预设项 | 落地口径 |
|---|---|---|
| 市值 | 全市场、大盘、中盘、小盘、微盘 | 以数据截止日的总市值或流通市值为准；阈值必须由后端按当日目标市场可用样本分位数计算并连同版本写入快照，不能把固定金额硬编码成跨时期标准。 |
| 估值 | 低估值、中低估值、中等估值、高估值；PE/PB 非正值排除 | PE_TTM、PB 均支持各自选择；预设使用当日正值样本分位数，缺失和非正值处理沿用右侧字段的明确选择。 |
| 流动性 | 高流动性、中等流动性、低流动性 | 使用最近 N 个有效交易日的日均成交额；N 默认 20，允许调整。预设按目标市场可用样本分位数计算，不使用单一绝对金额。 |
| 上市时间 | 已上市满 6 个月、1 年、3 年、5 年 | 使用上市日至数据截止日的实际交易日数，不以自然日替代；默认“满 120 个交易日”。 |

预设接口返回 `preset_code`、字段、运算符、计算口径、样本范围、分位数边界、`as_of_date` 和数据版本。提交快照时保存展开后的实际规则值及上述元数据，避免后续“低估值/大盘”因市场变化而无法复现。

**右侧：纵向可折叠分类**

右栏按以下顺序纵向排列 Accordion。每个分类独立展开/收起，标题行显示“已选条件数”和清除本分类按钮；分类之间使用全宽分隔线。首次进入只展开“市场与交易状态”，其余保持收起；已有条件的分类必须自动展开。分类内按“开关/多选 -> 范围 -> 缺失处理 -> 口径说明”排列，范围输入统一使用最小值/最大值双输入，单位紧随控件显示。

| 顺序 | 分类 | 条件 |
|---|---|---|
| 1 | 市场与交易状态 | 上证主板、深证主板、创业板、科创板、北交所，可多选；指数成分；排除停牌、退市整理、已退市；是否允许上市未满 N 个交易日。 |
| 2 | 风险标记 | 排除 ST、*ST、退市风险警示；名称/风险标签取候选池快照，不使用当前名称回填历史。 |
| 3 | 估值与市值 | 总市值、流通市值、PE_TTM、PB、股息率的范围；每一字段独立选择“缺失排除/缺失保留”。左栏预设应用后在此处可见且可细调。 |
| 4 | 盈利质量 | 近 1/2/3 年是否亏损、连续亏损年数、净利润同比、ROE_TTM 范围。财报条件仅对通过公告日 PIT 和覆盖率门禁的标的/日期生效。 |
| 5 | 流动性 | 近 N 日日均成交额、日均成交量、日均换手率最低值；N 为 5/10/20/60 或自定义整数。 |
| 6 | 上市时间 | 上市满 N 个交易日；默认 120。左栏预设应用后仍允许改为精确天数。 |
| 7 | 行业 | 行业支持当前主数据的行业包含/排除；首期仅用于 A 股候选池创建时的静态筛选。当前行业不具备历史分类版本，不得用于训练期逐日 PIT 筛选、行业中性化或行业横截面填充。 |

每次编辑先更新本地草稿；条件筛选变化后 300ms 防抖调用后端预览接口，底部实时更新命中数量、排除统计和示例代码。点击“生成候选池”时由后端按数据截止日再次完整计算并冻结快照。预览区按分类展示命中/排除数量，并说明某字段因缺失、PIT 门禁或数据版本不可用而未参与筛选的数量。前端不得自行按缓存结果筛选或计算分位数；防抖预览结果不能直接作为训练快照。

### 3.3 必须支持的过滤条件与数据边界

所有区间均包含端点；未填一端表示无该侧边界。范围冲突（最小值大于最大值）、不支持的市场/行业值、无可用数据的预设及字段覆盖率不达标，均在预览时以结构化错误阻断快照生成。

| 分类 | 数据边界 |
|---|---|
| 市值/估值 | 市值、PE_TTM、PB、股息率按指定交易日可见快照计算；未注册 DSL 或未通过 PIT/readiness 的字段可用于候选筛选时也必须标注“仅候选池”，不得自动进入训练公式。 |
| 流动性 | 仅使用数据截止日前已完成的有效交易日；停牌日不计入 N 日窗口，并返回实际有效天数。 |
| 上市时间 | 使用状态快照中的上市日；状态来源不足时不得以当前主表无提示替代。 |
| 市场范围 | 首期仅允许 A 股市场代码 `sh/sz/bj/cn` 及其板块映射；导入或条件过滤出现港股、美股、未知市场代码时，按“非首期支持资产”逐条拒绝并给出原因。 |
| 行业 | 当前 `symbols.industry`/资产主数据可提供当前行业标签；保存候选池时复制行业值和来源。历史行业表未建设前，不支持按历史行业变化重放。 |

### 3.4 “近三年亏损”的明确口径

默认定义为：按公告日可见的年度财报，向每个训练日期回看最近三个已公告年度；净利润小于 0 计为亏损。缺失年度不等于盈利，默认按“数据不足”排除并提示。用户可切换为：任一年亏损即排除、连续三年亏损才排除、近三年累计净利润小于 0。

### 3.5 股票池预览与快照

点击“预览股票池”显示每条过滤规则的排除数量、剩余数量、示例代码、最近完整交易日和数据来源。导入方式显示逐行匹配结果和错误下载；条件方式显示规则命中统计。通过后保存 `universe_snapshot_id`，训练过程只使用快照，不实时重新筛选。

必须阻断：结果为 0、有效股票数少于 50、关键字段覆盖率不足、股票池跨市场但行情不完整。`50` 为系统硬下限，不允许通过前端参数降低；不同任务只能在 50 以上调整自己的业务上限。

### 3.6 候选池结果可视化

生成后进入独立的候选列表页，不与主数据列表混用。列表至少包含：序号、股票代码、股票名称（主数据）、交易所/板块、上市日期、ST/风险标记、最新交易日、最新收盘价、总市值、流通市值、PE_TTM、PB、ROE_TTM、近三年亏损年数、近 N 日平均成交额、纳入来源（导入/过滤）、纳入原因摘要、数据完整性状态。

列表支持关键词搜索、板块/风险/完整性筛选、排序、分页、全选、批量删除和导出错误明细。批量删除只删除当前候选池与标的的关联，不删除 `symbols`、行情、财报或估值主数据；删除操作必须二次确认并写审计。

删除后若有效标的数低于 50，候选池状态变为"需重新校验"，不能进入下一步。

### 3.7 生成训练物料与候选池分析看板

候选池选择完毕后，不直接进入下一步，而是先执行"生成训练物料"：系统自动分析候选池特征，生成分析看板，用户确认后再进入时间与目标配置。

#### 3.7.1 交互流程

```
候选池列表页（筛选/导入功能可交互）
    ↓ 点击"生成训练物料"
按钮变loading："正在分析候选池特征..."（2~15秒，视股票池大小）
    ↓ 分析完成
筛选/导入功能锁定（变灰，hover提示"已生成训练物料，如需修改请点击重新选择"）
按钮变为"查看候选池看板"
    ↓ 点击
弹出看板详情弹窗（只读，不能编辑）
    ├─ 顶部：概览卡片
    ├─ 中部：市值分布/行业分布/风格暴露/市场环境/数据质量
    └─ 底部：[重新选择]  [下一步]
    ↓ 点击"重新选择"
确认弹窗："重新选择将清除当前分析结果，确定吗？"
    ↓ 确认
删除分析数据，释放筛选/导入功能，回到初始状态
    ↓ 点击"下一步"
进入 Step 2 时间与目标配置
```

#### 3.7.2 按钮状态变化

| 状态 | 按钮文字 | 样式 | 可点击 |
|------|----------|------|--------|
| 初始 | 生成训练物料 | 主按钮蓝色 | 是 |
| 分析中 | 正在分析候选池特征... | loading动画 | 否 |
| 分析完成 | 查看候选池看板 | 主按钮绿色 | 是 |
| 重新选择确认后 | 生成训练物料 | 主按钮蓝色 | 是 |

#### 3.7.3 锁定状态交互

生成训练物料后，候选池页面进入锁定状态：

- 所有筛选控件、导入按钮、批量删除按钮变灰，不可点击
- 每个已选筛选条件后加"🔒 已锁定"标签
- 股票列表可以查看/滚动/搜索/排序，但不能修改成员
- hover锁定控件时提示："已生成训练物料，如需修改请点击下方'重新选择'"
- 页面顶部显示黄色提示条："候选池已锁定，分析结果已生成。如需修改候选池，请点击'重新选择'。"

点击"重新选择"并确认后，锁定状态解除，所有控件恢复可交互，分析数据被删除。

#### 3.7.4 候选池分析看板弹窗

弹窗标题："候选池分析看板"，右上角显示数据截止日和分析时间。弹窗为只读模式，所有图表和数据只能查看，不能编辑。

**布局结构：**

```
┌─────────────────────────────────────────────────────────┐
│  候选池分析看板                              [×]          │
│  数据截止日：2026-09-05  |  分析时间：2026-09-08 14:30 │
├─────────────────────────────────────────────────────────┤
│                                                           │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐  │
│  │ 股票数量  │ │ 平均市值  │ │ 时间范围  │ │ 数据完整度│  │
│  │  285只   │ │  86亿    │ │ 5年       │ │  98.5%   │  │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘  │
│                                                           │
├──────────────────────────┬──────────────────────────────┤
│  市值分布                │  行业分布（Top 8）           │
│  小盘 62% ████████       │  新能源  28% ████████████   │
│  中盘 28% ████           │  医药    18% ███████        │
│  大盘 10% ██             │  TMT     15% ██████         │
│  (横向柱状图)            │  消费    12% █████          │
│                          │  金融     8% ███             │
│                          │  制造     7% ███             │
│                          │  其他    12% █████           │
├──────────────────────────┼──────────────────────────────┤
│  风格暴露                │  市场环境                    │
│  成长  ●────────────     │  市场阶段：震荡市            │
│  价值  ●────             │  波动率：高（32%年化）       │
│  质量  ●──────           │  趋势强度：中等              │
│  动量  ●───────          │  建议方向：反转/波动率       │
│  波动  ●────────         │                              │
│  (雷达图或条形图)        │  因子类型建议：              │
│                          │  波动率★★★ 反转★★☆         │
│                          │  质量★★☆ 趋势★☆☆ 估值★☆☆  │
├──────────────────────────┴──────────────────────────────┤
│  数据质量（字段覆盖率）                                   │
│  close 99.7% ████████████  volume 99.5% ████████████   │
│  turnover 99.7% ████████████  pe_ttm 92.1% ███████████ │
│  pb 91.8% ███████████  roe_ttm 85.3% █████████         │
│  有效交易日：1215天  |  平均每日有效标的：268只         │
│  覆盖率<80%的字段标红提示                                 │
├─────────────────────────────────────────────────────────┤
│           [重新选择]              [下一步 →]             │
└─────────────────────────────────────────────────────────┘
```

**各区块详细说明：**

1. **顶部概览卡片（4个）**
   - 股票数量：候选池有效标的数，<50只时红色警告
   - 平均市值：流通市值均值，标注"小盘/中盘/大盘"
   - 时间范围：用户选的训练时间范围（在Step 2配置后回填，候选池阶段显示"待配置"）
   - 数据完整度：行情字段平均覆盖率

2. **市值分布**：横向柱状图，分大盘（>500亿）、中盘（100~500亿）、小盘（<100亿）三档，每档显示数量和占比。

3. **行业分布（Top 8）**：横向柱状图按占比降序，超过8个归为"其他"。行业集中度>40%时提示"行业集中，建议生成行业中性因子"。

4. **风格暴露**：雷达图（5维：成长/价值/质量/动量/波动率），基于股票池基本面和技术面特征计算。标注"主导风格"和"互补风格建议"。

5. **市场环境**
   - 市场阶段：牛市/熊市/震荡市（基于区间涨跌幅+最大回撤）
   - 波动率：高/中/低（年化波动率分档）
   - 趋势强度：强/中/弱（基于趋势指标）
   - **因子类型建议**：根据市场环境自动推荐优先生成的因子类型，带星级。震荡市→反转★★★、波动率★★☆；趋势市→动量★★★、趋势★★☆；高波动→波动率收缩★★★、突破★★☆。

6. **数据质量**：各字段覆盖率进度条，覆盖率<80%标红；有效交易日数、平均每日有效标的数。

#### 3.7.5 分析内容与AI生成的联动

候选池分析看板中的以下数据会自动传递给Step 4的AI因子生成，作为Prompt投喂变量：

- 平均市值、市值分布 → 市场环境变量
- 行业分布 → 市场环境变量
- 风格暴露 → 市场环境变量
- 市场阶段、波动率、趋势强度 → 市场环境变量
- 数据质量（字段覆盖率） → 可用资源变量
- 因子类型建议 → 用户偏好变量（作为初始权重建议，用户可在Step 4调整）

用户在看板中看到的分析结果，会直接影响后续AI生成因子的方向，形成"候选池分析→AI因子生成"的数据闭环。

#### 3.7.6 分析耗时与性能

候选池特征分析是轻量统计计算，不需要异步长任务：

- 股票数<500：约2~3秒
- 股票数500~2000：约5~8秒
- 超过2000：约10~15秒

前端loading即可，分析结果写入候选池快照，和候选池成员一起冻结。重新选择时删除分析结果。

### 3.8 候选池数据隔离与存储

候选池必须使用独立业务表，不把筛选结果写回股票主数据：

- `training_candidate_pools`：候选池定义、来源类型、规则/导入批次、状态、创建人和版本。
- `training_candidate_pool_members`：候选池与 `symbols.id` 的关联、纳入时的校验快照和删除标记；不复制股票名称、行业等主数据。
- `training_candidate_pool_snapshots`：提交训练时冻结的成员集合、规则哈希、数据截止日和统计摘要；训练只引用该快照。
- `training_runs`：训练配置引用 `candidate_pool_snapshot_id`，不直接引用可变候选池。

成员表对 `(pool_id, symbol_id)` 建唯一索引，对 `snapshot_id`、`symbol_id` 建查询索引；大批量训练读取快照成员后一次性生成内存/分析表，避免逐行查询主库。不同训练即使使用同一候选池，也必须生成独立快照，保证规则、成员和数据截止日互不污染。

## 4. Step 2：时间与目标

配置训练开始日、结束日、调仓频率（日/周/月）、预测持有期、训练/验证/测试切分方式，并在同一页完成预测目标配置。开始日和结束日表示样本观察区间，结束日不得晚于数据截止日。

这里的“调仓频率”表示信号重算和常规组合调整的频率，不等于锁仓周期。选择月频时，系统默认每月生成一次常规换仓信号；但停牌、退市、风控触发、流动性失效等强制事件仍可在事件发生时退出。若启用止损、最大回撤、波动率异常或流动性阈值等风险退出规则，必须单独配置“风险监控频率”（首期至少支持每日）和退出规则，不能默认为月频。风险退出规则属于组合/回测执行层；只有用户明确将其纳入预测目标时，才进入标签定义和数据校验。

默认采用按时间先后切分的三段式方案：训练集 60%、验证集 20%、测试集 20%。用户可以在页面调整三段比例，或切换为自定义日期边界；比例调整后由系统重新计算各段日期边界。无论采用哪种方式，三段比例之和必须为 100%，训练、验证、测试顺序必须严格递增，并且仍需满足各阶段最低有效调仓点数、数据完整性和全部门禁。比例作用于“有效调仓点”而不是自然日或原始行情行数；系统根据实际有效调仓点计算三段的日期边界，并在页面同时展示每段的起止日期、有效调仓点数和占比。禁止随机打乱、随机抽样或跨区间混合样本。

有效调仓点定义为：在候选池快照内，满足行情/字段完整性、交易状态、上市时间、过滤规则，且能够生成完整预测标签的调仓日。停牌、无有效截面、数据未就绪和标签不完整的日期不计入；同一调仓频率下只保留一个调仓点，避免把日频行情行数误当成周频/月频样本量。

最小样本量按总有效调仓点校验：周频至少 104 个（约 24 个月），月频至少 36 个（约 36 个月）；日频首期至少 252 个有效调仓点。该数值是可运行硬门槛，不代表统计上充分。无论使用默认比例还是自定义比例，训练、验证、测试三段还必须分别达到配置的最低点数；若验证或测试点数过少，系统阻断并建议扩大时间范围、调整比例或降低调仓频率。生产建议优先使用周频不少于 156 点、月频不少于 60 点的数据，并在界面显示“最低可运行/建议样本量”差异。

切分边界执行 purge：若某调仓点的预测持有期会跨越训练/验证或验证/测试边界，则该调仓点从边界两侧样本中剔除，不得把未来收益标签带入前一段；必要时按持有期增加 embargo 间隔。公式最大回看窗口只影响样本起始前的历史数据准备，不改变切分顺序；系统需展示回看缓冲天数、purge/embargo 天数及因此减少的有效点数。

系统自动计算并展示：原始交易日数、有效调仓点数、各分段边界、被扣除的回看缓冲、上市不足、数据未就绪、无截面、标签跨界和 purge 数量。OOS/测试区间必须晚于训练区间；修改调仓频率、持有期、候选池或公式最大窗口后，Step 2 及下游校验全部标记为“需重新校验”。

预测目标可选个股未来 N 日简单收益、对数收益、基准超额收益或截面排名目标；多空分组收益仅用于研究展示，不直接生成订单。目标计算必须使用下一期数据，不能与当前因子值共用未来价格。调仓频率改变时，持有期可用范围和有效调仓点数自动重新计算并给出提示。若风险退出规则改变持有路径，必须重新生成标签并重新执行 Step 3 校验，不得继续使用原目标结果。

## 5. Step 3：字段与校验

页面显示当前公式目录中**已注册且通过 readiness** 的字段（数量随数据接入进度动态变化，不写死），分组为行情、估值、财报、资金流、事件/快照。每个字段显示：中文名、来源表、数据模式（continuous/PIT/event/snapshot/blocked）、最新日期、覆盖率、可用区间和限制原因。

用户可以：

- 勾选字段作为候选输入。
- 标准模式：选择字段范围、公式模板和参数网格，由系统自动生成候选公式；用户不需要逐条编写公式。高级模式可选“提交自定义 DSL 公式”，仅用于评估用户已有假设，或作为受控搜索模板，不能绕过 AST、数据依赖和复杂度校验。
- 设置方向、窗口、缺失策略、去极值、标准化和行业/市值中性化开关。
- 查看公式依赖字段和最大回看窗口。

字段“存在”不等于“可训练”：例如财报 ROE 必须按公告日 PIT 对齐，事件字段不能填 0，blocked 字段不能勾选。

用户勾选的字段必须全部通过数据完整性校验，系统不得自动排除、降级、填零或用其他字段替代。校验任务保存字段选择、公式配置、候选池快照、时间与目标配置及配置哈希；同一配置已有运行中的校验任务时复用该任务，不重复创建。校验是独立的异步校验任务，不占用“全局最多一个训练任务”的训练锁，但同一草稿不得同时运行多个相同校验。

校验耗时可能达到数小时。用户可以离开页面，任务按字段/时间段分片并支持断点续跑；完成或失败后通过系统消息通知。校验结果为“通过/阻断/警告”，只有全部已选字段通过且硬门禁无阻断时，Step 3 才能完成。覆盖率不足、PIT 违规、字段无数据等问题必须由用户“重新选择字段”或“去修复数据”，不允许静默降级。

### 5.1 字段校验结果与数据修复回流

点击“开始校验”生成一次带配置哈希的异步校验任务。校验至少包括：

1. 股票池快照是否存在且数量达标。
2. 每个公式字段的物理表、字段列、首末日期和来源批次。
3. 训练/验证/测试区间的截面覆盖率、缺失率和最长缺失连续期。
4. 财报/估值的 PIT 约束，检查 `announcement_date <= trade_date`。
5. 公式最大回看窗口是否超出区间前置数据。
6. 目标收益是否存在，是否与因子日期发生未来数据重叠。
7. ST、停牌、退市和上市日期过滤是否有对应快照字段。
8. 训练资源的初步估算（仅用于提示；最终资源确认在提交时执行）。

结果分为阻断、警告、通过：

- 阻断：不能进入下一步，例如字段无数据、PIT 失败、有效截面不足。
- 警告：可以继续但必须确认，例如覆盖率偏低、事件字段占比高。
- 通过：全部硬门禁满足。

校验报告必须保存，不允许只在前端临时展示。

每个阻断字段必须展示：字段代码和中文名、问题类型（覆盖率不足/PIT 违规/无数据/回看窗口超限）、当前值与要求值、原因分析、按时间和股票分布的缺失摘要。系统不提供逐字段自动修复按钮，统一引导用户重新选择字段或去修复数据；Step 1 股票数不足、Step 2 样本量不足等其他阻断也必须说明当前值、门槛和调整方向。

校验不通过时仅提供两个主操作：

- **重新选择字段**：返回字段选择，保留其余配置，移除或替换问题字段后重新校验。
- **去修复数据**：将当前 Step 3 全部配置标记为“待修复后重校验”，跳转现有数据修复/同步页面。

数据修复完成不会由系统猜测外部任务已结束；用户从数据修复页点击“返回因子训练”后，系统恢复暂存配置并自动提交一次新的异步校验。多个待修复草稿可以并存，草稿长期保留，且每次回流都重新生成配置哈希。

## 6. Step 4：进化参数

### 6.1 挖掘模式

- **遗传算法进化（默认）**：初始种群 → 逐代进化（选择→变异→交叉→评估）→ 收敛判断 → 最终全量验证。
- 单因子评估：只计算 IC/ICIR、覆盖率和分层收益（用于单个公式的快速验证，不进入进化流程）。
- 本向导不执行多因子权重学习、Ridge、评分生成或模型激活；这些属于"设置 → 因子模型"的后续独立流程。

### 6.2 进化参数

页面提供"简单/高级"模式。简单模式只显示"进化强度"滑块（弱/中/强，对应最大代数5/10/20），其余用默认值；高级模式展开所有参数。所有参数均显示推荐值、安全上限和术语说明，并记录到实验快照。

| 配置项 | 默认值 | 范围 | 说明 |
|--------|--------|------|------|
| 种群大小 | 100 | 50~300 | 每代个体数 |
| 最大代数 | 20 | 5~50 | 进化上限 |
| 选择比例 | 30% | 20%~50% | 每代选出的父代比例；按6.5"赛道竞争+多目标+锦标赛"选出，非简单全局Top |
| 精英保留 | 5个 | 0~10 | 直接进入下一代不变异的Top个体（各赛道/全局精英，不参与锦标赛） |
| 锦标赛K | 3 | 2~7 | 每次随机抽K个取最优出线（B2），K越大选择压力越强 |
| 变异率 | 60%（基线） | 40%~80% | 新个体中来自变异的比例；自适应开启时为基线值，由C1在区间内按6.6动态调节 |
| 交叉率 | 30%（基线） | 10%~50% | 新个体中来自交叉的比例；同上由C1动态调节 |
| 随机注入率 | 10%（基线） | 5%~20% | 全新随机个体比例(D2)，防早熟；率由C1动态调节，不再固定 |
| 自适应调度 | 开启 | 开/关 | C1根据D3多样性+D1停滞动态调三率与变异类型/跨赛道比例（见6.6）；关闭则用上述固定基线值并解锁手动配比例 |
| 自适应敏感度 | 平衡 | 保守/平衡/激进 | 映射健康分阈值与停滞代数，不暴露原始数值 |
| 收敛阈值 | 0.01 | 0.005~0.05 | 连续3代最优ICIR提升小于此值则停止（D1先自救后停，见6.6.7） |
| 每代评估样本长度 | 1年 | 6月~2年 | 每代快速评估用的样本长度 |
| 最终验证样本 | 全量 | - | 最终Top因子用完整训练区间验证 |
| 复杂度上限 | 6算子 | 3~8 | 个体最大复杂度（算子数） |
| 随机种子 | 42 | - | 可复现 |

并行度由后端根据当前 CPU 负载自动控制，不暴露给用户；默认至少保留 2 个核心给系统和 API。单任务锁存在于服务端，前端禁用按钮不能代替锁。

### 6.3 初始种群来源配置（三来源混合）

初始种群由**经典因子模板、AI生成骨架、受约束随机生成**三类来源构成，采用**两层结构**：经典因子是独立的"保底底座"（优先保证6类维度覆盖），AI生成和受约束随机生成负责"探索剩余名额"。三者互补：经典底座保底（已知有效、维度齐全）、AI生成探索（新颖组合）、随机生成保障多样性（基因原材料）。

#### 6.3.1 两层结构：经典独立底座 + AI/随机填充剩余

经典因子的核心价值是**各维度保底覆盖**，不与AI/随机放在同一个比例池竞争（否则用户把经典比例调小后覆盖不全，失去保底意义）。因此分为两层：

```
第一层：经典底座（独立，优先保证6类维度覆盖，用户自选类别和数量上限）
第二层：剩余名额 = 种群大小 − 经典实际入库数，由 AI + 随机 按比例分配
```

**入库顺序：** 先生成并锁定经典底座 → 再用AI填、随机补剩余名额 → AI不足随机补 → 都不足时允许种群略小于目标，不硬凑重复因子。

#### 6.3.2 初始种群配置界面与规则

Step 4 进化参数 → 初始种群配置区：

```
种群大小：[100]个

▼ 第一层：经典因子底座（独立保底，优先维度覆盖）
  ☑ 启用经典因子
  经典数量上限：[40]个（硬上限不超过种群大小的60%）
  类别覆盖（勾选启用，默认全选，按覆盖策略分配名额）：
    ☑ 趋势类    5个模板
    ☑ 反转类    4个模板
    ☑ 波动率类  4个模板
    ☑ 估值类    4个模板（需估值字段，未选将置灰）
    ☑ 质量类    4个模板（需财报字段，未选将置灰）
    ☑ 量价类    4个模板
  覆盖策略：● 均衡覆盖（每启用类别名额均等，保证各类都有代表）
            ○ 优先级覆盖（按模板优先级排序取Top）
  [查看完整模板清单 →]

▼ 第二层：探索部分（剩余名额由 AI + 随机分配）
  剩余名额：100 − 经典实际入库数
    AI生成：  [57%]
    随机生成：[43%]
  快速预设：○ 保守探索(AI40:随机60) ● 平衡(AI57:随机43) ○ 激进探索(AI70:随机30)
  [AI生成配置]
```

**经典底座规则：**
1. **数量上限而非固定比例**：用户设置经典数量上限（默认40，硬上限=种群大小×60%，防止经典过多挤占探索空间）；实际入库数取决于启用类别/模板展开+校验+去重后的合格数，有多少用多少，不硬凑低质量经典。
2. **均衡覆盖（默认）**：保证启用的每一类都有代表。算法：每类基础名额=floor(上限/启用类别数)，余数按类别优先级补；每类内部按模板优先级+复杂度升序取Top；某类候选不足（如估值类只展开4个）或字段未选（质量类需财报字段）时，缺口流转给其他候选充足的类。
3. **优先级覆盖（可选）**：不按类别均分，所有启用模板统一按优先级+复杂度排序取Top到上限。
4. **字段联动**：估值/质量类依赖估值/财报字段，Step3未选对应字段时该类别自动置灰并提示"需选择XX字段"，不可勾选。
5. **可完全关闭**：取消"启用经典因子"后，全部名额由AI+随机分配（纯探索场景）。

**AI/随机探索部分规则：**
1. 剩余名额 = 种群大小 − 经典实际入库数；AI、随机按比例分配剩余名额，两者比例之和=100%。
2. 默认 AI:随机 = 57:43（由原三来源40:30换算）；三个预设：保守探索40:60（省API成本、随机多铺多样性）、平衡57:43（默认）、激进探索70:30（最大化AI新颖因子）。
3. AI或随机某一方不足时由另一方补位；AI API失败时自动降级，其名额全部由随机填充。

**数量示例（种群100）：**

| 场景 | 经典实际 | 剩余 | AI(57%) | 随机(43%) |
|------|---------|------|---------|-----------|
| 默认全选6类 | 38 | 62 | 35 | 27 |
| 只选趋势+反转+量价 | 18 | 82 | 47 | 35 |
| 关闭经典 | 0 | 100 | 57 | 43 |
| 经典上限拉满60 | 55 | 45 | 26 | 19 |

所有配置（启用类别、上限、覆盖策略、AI/随机比例、预设）写入实验快照，保证可复现。

#### 6.3.3 经典因子模板清单（25个系统预设模板）

系统预设25个经典因子模板，按6类分组，不可删除，可查看和复制为个人模板：

**趋势类（5个）：**

| 模板名称 | 公式结构（带参数占位符） | 参数范围 | 经济逻辑 |
|----------|------------------------|----------|----------|
| 均线趋势 | mean(close,{n1})/mean(close,{n2})-1 | n1∈[5,10,20], n2∈[20,60,120] | 短期均线上穿长期均线，捕捉趋势启动 |
| 价格动量 | delta(close,{n1}) | n1∈[10,20,60] | 过去N日涨跌幅，捕捉价格动量 |
| 通道突破 | (close-llv(low,{n1}))/(hhv(high,{n1})-llv(low,{n1})) | n1∈[20,60] | 价格在N日通道中的位置，突破上沿看多 |
| MACD | ema(close,{n1})-ema(close,{n2}) | n1∈[12], n2∈[26] | 快慢均线差值，趋势强度指标 |
| 均线斜率 | (mean(close,{n1})-mean(close,{n2}))/{n2} | n1∈[5,10], n2∈[20,60] | 短期均线相对长期均线的斜率，趋势加速度 |

**反转类（4个）：**

| 模板名称 | 公式结构 | 参数范围 | 经济逻辑 |
|----------|----------|----------|----------|
| 短期反转 | -delta(close,{n1}) | n1∈[5,10] | 过去N日跌幅越大，未来反弹概率越高 |
| 超买超卖 | (close-llv(low,{n1}))/(hhv(high,{n1})-llv(low,{n1}))-0.5 | n1∈[14,20] | 类似RSJ，超卖（<0）看多，超买（>0）看空 |
| 均值回归 | -(close/mean(close,{n1})-1) | n1∈[20,60] | 价格偏离均线越远，回归概率越高 |
| 量价背离 | -delta(close,{n1}) * rank(volume) | n1∈[5,10] | 价格下跌但成交量放大，可能是底部信号 |

**波动率类（4个）：**

| 模板名称 | 公式结构 | 参数范围 | 经济逻辑 |
|----------|----------|----------|----------|
| 波动率收缩 | std(close,{n1})/mean(std(close,{n2}),{n3}) | n1∈[5,10], n2∈[20,60], n3∈[20,60] | 当前波动率相对于历史波动率的位置，收缩后可能突破 |
| 波动率突破 | std(close,{n1})/mean(std(close,{n2}),{n3}) | n1∈[5,10], n2∈[20,60], n3∈[20,60] | 波动率突然放大，可能是趋势启动信号 |
| ATR比率 | atr({n1})/close | n1∈[14,20] | 真实波幅占价格比例，波动率水平 |
| 布林带位置 | (close-mean(close,{n1}))/(2*std(close,{n1})) | n1∈[20] | 价格在布林带中的位置，±1为上下轨 |

**估值类（4个）：**

| 模板名称 | 公式结构 | 参数范围 | 经济逻辑 |
|----------|----------|----------|----------|
| PE反转 | -rank(pe_ttm) | - | 低PE股票未来收益可能更高（价值效应） |
| PB反转 | -rank(pb) | - | 低PB股票未来收益可能更高（价值效应） |
| 估值比价 | rank(pe_ttm)-rank(pb) | - | PE和PB的相对位置，寻找估值错配 |
| 盈利收益率 | rank(1/pe_ttm) | - | 盈利收益率（E/P），价值因子的另一种表达 |

**质量类（4个）：**

| 模板名称 | 公式结构 | 参数范围 | 经济逻辑 |
|----------|----------|----------|----------|
| ROE变化 | delta(roe_ttm,{n1}) | n1∈[1,2,4] | ROE提升的公司未来收益可能更好（盈利动量） |
| 盈利加速度 | delta(roe_ttm,{n1})-delta(roe_ttm,{n2}) | n1∈[1], n2∈[2,4] | ROE变化的变化，盈利加速的公司更优 |
| 毛利率变化 | delta(gross_margin,{n1}) | n1∈[1,2,4] | 毛利率提升说明竞争力增强 |
| 资产周转率 | delta(asset_turnover,{n1}) | n1∈[1,2,4] | 资产周转率提升说明运营效率改善 |

**量价类（4个）：**

| 模板名称 | 公式结构 | 参数范围 | 经济逻辑 |
|----------|----------|----------|----------|
| 换手率异常 | rank(turnover)-rank(mean(turnover,{n1})) | n1∈[20,60] | 当前换手率相对于历史均值的偏离，异常放量可能有行情 |
| 成交量趋势 | mean(volume,{n1})/mean(volume,{n2})-1 | n1∈[5,10], n2∈[20,60] | 短期成交量相对长期的变化，放量看涨 |
| 量价背离 | rank(volume) - rank(delta(close,{n1})) | n1∈[5,10] | 成交量大但价格不涨，可能是出货信号 |
| 资金流强度 | amount/mean(amount,{n1}) | n1∈[20,60] | 当前成交额相对于历史均值的强度 |

> 注：质量类和估值类模板依赖对应字段，用户未选相关字段时自动跳过该模板，不报错。

#### 6.3.4 经典底座生成逻辑（均衡覆盖）

```
输入：模板库（25个模板）+ 用户已选字段 + 启用类别 + 数量上限 + 覆盖策略
    ↓
1. 类别/模板过滤：只保留用户启用类别、且字段依赖在已选范围内的模板
   （未选roe→质量类跳过；未选估值字段→估值类置灰不可选）
    ↓
2. 参数网格展开：每个模板按参数定义展开
   例：均线趋势模板 n1∈[5,10,20], n2∈[20,60,120] → 3×3=9个具体公式
    ↓
3. 批量校验：AST语法+字段依赖+复杂度+类型匹配
    ↓
4. 来源内去重：formula_hash去重 + AST结构相似度聚类（>0.9留1个）
    ↓
5. 按覆盖策略分配名额：
   【均衡覆盖】每类基础名额=floor(上限/启用类别数)，余数按类别优先级补；
               每类内部按模板优先级+复杂度升序取Top；某类不足缺口流转其他类
   【优先级覆盖】所有启用模板统一按优先级+复杂度排序，取Top到上限
    ↓
6. 锁定经典底座（实际入库数≤上限，不足不硬凑），operation=enumerated, generation=0
    ↓
7. 计算剩余名额 = 种群大小 − 经典实际入库数，交给AI+随机填充
```

#### 6.3.5 AI生成骨架逻辑

AI生成骨架负责探索剩余名额，根据当前市场环境和用户配置实时生成新颖公式骨架，参数展开后加入种群。AI生成时参考经典模板作为Few-shot示例（不是凭空生成），且Prompt中注入"已入库经典底座公式+已生成AI骨架"，避免与经典和自身重复。AI生成配置和自由度约束详见6.3.9节。AI骨架目标数量 = 剩余名额 × AI比例，超额生成（×冗余系数2.0）后校验去重取Top，不足部分由随机补位。

#### 6.3.6 受约束随机生成逻辑

随机生成不是瞎随机，是在严格约束下的受约束随机组合：

**约束条件：**
1. 字段自动分类：价格类(close/open/high/low)、成交量类(volume/amount)、估值类(pe_ttm/pb)、质量类(roe_ttm)、换手率(turnover)
2. 算子带类型签名：mean(价格类)→价格类、rank(任意)→无量纲、+(同类型)→同类型
3. 类型匹配：同类型才能加减，横截面算子(rank/zscore)输出无量纲可以跨类型
4. 复杂度上限：算子数≤4，嵌套≤2，字段引用≤3（随机生成的复杂度上限比AI生成低，保证基础有效性）
5. 必须通过AST校验和去重

**生成过程：**
```
1. 随机决定复杂度（1~4个算子）
2. 从根节点开始，随机选算子
3. 根据算子的输入类型，递归生成子节点
4. 叶子节点随机选对应类型的字段（从用户已选字段中）
5. 参数随机生成（窗口期5~120）
6. 生成后校验类型匹配，不通过则重新生成
7. 实时哈希拦截：每生成一个立即和已有（经典+AI+随机）比对，撞了立即重新生成（单公式最多尝试20次）
8. 同结构限变体：相同AST结构（只参数不同）每种最多保留3个参数变体
9. 循环生成直到得到足够数量（目标数量×冗余系数2.0）
10. 批量校验+去重
11. 多样性排序：计算每个候选和已有底座/AI候选的AST结构相似度，相似度低的优先
12. 取Top N（随机目标数量=剩余名额×随机比例，并承担AI不足时的补位）加入种群
```

**随机生成的4个核心作用：**
1. **保证种群多样性**：经典模板和AI生成可能趋同，随机生成带来完全不同的结构
2. **探索人类和AI都没想到的组合**：有些有效因子是偶然发现的
3. **提供变异和交叉的"基因原材料"**：随机生成的独特结构，在进化中和经典/AI因子交叉，可能产生新颖后代
4. **防止局部最优**：种群全部围绕几个经典公式优化可能陷入局部最优，随机个体带来"基因注入"打破局部最优

#### 6.3.7 数量链路与冗余系数

初始种群不是"生成多少入库多少"，而是"超额生成 → 校验去重 → 按两层结构取Top"：

```
用户配置：种群大小P（默认100）
    ↓
第一层：经典底座
  启用模板参数网格展开（25模板约展开60~80个）→ 校验+去重 → 按覆盖策略锁定C个（C≤上限）
    ↓
第二层：剩余名额 R = P − C
  AI目标 = R × AI比例；随机目标 = R × 随机比例
    ↓
各来源按"目标数量 × 冗余系数（默认2.0，高级模式可配）"超额生成：
  经典：展开60~80个
  AI：生成约20个骨架 × 2~3参数展开 ≈ 40~60个
  随机：循环生成目标×2个
    ↓
各自校验（淘汰10~20%）→ 来源内去重（淘汰15~25%）→ 跨来源去重（淘汰约10%）
    ↓
按两层结构入库：先锁定经典C个 → AI取Top → 随机补足
    ↓
AI不足随机补，都不足允许种群略小于P（不硬凑重复因子）
```

**为什么要超额生成（冗余系数2.0）：** 生成过程有三层损耗——校验不通过（类型/字段/复杂度，约10~20%）、来源内重复（约15~25%）、跨来源重复（约10%）。超额生成2倍保证损耗后仍凑得够目标数量。

#### 6.3.8 四层去重机制（规避AI/随机的高重复）

AI和随机生成不确定性强，天然重复率高，通过"事前规避 + 事后去重"四层机制处理。**生成顺序：经典（确定性）→ AI（能看到经典）→ 随机（能看到经典+AI），后生成的能看到先生成的，天然规避重复。**

**第1层：生成时规避（事前，最重要）：**

| 来源 | 手段 |
|------|------|
| AI生成 | ①Prompt注入已入库经典公式+已生成AI骨架，要求不重复 ②分4批每批5个，每批带上前批结果 ③类型配额（6类各3~4个，防全挤一类）④去重反馈重试（撞了反馈"已存在请换结构"重新生成，单骨架最多重试3次，仍撞则放弃由随机补）⑤结构差异化要求（算子组合/字段引用数/嵌套深度要有差异） |
| 随机生成 | ①实时哈希拦截，撞了立即重新生成（单公式最多20次）②同AST结构最多保留3个参数变体 ③按"复杂度×字段组合×算子类型"分层采样，保证结构分散 ④与经典+AI算AST树编辑距离，相似度高的降权 |

**第2层：formula_hash精确去重（完全相同）：** 公式先规范化（交换律归一a+b=b+a、参数排序、去冗余括号、算子别名统一）再算哈希，哈希相同只保留一个。三来源候选合并后统一做一次，解决跨来源重复，保留优先级：经典 > AI > 随机。

**第3层：AST结构相似度聚类（近似重复）：** formula_hash抓不到"换参数重复"（mean(close,20)与mean(close,10)）。计算AST树编辑距离→结构相似度（0~1），相似度>0.9归为一个重复组，每组留1个代表（初始种群按经典优先+复杂度低优先，进化阶段按ICIR高优先）。

**第4层：因子值相关性去重（运行时）：** 结构不同但因子值可能高度相关（close/mean(close,20)与close/mean(close,18)），必须算出因子值才能判断，初始种群阶段未评估做不了，放到每代短样本评估时通过三层指纹（语义类别→统计指纹→采样相关→精确相关）处理，相关系数≥0.95标记淘汰，原因`eliminated_similar`。

**各阶段重复率（预估）：**

| 阶段 | 重复率 |
|------|--------|
| 生成后未去重 | 30~40% |
| 第1层生成规避后 | 15~20% |
| 第2层hash去重后 | 8~12% |
| 第3层结构聚类后 | 3~5% |
| 第4层运行时去重后 | <1% |

最终进入初始种群的个体结构唯一性>95%，保证多样性。

#### 6.3.9 AI生成配置与自由度约束（保留原有内容）

基础配置：
- 生成骨架数量：5~30个（默认15）
- 因子类型偏好：趋势/反转/波动率/估值/质量/量价，可设权重
- 模型选择：豆包Pro/GPT-4o/Claude/自定义（可配置API Key）
- 温度：0~1（默认0.7）

**AI自由度约束（可调约束盒子）：**

AI生成不是完全自由的，而是在用户配置的约束范围内生成。约束同时作用于两层：Prompt约束层（写进系统提示词告诉大模型）和系统校验层（输出后强制执行，不合法的丢弃并用随机个体补）。

提供3个预设快速选择，也可自定义微调：

| 预设 | 最大公式长度 | 最大嵌套深度 | 最大字段引用数 | 创新程度 | 适用场景 |
|------|-------------|-------------|---------------|----------|----------|
| 保守探索 | 3 | 1 | 1 | 保守 | 数据量少、快速验证、命中率优先 |
| 平衡模式 | 5 | 2 | 2 | 中等 | 默认，兼顾命中率和创新性 |
| 激进探索 | 8 | 3 | 4 | 激进 | 数据充足、发现新颖因子、接受较高无效比例 |

自定义约束参数：

| 配置项 | 默认值 | 范围 | 说明 |
|--------|--------|------|------|
| 最大公式长度 | 5 | 3~8 | 公式中算子的最大数量 |
| 最大嵌套深度 | 2 | 1~3 | 算子嵌套的最大层数 |
| 最大字段引用数 | 2 | 1~4 | 一个公式中最多引用几个不同的基础字段 |
| 窗口期范围 | 5~120 | 1~250 | 参数搜索的窗口期最小/最大值 |
| 允许算子子集 | 全部 | 多选 | 勾选哪些算子允许AI使用 |
| 允许字段子集 | 用户已选字段 | 多选 | 勾选哪些字段允许AI使用 |
| 创新程度 | 中等 | 保守/中等/激进 | 保守=接近经典因子，激进=鼓励新颖组合 |
| 类型严格度 | 严格 | 宽松/严格 | 严格=同类型字段才能运算，宽松=允许跨类型 |
| 禁止结构 | - | 多选 | 明确禁止的公式结构（如禁止除法、禁止log） |

系统提示词中动态注入所有约束参数，明确告知大模型"违反以上任何约束的公式将被系统丢弃"。大模型输出后，系统逐项校验（公式长度/嵌套深度/字段引用/参数范围/算子白名单/字段白名单/类型匹配/禁止结构），任何一项不通过则丢弃该骨架，记录失败原因，用随机个体补位。

AI生成的骨架必须经过系统校验（AST/字段/算子/复杂度/类型匹配），不合法的丢弃，用随机个体补。API调用失败时自动降级为纯枚举+随机。

**AI生成与经典模板的结合：**
1. AI生成时，经典模板作为Few-shot示例投喂给大模型，AI参考经典模板的结构做创新，不是凭空生成
2. 进化过程中，经典模板因子和AI生成因子在同一种群中互相交叉，可能产生更优组合
3. AI生成的优质因子（通过门禁+人工确认）可以保存为个人模板，反哺模板库

#### 6.3.10 两层结构与三类来源组合的优势

| 优势 | 说明 |
|------|------|
| 经典维度保底 | 经典底座独立+均衡覆盖，趋势/反转/波动率/估值/质量/量价6类都有代表，不会因比例调小而覆盖不全 |
| 保底+探索+多样性 | 经典保底（已知有效）、AI探索（新颖组合）、随机保障多样性（基因原材料） |
| 比例易把控 | 经典管保底（用户自选类别/上限），AI/随机只管探索剩余，不再三者混在一个比例池难以把控 |
| 鲁棒性强 | 某个来源失败（AI API挂了），随机补位，种群不崩 |
| 交叉产生更优后代 | 经典结构+AI视角+随机组合，在进化中交叉，可能产生比任何单一来源都优的因子 |
| 可解释性有基准 | 经典模板提供经济逻辑基准，AI生成的因子也需要有逻辑解释 |
| 搜索空间覆盖全面 | 经典覆盖已知有效区域，AI覆盖新颖组合区域，随机覆盖未探索区域 |
| 进化效率高 | 初始种群就有一批已知有效因子做底座，不需要从纯随机开始进化，收敛更快 |
| 防重复 | 四层去重保证结构唯一性>95%，AI/随机的高重复被有效控制 |
| 用户可控 | 用户可自选经典类别/上限/覆盖策略，可调AI/随机比例，可完全关闭经典 |

**单独使用任何一个来源都有缺陷：** 纯经典没有新颖性、纯AI没有保底且依赖API、纯随机有效率低收敛慢。两层结构让经典稳定保底，AI/随机弹性探索，取长补短。

#### 6.3.11 因子模板维护页面

在设置中新增一级菜单"因子基础配置"，二级菜单"因子模板配置"：

```
设置 → 因子基础配置 → 因子模板配置
├── 系统预设模板（25个，不可删除，可查看/复制）
│   ├── 趋势类（5个）
│   ├── 反转类（4个）
│   ├── 波动率类（4个）
│   ├── 估值类（4个）
│   ├── 质量类（4个）
│   └── 量价类（4个）
│
└── 个人模板（用户保存的，可增删改）
    ├── 从挖掘结果保存的优质因子
    └── 用户手动创建的模板
```

每个模板显示：模板名称、公式结构（带参数占位符）、因子类型、经济逻辑解释、参数范围、创建来源（系统预设/AI生成/手动创建）、优先级、创建时间。

**模板操作：**
- 系统预设模板：可查看详情、可复制为个人模板、不可删除/修改
- 个人模板：可编辑、可删除、可调整优先级、可启用/禁用
- 模板列表支持按类型筛选、按名称搜索、按优先级排序

**模板来源：**
1. 系统预设：25个经典因子，系统内置
2. AI生成保存：挖掘结果中AI生成的优质因子，用户点击"保存为模板"
3. 手动创建：用户手动输入公式结构、参数定义、经济逻辑

模板配置写入实验快照的模板版本号，保证可复现。修改模板不影响历史挖掘任务。

### 6.4 每代评估与预筛机制

每代个体使用短样本（默认1年）快速评估，计算ICIR/覆盖率/换手率。评估机制复用三层指纹预筛加速相似度计算：

1. **语义类别指纹**：不同因子类型直接跳过比对
2. **统计指纹**：多空方向、IC均值、因子值标准差、换手率，任意3项差异大则跳过
3. **采样相关**：随机抽10个调仓日算相关，<0.7则跳过
4. 只对通过前三层的个体对算全量精确斯皮尔曼相关系数

每代评估后按ICIR排序，用于选择种子。与已有因子库比对可选开启，命中的个体标记但不丢弃（可作为变异基础）。

**最终验证：** 进化结束后，对最终种群Top 50（可配置）做全量评估（完整训练区间），计算精确的IC/ICIR/分层收益/覆盖率，并通过门禁筛选有效因子。

### 6.5 选择机制：赛道竞争 + 多目标评价（NSGA-II）+ 锦标赛

本节定义每代"从当前种群选出繁殖父代"的完整机制，由三个层次叠加，职责互不重叠：

- **赛道竞争（A1，学术名 Niche，本系统命名为"赛道竞争"以避免"小众/稀有数据"误解）：** 决定"在哪个因子类型池里选"，保证趋势/反转/波动率/估值/质量/量价6类不被单一强势类型垄断。
- **多目标评价（B1，NSGA-II）：** 决定"用什么标准判优劣"，不只看ICIR，同时兼顾覆盖率、换手率、复杂度，产出非支配层级与拥挤度。
- **锦标赛选择（B2，Tournament）：** 决定"用什么动作把父代选出来"，随机抽K个取最优，选择压力可控、抗垄断。

#### 6.5.1 总体选择链路

```
当前代种群（已完成短样本评估，每个体有4个目标值）
  │
  ├─ B1：目标向量化 → 非支配排序得rank → 同rank算拥挤度CD
  │       每个体得到比较钥匙：(rank 升序, CD 降序)
  │
  ├─ A1：按因子类型分6条赛道，按动态配额算法算出每条赛道要选几个父代
  │
  └─ 每条赛道内，用 B2 锦标赛：随机抽K=3 → 用比较钥匙(rank→CD)取最优 → 出线
        重复直到该赛道配额满；6条赛道合计得到全部父代
  │
  └─ 精英保留：全局/各赛道Top精英直接进下一代（不参与锦标赛、不变异）
  │
  └─ 其余父代 → 变异（以同赛道为主）/交叉（含少量跨赛道）/随机注入 → 下一代
```

#### 6.5.2 赛道竞争（A1）

**（1）6条赛道（因子类型固定6类，首期不再细分，避免每类样本过少）：** trend趋势、reversal反转、volatility波动率、valuation估值、quality质量、volume_price量价。

**（2）因子归类规则（在公式生成/AST解析时一次性确定，存category）：**
- 经典模板来源：直接继承模板的category。
- AI/随机来源：按AST特征推断主类别，命中多类时按"特征专属度"优先级归类：**quality > valuation > volatility > volume_price > reversal > trend**（trend最宽泛，作为兜底类）。判定信号：
  - 含财报字段（roe/毛利率/周转率等）→ quality
  - 含估值字段（pe/pb/ps等）→ valuation
  - 含std/atr/boll/波动率类算子 → volatility
  - 同时含成交量/换手字段且与价格结合 → volume_price
  - 含短周期-delta/偏离均线等反转结构 → reversal
  - 其余价格趋势结构 → trend
- 变异后若结构/字段变化导致类别改变，重新归类并进入对应赛道；交叉后代按同一规则归类。

**（3）动态配额算法（保底+动态，规避"保护落后"缺陷）：**

设本代需选父代总数 S（=种群×选择比例），当前代"活跃赛道"为有存活个体且对应字段已选的赛道集合 C（空赛道直接关闭）。
- **保底部分（占60%）：** 每个活跃赛道先分得 `base = floor(S×0.6 / |C|)`，保证每类都有繁殖机会。
- **动态部分（占40%）：** 剩余 `S×0.4` 名额按各赛道内最优个体的质量（rank优先、ICIR次之）加权分配，强赛道可多分，使进化不被平均主义拖慢。
- **配额上限：** 任一赛道配额不超过该赛道当前存活个体数，不足部分回流动态池再分配。
- **空赛道处理：** 用户未选对应字段（如未选财报则quality为空）则不开赛道，其保底名额释放给动态池，不硬凑。
- **弱赛道流转（防养懒汉）：** 某活跃赛道连续 W 代（默认W=3）其最优个体rank都处于最末层且ICIR低于预筛门槛，判定为弱赛道，其保底名额减半并转入动态池；之后该赛道恢复达标则恢复全额保底。

**（4）区内选择与变异/交叉方向：**
- 选择只在赛道内进行（锦标赛见6.5.4），不跨赛道比绝对ICIR。
- 变异以**同赛道内**为主（参数/算子/字段变异通常不改变类别，保持进化方向）。
- 交叉默认 **80%同赛道交叉 + 20%跨赛道交叉**（跨类型产生新颖组合，须通过类型匹配校验）；跨赛道比例 `cross_category_ratio` 默认0.2，高级可调。

**（5）类型均衡度（多样性度量）：** 用各类占比的均匀度衡量：
- p[c]=赛道c个体数/种群数；HHI=Σ p[c]²；活跃赛道数 n=|C|
- 类型均衡度 `category_evenness = (1−HHI)/(1−1/n)`，取值0~1：6类完全均匀=1，全是一类=0。
- 该值写入generations表（复用为多样性曲线），并作为D3多样性反馈的输入（均衡度连续偏低时提高随机注入率与跨赛道交叉率，D3后续细化，本节先产出指标）。

**（6）结果端类型验收（对应H1）：** 最终有效因子按6类分组统计数量并在结果页展示，保证结果不是清一色一类；某类为0时如实展示并提示"该类型本次未产出有效因子"。

#### 6.5.3 多目标评价 NSGA-II（B1）

**（1）目标池（系统内置固定，用户只能开关/选偏好，不能自建，方向锁死）：**

| 目标 | 方向 | 默认 | 口径 |
|------|------|------|------|
| ICIR | 最大化 | 开 | 短样本/全量ICIR |
| coverage覆盖率 | 最大化 | 开 | 截面有效覆盖率 |
| turnover换手率 | 最小化 | 开 | Top20%组合每期换仓比例（需新补计算） |
| complexity复杂度 | 最小化 | 开 | AST算子数，生成时即得 |
| oos_stability | 最大化 | 关（可选） | IS/OOS方向一致性与ICIR波动 |
| monotonicity | 最大化 | 关（可选） | 五分组收益单调性 |

- 启用目标数限定 **3~5个**，超出前端拦截。
- **与已有因子相关性（<0.95）不进帕累托，作为硬门禁单独卡**（避免目标过多导致"谁也支配不了谁"、分层失效）。

**（2）目标向量化与支配：** 最小化目标取负，统一成"越大越好"，个体i的目标向量 f(i)。A支配B当且仅当：A在所有目标上都不差于B、且至少一个目标严格更好。该比较只看逐维大小，不需要权重、不需要加权总分、不要求量纲一致。

**（3）非支配排序得rank：** rank=1为全种群无人能支配者；移除rank=1后再找无人支配者为rank=2，逐层剥到分完。

**（4）拥挤度CD（仅同一rank内计算）：**
$$CD_i = \sum_{j=1}^{m}\frac{f_j(i_{+1})-f_j(i_{-1})}{f_j^{max}-f_j^{min}}$$
同一前沿内按每个目标排序，个体i在目标j上取前后邻居间距、除以该前沿在j上的极差（归一化消除量纲差异），m个目标累加；每个目标上的两端极值个体CD=∞（保住前沿极值）。

**（5）比较算子（字典序，质量优先）：** A优于B ⇔ rank_A<rank_B，或 rank_A=rank_B 且 CD_A>CD_B。档次不同时拥挤度不起作用，永远不为多样性选更差的因子。复杂度O(m·N²)，N=100、m=4约4万次比较/代，毫秒级。

**（6）偏好档位（不进入进化计算，只作用于结果端）：** 提供 balanced均衡（默认）/stable稳健（偏向高覆盖、低换手）/aggressive进攻（偏向ICIR）/simple简洁（偏向低复杂度）。进化选择过程对所有目标一视同仁、不掺主观权重；偏好只在Step5最终排序与帕累托前沿高亮时体现，保证进化客观、取舍交给用户。

#### 6.5.4 锦标赛选择（B2）

- 在每条赛道内循环执行：随机抽 K 个（默认K=3）个体，用6.5.3的比较钥匙(rank升→CD降)选最优出线，重复至该赛道配额满；允许同一被重复抽中。
- K控制选择压力：K小偏探索（近似随机）、K大偏收敛（近似全局取Top），K=3为经验平衡值，高级可调 `tournament_k`。
- 随机性只作用在"抽谁进组"，组内比较严格按B1钥匙；固定随机种子可完全复现。
- **与精英保留并存：** 各赛道/全局Top精英（见6.2精英数）不参与锦标赛、直接复制进下一代，其余父代名额由锦标赛产生。

#### 6.5.5 Step 4 配置界面

复用简单/高级模式：
- **简单模式：** 不出现帕累托/非支配/拥挤度/赛道等术语。仅显示"选优偏好"单选（均衡/稳健/进攻/简洁，默认均衡，可不选），赛道竞争与多目标默认开启、默认参数自动生效。
- **高级模式：** 展开三块折叠区——①赛道竞争：开关、保底比例(默认0.6)、弱赛道判定代数W(默认3)、跨赛道交叉率(默认0.2)；②多目标：目标勾选（限3~5个，方向只读）、显示各目标口径tooltip；③选择：锦标赛K值(默认3)。所有参数带推荐值与术语tooltip，并写入实验快照。

#### 6.5.6 Step 5 结果端展示

- **类型分布：** 最终有效因子按6类分组的数量柱状图（类型验收），点击某类过滤列表。
- **帕累托前沿散点图：** X=换手率、Y=ICIR，点颜色=覆盖率、点大小=复杂度，rank=1前沿高亮描边；坐标轴可切换为复杂度vs ICIR等；顶部"均衡/稳健/进攻/简洁"切换可实时重排与高亮；悬停展示该因子4个目标值与公式。
- **因子列表默认排序：** 先按rank再按CD；同时显示每个因子的category、4个目标值、rank、CD，供人工确认。

#### 6.5.7 明确不纳入本期的机制

以下机制经评估后明确排除在本期开发范围之外，代码评审、需求评审、排期与验收均不包含：

| 机制 | 不做理由 |
|------|---------|
| D4 Tabu 禁忌表 | 与C2变异后的formula_hash去重+三层指纹相似度预筛功能高度重叠，重复建设 |
| D5 年龄结构淘汰 | 与B1多目标选择标准冲突（都在选择阶段决定父代，D5给老个体打折等于篡改B1的rank+CD比较结果），且与精英保留直接矛盾 |

未来如重新评估需单独立项，不得在本期代码中混入相关逻辑。

### 6.6 繁殖与自适应机制（C1/C2/C3 + D1/D2/D3）

本节定义选择之后"如何生成下一代、何时停止"的机制。C1/C2/C3 合并为一个**繁殖模块**，D1/D2/D3 围绕它提供信号、注入与停止。与6.5选择机制(A1/B1/B2)严格分工、信号单向流动，保证功能贯通且不业务重叠。

#### 6.6.1 九机制职责分工（各司其职、禁止越权）

| 机制 | 唯一职责 | 不做什么（防重叠） |
|------|---------|-------------------|
| A1 赛道竞争 | 分类型池、定每池选择配额 | 不判优劣、不执行变异 |
| B1 多目标NSGA-II | 算rank+拥挤度，提供比较标准 | 不负责抽选动作、不加权总分 |
| B2 锦标赛 | 赛道内抽K个按标准取优选父代 | 不自定义优劣标准 |
| 精英保留 | 最优个体直接复制进下一代 | 不参与锦标赛、不变异 |
| **D3 多样性监控** | 只产"多样性健康分"信号 + 前端曲线/告警 | **不直接改任何率**（不自己调参，避免与C1两套调度） |
| **C1 自适应调度器** | 读D3/D1信号，只算"本代繁殖策略参数" | 不执行变异交叉（纯参数计算） |
| **C2 四种变异** | 按C1给的比例执行4种变异 | 不自定比例、不定变异率 |
| **C3 子树交叉** | 按C1/A1给的比例执行子树交叉 | 不自定跨赛道比例 |
| **D2 随机注入** | 按C1给的注入率生成受约束随机公式 | 不自定注入率（不再固定10%） |
| **D1 收敛早停** | 只判断"是否停止进化" | C1自救无效才触发，不在高原期误停 |

**单向信号流（贯通主线）：**
```
评估(每代指标)
  → D3产多样性健康分 + D1产停滞计数stall（只观测、产信号）
  → 选择阶段：A1定配额 → B1定标准 → B2选父代（独立运作，不依赖C1）
  → C1读【D3健康分 + D1 stall + 进化代数】算出本代繁殖策略（变异率/交叉率/注入率/4种变异比例/跨赛道比例）
  → 执行阶段：C2变异 + C3交叉 + D2注入（严格按C1策略执行）+ 校验补位
  → D1判断：C1已自救但连续3代仍无进步 → 早停
```
**原则：观测(D3/D1)与决策(C1)分离、决策(C1)与执行(C2/C3/D2)分离。任何机制都不跨层越权，因此不会出现两套逻辑同时调参。**

#### 6.6.2 每代执行主流程（贯通顺序）

```
代 g：
1) 短样本评估：算每个体 ICIR/覆盖率/换手率/复杂度（公共子表达式缓存复用，见G2）
2) 观测：D3 算三层多样性并合成健康分；记录本代最优ICIR，更新 convergence_delta 与 stall_count（D1）
3) 选择：B1非支配排序(rank)+拥挤度(CD)；A1分赛道定配额；B2赛道内锦标赛选父代；精英直接保留
4) C1调度：依据健康分/stall/代数，计算本代繁殖策略 strategy(g)，并落库实际值
5) 繁殖：按strategy，C2变异、C3交叉、D2注入，产出新个体；统一过复杂度+去重+字段依赖校验，非法丢弃、用D2随机个体补齐
6) 停止判断(D1)：若已触发C1自救且 stall_count≥3 仍无进步 → converged 早停，进入最终全量验证；否则进入下一代
```

#### 6.6.3 C1 自适应调度器

**输入信号（全部来自现成指标，不新增数据）：**
- 多样性健康分 health（D3产出，0~1）
- 停滞计数 stall_count、进步幅度 convergence_delta（D1/已有字段）
- 进化阶段：early（前30%代数）/mid/late（后30%代数）

**输出为本代繁殖策略对象（落库 generations，可复现）：**
```
strategy(g) = {
  mutation_rate, crossover_rate, random_rate,      # 三率
  mutation_type_distribution,                      # C2四种变异占比{param,op,field,struct}
  cross_category_ratio                             # C3跨赛道交叉比例（与A1联动）
}
```

**分阶段/状态调节规则（规则式，不用模型）：**

| 状态 | 判定 | 变异率 | 注入率 | C2变异类型倾向 | C3跨赛道率 |
|------|------|--------|--------|---------------|-----------|
| 早期 early | 前30%代 | 0.70 | 0.12 | 结构/字段为主（合计约60%） | 偏高0.3 |
| 中期 mid | 正常进步 | 0.60 | 0.10 | 四种均衡 | 0.2 |
| 后期 late | 后30%代 | 0.45 | 0.05 | 参数微调为主（约70%） | 偏低0.1 |
| 停滞自救 | health<0.5持续2代 或 stall≥2 | 0.75 | 0.15 | 结构/字段变异拉高 | 拉高0.4 |
| 快速进步 | delta显著为正 | 0.45 | 0.05 | 参数微调为主 | 0.15 |

**安全护栏（写死，不可被规则突破）：**
- 三率始终夹在安全区间：变异率[0.40,0.80]、交叉率[0.10,0.50]、注入率[0.05,0.20]；且变异率+交叉率+注入率=1（由系统归一）
- 防抖滞回：连续2代满足才调节，单次调整幅度≤0.10，避免这代升下代降震荡
- 精英永不参与变异/交叉/注入，是提高变异率时的安全网
- 随机种子固定 + 规则确定，整条进化可复现

#### 6.6.4 C2 四种变异、C3 子树交叉（执行器）

**C2 四种变异（类型比例严格由C1的mutation_type_distribution决定，自身不定比例）：**
1. 参数微调：只改数值参数（如窗口5→10、阈值微调），不改结构
2. 算子替换：同类型算子替换（如mean→median、+→−）
3. 字段替换：换用到的字段（如close→open、PE→PB），须在用户已选字段范围内
4. 结构变异：增删/改写子树（最激进），受复杂度上限约束
- 变异后统一过AST合法性、复杂度上限、字段依赖、formula_hash去重校验，不合法丢弃并由D2补位。

**C3 子树交叉（跨赛道比例由C1的cross_category_ratio决定，基准来自A1）：**
- 默认80%同赛道交叉、20%跨赛道交叉（停滞自救时由C1拉高到40%，后期降到10%）
- 两个父代各剪一个子树交换拼接；后代按6.5.2归类规则重新定category
- 跨赛道交叉须通过字段依赖/复杂度/去重校验，不合法同样丢弃补位。

#### 6.6.5 D2 随机注入（执行器）

- 注入率不再固定10%，由C1的random_rate决定（健康时0.05、趋同/停滞时最高0.15~0.20，始终夹在[0.05,0.20]）
- 注入个体为受约束随机公式（遵守已选字段范围、允许算子子集、复杂度上限），复用初始种群的随机生成器
- 作用是引入变异/交叉无法产生的全新基因，防近亲繁殖；与精英保留配合，随机个体质量低也不会破坏当代最优。

#### 6.6.6 D3 多样性监控（只观测+前端展示，不调参）

**三层多样性（合成健康分health，喂给C1）：**

| 层级 | 指标 | 复用来源 |
|------|------|---------|
| 类型多样性 | category_evenness（6.5.2已定义） | A1 |
| 基因型多样性 | 个体两两AST树编辑距离均值 | 复用去重的树编辑距离，不重复算 |
| 表现型多样性 | 因子值截面平均相关性 / ICIR离散度 | 复用三层指纹预筛 |

- 三层默认等权合成health∈[0,1]（高级可调权重）；任一层单独偏低都会拉低health
- **D3只产出health并写库、画图、告警，不修改任何变异率/注入率**（调参动作统一归C1，避免双调度）
- health<0.5持续2代 → 作为信号触发C1"停滞自救"；若health健康但ICIR也在进步，C1不干预
- **前端（Step5运行中）：** 进化曲线旁增加多样性health随代数曲线；持续偏低时黄色提示"种群多样性偏低，系统已自动加大探索"；结果页可展开三层明细，用于诊断是"类型缺失/结构雷同/行为雷同"。

#### 6.6.7 D1 收敛早停（先自救、无效再停）

- 每代记录最优ICIR与convergence_delta，stall_count=连续|delta|<阈值(默认0.01)的代数
- **先自救：** stall_count达到2时不立即停，交给C1触发停滞自救（提高变异/注入/结构变异/跨赛道）
- **后停止：** 自救后stall_count仍累计到3（连续3代无进步），才置converged进入最终全量验证，避免在"高原期"误停
- 阈值与连续代数高级可调；D1只负责停止判断，不负责调参（调参归C1）。

#### 6.6.8 Step4 配置交互（降门槛）与 Step5 运行可视化

**Step4"繁殖与变异"区（并入高级设置，简单模式只见进化强度）：**
- 简单模式：仅"进化强度"（保守/平衡/激进），C1/C2/C3/D1/D2/D3全部自动、零配置
- 高级模式折叠区：①自适应调度开关（关闭则用固定率、此时才解锁手动配C2类型比例与C3跨赛道率，二选一不打架）；②基线变异率/交叉率/注入率；③自适应敏感度（保守/平衡/激进，映射health阈值与stall代数，不暴露原始数值）；④C2四种变异勾选（默认全选）。安全上下限与防抖参数锁死不暴露。
- 自适应开启时，C2类型比例、C3跨赛道率、D2注入率显示为灰色"自动"，不可手改。

**Step5运行可视化：** 最优/平均ICIR进化曲线 + D3多样性health曲线（双轴）、本代实际变异率/注入率曲线（验证C1自适应过程）、低多样性告警、当前繁殖策略摘要（如"检测到停滞，已提高结构变异至45%"）。

#### 6.6.9 联动关系总表（贯通自检）

| 信号/动作 | 产生者 | 消费者 | 流向 |
|-----------|--------|--------|------|
| category_evenness/基因型/表现型→health | D3 | C1 | 单向 |
| stall_count/convergence_delta | D1 | C1（自救）、D1自己（停止） | 单向 |
| 繁殖策略(三率/类型比例/跨赛道率) | C1 | C2/C3/D2 | 单向 |
| 赛道配额 | A1 | B2、C3（跨赛道基准） | 单向 |
| rank+CD比较标准 | B1 | B2 | 单向 |
| 父代 | B2+精英 | C2/C3 | 单向 |
| 停止指令 | D1 | 任务主循环 | 单向 |

无任何双向耦合、无两个机制同时写同一参数；新增机制必须先在本表登记"唯一职责/上下游"，防止业务重叠。

### 6.7 远期参考方向：多种群岛屿模型（⚠ 不在本期需求，仅未来演进参考）

> **边界声明：本节描述的多种群岛屿模型（Island Model）属于远期演进方向，明确不在本期开发范围。** 本期不实现任何岛屿运行时逻辑（不拆分种群、不做岛间迁移、不做分岛合岛、不做分岛前端展示），仅在数据结构上预留可空字段，避免未来改表。请勿将本节内容纳入本期排期、开发与验收。

**前置概念：** 本期在单种群内通过"赛道竞争"（学术名 Niche，按趋势/反转/波动率/估值/质量/量价 6 类因子类型分区、区内竞争、区间保底，防止单一类型垄断种群）维持类型多样性，它是**不拆分总种群的"软隔离"**。多种群岛屿模型是更进一步的**"硬隔离"**：把种群拆成多个物理独立的子种群（岛屿），各岛独立进化、定期迁移优质个体交流。两者未来是"岛内赛道竞争 + 岛间迁移"的嵌套融合关系。

#### 6.7.1 岛屿模型相对本期赛道竞争的增量优势

1. **真正独立的多路径搜索**：不同岛屿可走完全独立的进化路径（如岛A偏短周期、岛B偏长周期、岛C偏价值、岛D偏量价），物理隔离使各岛不会被彼此的选择压力拉向同一方向。
2. **更强的局部最优逃逸能力**：某岛陷入局部最优时，其他岛进化出的优质个体迁移进来可打破僵局；单种群一旦走偏只能靠随机注入缓慢恢复。
3. **更高质量的基因交流**：岛间迁移交换的是"别的岛已经进化过的优质个体"，比每代随机注入未经验证的随机公式质量更高、收敛更快。
4. **多样性更持久**：硬物理隔离下种群趋同速度显著慢于赛道竞争的软隔离。
5. **天然适配并行**：多岛可多进程并行进化，大种群场景下反而能提升 CPU 利用率。

#### 6.7.2 它解决赛道竞争（本期方案）的哪些痛点/局限

| 本期赛道竞争的局限 | 岛屿模型如何补足 |
|--------------------|-----------------|
| 软隔离：6 类个体仍在同一基因池，长期进化后可能通过交叉逐渐趋同 | 硬隔离：岛屿物理分开，趋同速度大幅降低 |
| 只按"因子类型"这一个固定维度分区，同一类型内无法走多条独立探索路径 | 每岛可设定不同探索方向（参数风格/市场假设/复杂度取向），维度更丰富 |
| 整个种群早期若被引向错误方向，类型分区无法把全局拉回正轨 | 多岛独立路径，某岛走偏不影响其他岛，迁移可纠偏 |
| 多样性崩溃后只能靠随机注入恢复，慢且随机 | 岛间迁移引入"外来优质基因"，恢复更快更有质量 |

#### 6.7.3 为什么本期不做（成本与门槛）

1. **种群规模门槛（最根本）**：硬拆分要求每岛至少约 50 个个体才能有效进化；默认种群 100 若开 4 岛，每岛仅 25 个，选择/变异基因池过小，效果反而不如单种群。要有效需把总种群提到 150~300+。
2. **算力代价**：种群提升后单次运行耗时约翻 3~5 倍（本期约 10 分钟 → 30~50 分钟），内存同步上升。
3. **开发复杂度**：多岛调度、岛间迁移通信、多进程同步、自适应分岛/合岛、跨岛血缘追溯、分岛前端曲线与筛选，开发量约 2~3 周。
4. **配置门槛**：岛屿数量、迁移间隔、迁移率、迁移拓扑、迁出策略等参数用户难以理解，需要额外的配置辅助。

#### 6.7.4 未来落地形态（供远期参考，本期不实现）

1. **弹性岛屿（自适应分岛/合岛）**：起跑仍为单种群+赛道竞争（不摊薄）；系统监测到连续多代多样性下降且最优 ICIR 无提升（疑似局部最优）时才分裂出新岛（部分个体+差异化新个体走不同方向），岛屿方向趋同时自动合并，最多到护栏上限。
2. **规模护栏（硬规则）**：`岛屿数 = min(4, floor(种群/50))`，每岛≥50；种群<150 时禁止开岛（前端灰掉并提示扩大种群），从根本上避免"越拆越弱"。
3. **参数自动推导 + 预设档位**：用户只看到"关闭/标准探索/强探索"档位，底层迁移间隔=`max(3, 总代数/5)`、迁移数=每岛×5%、拓扑固定环形；高级用户才展开手动配置。
4. **AI 配置顾问（降低使用门槛，规则兜底）**：根据股票池规模/字段数/调仓频率/时间长度/种群代数推荐档位与岛屿数、把迁移参数翻译成大白话、对矛盾配置做体检和一键修正；AI 仅做推荐解释，运行时以规则为准，AI 不可用不影响运行。
5. **与赛道竞争融合**：每个岛屿内部仍做 6 类赛道竞争，岛屿之间做迁移，形成"全局多岛 → 岛内分赛道"的两层多样性结构。

#### 6.7.5 本期仅做的数据结构预留（不写运行时逻辑）

为避免未来改表，本期在候选/代次表预留以下**可空字段**，本期运行时一律为空、不参与任何逻辑：

- `candidates.island_id`（可空，所属岛屿，本期恒为 NULL/单岛）
- `generations.island_id`（可空，该代汇总所属岛屿）
- `candidates.parent_island_id`（可空，迁移来源岛）
- 迁移事件预留：不在本期建表，远期落地时新增 `factor_mining_migrations`（run_id、from_island、to_island、candidate_id、generation、created_at）

**触发未来立项的条件（供参考）：** 本期单种群+赛道竞争上线后，实际任务反复出现"连续多代多样性低、陷入局部最优、扩大变异率/随机注入仍无法跳出"的情况，且用户愿意把种群提升到 200+、接受更长运行时间时，再启动岛屿模型。

### 6.8 AI引导变异（F2，本期必做）

> **定位：因子挖掘进化过程内部的智能增强功能，不是独立模块。** 只在进化过程中起作用，通过C1调度触发，与C2随机变异并列执行。F2通过F1的读接口查询相关经验，不直接操作F1数据。

#### 6.8.1 触发机制与成本控制

F2不是每代都调用AI，有明确的触发条件和成本控制：

| 触发条件 | 触发频率 | AI作用 |
|---------|---------|--------|
| D1停滞（stall≥2） | 停滞时必触发 | AI分析"为什么停滞、该往哪个方向突破" |
| D3多样性低（health<0.5） | 低多样性时必触发 | AI分析"当前种群缺什么类型、补什么方向" |
| 定期引导 | 每3代一次 | 对Top因子做定向优化建议 |
| 正常进化（无停滞、多样性够） | **不触发** | 纯C2随机变异，省AI成本 |

**设计原则：AI是"急救包"和"指南针"，不是"日常工"。** 正常进化靠C2随机探索保持广度，遇到问题时F2定向突破提高效率。

#### 6.8.2 与C1/C2/D3/B1/F1的协作流程

```
每代评估完成
    ↓
D3算多样性health、D1算stall
    ↓
C1判断是否触发F2（停滞/低多样性/每3代）
    ↓ 触发
C1决定AI引导变异比例（默认30%~50%，停滞时50%~70%，上限70%）
    ↓
F2调用AI：
  输入：Top10因子+市场环境+缺失类型+字段范围+约束盒子+F1相关经验Top5（通过F1读接口获取）
  输出：5~10个定向变异建议（目标因子+变异类型+具体内容+自然语言理由）
    ↓
校验管道（和C2完全相同）：
  AST语法→字段范围→复杂度→三层指纹去重
  不合法丢弃，名额回退C2随机变异
    ↓
AI引导变异因子 + C2随机变异因子 一起进入B1评估
    ↓
B1分别统计：AI引导变异平均ICIR vs 随机变异平均ICIR
    ↓
反馈C1：AI明显更优→提高比例；AI明显更差→降低甚至暂停1~2代
```

**关键边界：**
- C1是唯一决定F2触发时机和比例的模块，F2不自行决定调多少；
- C2和F2是并列执行器，都受C1调度，走相同校验管道，AI没有特权；
- F2通过F1的读接口查询相关经验，不直接读写F1数据表；
- B1评估时AI变异因子和随机变异因子公平竞争，没有特权。

#### 6.8.3 AI输入输出规范

**AI输入（每次调用）：**
```json
{
  "top_factors": [
    {"formula": "mean(close,20)/mean(close,60)-1", "icir": 0.45, "category": "trend", "coverage": 0.85}
  ],
  "market_env": {"regime": "bull", "avg_market_cap": "mid", "style_exposure": "growth"},
  "missing_categories": ["valuation", "quality"],
  "field_scope": ["roe_ttm", "pb", "close", "volume", "pe_ttm"],
  "constraints": {"max_formula_length": 6, "max_nesting_depth": 3, "allowed_operators": ["mean","rank","delta","zscore","/","-"]},
  "related_experiences": [
    {"formula_template": "rank({roe})-rank({pb})", "avg_icir": 0.52, "success_rate": 0.65}
  ],
  "trigger_reason": "stall=2, 连续2代无进步"
}
```

**AI输出（每次调用）：**
```json
{
  "suggestions": [
    {
      "target_factor_index": 3,
      "mutation_type": "field",
      "content": "将因子#3的close字段替换为roe_ttm，尝试估值维度",
      "reason": "当前种群均为趋势类，缺失估值维度，历史上rank(roe)-rank(pb)结构有效（来自经验库）"
    }
  ],
  "analysis_summary": "当前种群趋同于短周期趋势，建议向估值+质量维度探索"
}
```

#### 6.8.4 前端交互设计

**Step4 配置页（繁殖与变异折叠区，高级模式）：**
```
☑ AI引导变异（默认开启）
AI变异比例上限：[滑块 0%~70%，默认50%]
触发敏感度：○保守  ●平衡  ○激进
（说明：AI仅在停滞或多样性低时介入，正常进化不调用，不增加常规耗时）
```

**Step5 运行页（新增AI引导状态区）：**
```
┌─────────────────────────────────────────────┐
│ AI引导状态：✅ 已触发（原因：停滞2代）       │
│ 本代AI变异：24个建议 → 20个通过校验 → 4个回退随机 │
│ AI效果对比：AI引导平均ICIR 0.45 vs 随机 0.38 │
│ [展开] AI建议列表                              │
│   #1 因子#3 字段替换 close→roe_ttm           │
│      理由：当前缺估值维度，历史rank(roe)-rank(pb)有效 │
│   #2 ...                                       │
└─────────────────────────────────────────────┘
```

**结果页因子详情：**
- 来源标签：`AI引导变异` / `随机变异` / `交叉` / `精英保留`
- AI引导变异的因子显示"AI建议理由"

#### 6.8.5 异常处理

| 异常 | 处理 |
|------|------|
| AI调用失败/超时 | 回退纯C2随机变异，不阻塞进化，记录日志 |
| AI建议全部校验不通过 | 名额全部回退C2，记录"AI建议0通过" |
| AI持续无效（连续2代ICIR低于随机） | C1自动降低AI比例，暂停1~2代后再试 |
| 用户手动关闭F2 | 回退纯C2随机变异，C1不再分配AI名额 |
| AI调用成本超预算 | 限制每代最多调用1次，每次最多输出10个建议 |

### 6.9 历史经验库（F1，独立功能模块，本期必做）

> **架构原则：F1是独立于因子挖掘的功能模块，设置中单独页签。因子挖掘只是F1的输入输出一个口子，不与F1维护逻辑耦合。核心红线：挖掘→沉淀→复用→再挖掘闭环通过明确接口实现。**

#### 6.9.1 模块定位与边界

**F1是什么：** 独立的因子经验沉淀与复用模块，负责存储、维护、检索、导入导出历史优质因子骨架。

**F1不是什么：**
- 不是因子挖掘的子流程——不嵌入5步向导，有独立页面；
- 不做因子评估——评估是因子挖掘模块的事，F1只存评估结果；
- 不做进化计算——进化是因子挖掘模块的事，F1只提供经验抽取。

**和因子挖掘的边界：**

| 维度 | F1独立模块 | 因子挖掘模块 |
|------|-----------|-------------|
| 知道对方的什么 | 只知道"有人存经验、有人取经验"，不知道进化流程 | 只知道"有个经验库可以存取"，不知道维护逻辑 |
| 交互方式 | 提供2个API接口（存/取） | 调用2个API接口，不直接读写F1表 |
| 页面 | 设置→因子基础配置→历史经验库（独立页签） | 5步向导（不嵌入F1维护功能） |
| 数据 | 4张独立表（factor_experience系列） | candidates/runs/generations等独立表 |
| 可独立演进 | 第三方格式、共享库、智能推荐可独立迭代 | 进化算法可独立迭代，不影响F1 |

#### 6.9.2 两个接口（核心红线贯穿）

**接口1：输出口子（因子挖掘→F1，唯一写接口）**

```
POST /api/f1/experience
Body: {
  "formula_ast": {...},
  "category": "trend",
  "complexity": {"operators": 4, "nesting_depth": 2},
  "source": "ai_generated",
  "origin_run_id": "run_xxx",
  "origin_factor_id": "factor_xxx",
  "metrics": [{"type": "icir", "value": 0.45, "period": "2025-2026", "is_oos": true}],
  "task_context": {"stock_pool": "chinext", "market_env": "bull", "field_scope": ["roe","pb","close"]}
}
Response: { "status": "created|updated|duplicate", "experience_id": "exp_xxx" }
```

F1内部处理：参数泛化→三层指纹去重→自动分类→自动打标签→写入4张表。因子挖掘不直接操作F1表。

**接口2：输入口子（F1→因子挖掘，唯一读接口）**

场景A：初始种群抽取
```
GET /api/f1/experience/sample?stock_pool=chinext&field_scope=roe,pb,close&market_env=bull&count=25
Response: [
  {"formula": "rank(roe_ttm)-rank(pb)", "category": "valuation", "source": "experience"},
  ...
]
```
F1内部：字段硬过滤→场景匹配打分→加权随机抽取→参数实例化→返回具体公式。

场景B：F2相关经验查询
```
GET /api/f1/experience/related?top_categories=trend,volume_price&missing_categories=valuation,quality&count=5
Response: [
  {"formula_template": "rank({roe})-rank({pb})", "avg_icir": 0.52, "success_rate": 0.65, "category": "valuation"},
  ...
]
```

**核心红线：** 因子挖掘只传配置、收结果，F1内部的加权衰减、场景匹配、去重、过滤对因子挖掘透明。F1中归档/删除/优质标记的经验在抽取时自动处理，因子挖掘无感知。

#### 6.9.3 独立页面设计（4个Tab）

**页签位置：** 设置→因子基础配置→历史经验库（与因子模板配置并列）

**Tab1：概览**
```
┌─────────────────────────────────────────────────────────┐
│ 历史经验库概览                              [导入] [导出] │
├─────────────────────────────────────────────────────────┤
│ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐  │
│ │总经验 156│ │本月新增 8│ │总使用 423│ │平均成功率58%│ │
│ └──────────┘ └──────────┘ └──────────┘ └──────────┘  │
├──────────────────────────┬──────────────────────────────┤
│ 类型分布饼图              │ 来源分布饼图                  │
│ 趋势32% 估值28% ...       │ AI生成55% 随机25% ...        │
├──────────────────────────┼──────────────────────────────┤
│ 积累趋势折线图（按月新增） │ 成功率分布柱状图              │
└──────────────────────────┴──────────────────────────────┘
```

**Tab2：经验列表**
- 列：公式模板（过长折叠）、类型、来源、创建时间、最后使用、使用次数、成功率、平均ICIR、状态（正常/优质/归档/待补字段/不可用）
- 筛选：类型/来源/状态/时间/最小成功率/最小ICIR
- 排序：创建时间/使用次数/成功率/ICIR
- 搜索：公式关键字
- 批量操作：导出/标记优质/归档/删除
- 点击→详情弹窗（公式结构、标签、效果历史曲线、使用记录、关联因子、操作）

**Tab3：导入导出**
- 导入区：3种方式（上传JSON/CSV、共享库同步、从其他项目拷贝）
  - 上传后自动解析预览→显示总条数/类型分布/字段依赖检查/问题标记
  - 确认导入范围（全部/只导入无问题的/按类型筛选）
  - 异步执行→完成后消息通知→导入报告（成功/更新/过滤/缺字段/格式错误）
- 导出区：选择范围（全部/按筛选/勾选）、格式（系统JSON/通用CSV/第三方预留）、内容（效果数据/标签/字段依赖、参数模板还是具体公式）→生成下载

**Tab4：清理建议**
- 每季度自动生成（也可手动生成），消息通知
- 三类清单：待清理（超3年成功率<20%）、过期（超5年成功率<30%）、不可用（字段废弃）
- 每条显示系统建议（归档/删除/保留/重新验证）
- 操作：逐条改主意 或 一键"按建议批量清理"
- 默认归档不删除（保守可恢复），删除有二次确认
- 完成报告：归档/删除/保留各多少、释放多少空间

#### 6.9.4 数据自动沉淀与反馈闭环

```
因子挖掘发现优质因子
    ↓ 人工确认入库
接口1自动存回F1
    ↓ F1内部
参数泛化→去重→分类→打标签→写4张表
    ↓
下次任务初始种群
    ↓ 接口2抽取
F1内部：字段过滤→场景打分→加权随机→参数实例化
    ↓ 返回具体公式
加入初始种群 → 进化 → 验证
    ↓ 验证结果
接口1反馈更新use_count/success_count/效果数据
    ↓
成功率高的经验权重自动提高 → 下次更容易被抽中
（正向循环）
```

#### 6.9.5 共享库模式（单用户推荐）

- F1库文件路径可配置为共享目录（如`D:\ai_project\shared\factor_experience.duckdb`）
- 多个项目实例读写同一个库，A项目积累的经验B项目直接能用
- DuckDB单文件支持并发读，单用户写入冲突概率极低，加文件锁
- 每个经验记录`origin_project_id`，但所有项目都能检索
- 不配置共享目录时，各项目用本地库，通过导入导出迁移

#### 6.9.6 第三方格式兼容（插件式转换层）

```
第三方格式文件
    ↓
格式识别器（自动识别JSON/CSV/WQ/qlib）
    ↓
格式转换器（插件式，每种格式一个转换器）
    ↓ 转换为系统内部AST
标准校验管道（字段范围/复杂度/去重）
    ↓
写入F1
```

- 首期支持：系统JSON（完整）+ 通用CSV（简化，列：formula,category,icir,coverage,description,source）
- 第三方格式（WQ Alpha/qlib/聚宽等）：预留转换接口，新增格式只需写一个转换器，不改核心逻辑
- 字段映射表：第三方字段名→系统字段名，映射不了的标记"待补字段"
- 算子映射表：第三方算子→系统等价算子，无等价算子的标记"不支持算子"

### 6.10 超参数元学习（F3，⚠ 未来规划方向，不在本期）

> **明确不纳入本期开发、排期与验收。** 本期C1自适应调度器已覆盖动态调参核心价值，F3待积累足够历史任务后再立项。

**是什么：** 用元学习自动学习"什么超参数配置在什么任务/市场环境下效果最好"，系统根据历史任务经验自动推荐甚至动态调整超参数。

**本期不做的原因：**
1. **数据门槛**：需要50+历史任务才能学出可靠策略，本期刚上线无足够样本；
2. **C1已覆盖**：C1自适应已能根据停滞/多样性动态调变异率/交叉率/注入率；
3. **超参数空间小**：种群/代数/变异率/交叉率/注入率就几个，都有标定默认值，简单模式零配置；
4. **实现复杂收益不明确**：MAML/Reptile框架复杂调试难，且本身有过拟合风险。

**未来立项条件：** 积累50+已完成历史任务；用户频繁做大量类似任务，超参数调优成为瓶颈；C1自适应明确不够用。未来先做"基于历史任务相似度的超参数推荐"（简单匹配），再考虑真正的元学习。

### 6.11 性能优化：并行评估（G1）+ 公共子表达式缓存（G2）（本期必做）

> G1负责"快算"（多进程并行），G2负责"少算"（去重缓存），两者叠加先去重再并行，效果1+1>2。核心原则：先保证结果对，再追求跑得快，缓存和并行都加结果校验兜底。

#### 6.11.1 G1 并行评估

**并行粒度（两层，首期都做）：**
1. **子表达式级并行**：G2去重后150~250个唯一子表达式，分到N个进程，每进程算30~40个，向量化计算（500股×300日一次算完）
2. **因子级并行**：组装后的100个因子的IC/ICIR/覆盖率/换手率/分层收益等指标计算，分到N个进程并行

**进程管理：**
- 进程数自适应：默认`min(6, cpu_count-2)`，根据可用内存自动调整（<8GB开4进程，<4GB开2进程，<2GB串行）
- 进程池复用：任务开始时启动一次，全程复用，不每代启动销毁，避免启动开销
- 主进程汇总写入：子进程只计算，结果通过共享内存返回主进程，主进程统一写DuckDB和日志，子进程不写任何共享存储，避免数据竞争

**负载均衡：**
- 按子表达式预估复杂度降序排列（简单字段引用O(1) < 窗口计算O(n) < 排名O(n log n) < 嵌套多层）
- 贪心分配：每次把当前最复杂的子表达式分给当前负载最小的进程
- 大任务优先：复杂子表达式先分配，避免最后剩一个大任务拖慢整体
- 每代监控并行效率（实际加速比/理论加速比），持续<50%自动减少进程数

**通信优化：**
- 行情数据只读共享内存，所有进程不复制，零拷贝访问
- 子进程只返回结果矩阵（共享内存），不返回日志/调试信息
- 子进程日志写本地文件，主进程最后汇总

#### 6.11.2 G2 跨个体公共子表达式缓存（层面1，本期做）

**5步流程：**
1. **AST遍历提取子表达式**：递归遍历100个因子的AST，提取所有子表达式（包括叶子节点字段引用），平均500~800个
2. **SHA-256强哈希去重**：算子类型+参数值+字段唯一ID+数据快照版本号全部序列化后算SHA-256，语义相同则哈希相同。去重后通常只剩150~250个，去重率60%~70%
3. **向量化批量计算**：唯一子表达式用DuckDB SQL或numpy向量化计算（500股×300日一次算完，不逐股票循环），结果用float32存储
4. **缓存到DuckDB内存表**：`subexpr_cache(hash PK, formula, values_matrix BLOB, status)`，任务生命周期内有效
5. **因子组装查缓存**：递归遍历AST，每个节点查缓存，所有子表达式均已缓存，不触发新计算。用单元测试验证组装时`compute_node`调用次数为0

**缓存管理：**
- 生命周期与挖掘任务绑定，任务结束释放
- 断点续跑时持久化到磁盘，恢复时校验数据快照哈希，快照变了自动废弃重算
- 预筛阶段（短样本1年）和全量验证阶段（3~5年）分别建缓存，数据范围不同不混用
- 每代结束及时释放上一代缓存，不累积
- 缓存带`schema_version`和`hash_algorithm_version`，版本不匹配自动废弃

**层面2（父子代增量评估）放P1**：对比父子代AST树找出变化的子树，只算变化部分复用不变部分。开发1~2周，缓存失效管理复杂，等层面1验证效果后再做。

#### 6.11.3 G1+G2配合总流程

```
主进程：
  1. G2提取100个因子的所有子表达式 → 500~800个
  2. G2 SHA-256哈希去重 → 150~250个唯一子表达式
  3. G1按复杂度贪心分到N个进程
  4. N个进程并行向量化计算，结果通过共享内存返回
  5. 主进程汇总写缓存表（含status校验）
  6. G1把100个因子的组装+指标计算分到N个进程并行
  7. 每进程从缓存查子表达式结果，组装因子值，算IC/ICIR等
  8. 主进程汇总写generations表
  9. 抽样校验：随机抽5个因子，缓存结果与直接全量计算对比，差异>1e-6报警废弃缓存
```

#### 6.11.4 性能目标

| 指标 | 无优化 | G1并行 | G1+G2叠加 |
|------|--------|--------|-----------|
| 每代短样本评估 | ~5分钟 | ~50秒 | **~25~35秒** |
| 20代进化总耗时 | ~100分钟 | ~17分钟 | **~8~12分钟** |
| 端到端（含初始种群+最终验证） | ~2小时 | ~30分钟 | **~20~25分钟** |

该目标为基线参考，实际以压测结果为准。性能监控每代记录：子表达式总数/唯一数/去重率、并行效率、各阶段耗时，写入generations表，前端展示。

#### 6.11.5 风险与规避（必须落实）

| 风险 | 影响 | 规避措施 |
|------|------|---------|
| 缓存哈希碰撞/语义不等价 | 因子值算错，结果不可信 | SHA-256强哈希（非Python内置hash，Python hash有随机化种子），哈希包含算子+参数+字段ID+数据快照版本；每代随机抽5个因子缓存结果与直接全量计算对比，差异超1e-6报警并废弃缓存重算 |
| 缓存污染（错误结果被缓存） | 一个子表达式错，几十个因子跟着错 | 每个子表达式算完检查NaN/Inf/空值比例，异常不写入缓存标记failed；缓存表加status字段（valid/failed/computing）；失败记录错误原因，前端展示数据质量问题 |
| 数据竞争/写入冲突 | 任务崩溃、数据损坏 | 主进程汇总写入，子进程不写共享存储；DuckDB单连接写入利用自带锁；进度用原子操作更新；主进程监控子进程心跳，超30秒无响应杀了重试 |
| 内存爆炸 | OOM崩溃或swap变慢 | 行情数据只读共享内存不复制；缓存矩阵用float32；进程数按内存自适应；每代结束及时释放上一代缓存；因子值矩阵用稀疏存储（大量NaN停牌缺失） |
| 并行结果不一致/浮点误差 | 固定种子不可复现 | 子表达式级并行（每个子表达式单进程内完整计算，不跨进程合并部分结果）；IC/ICIR等指标主进程单线程计算；验收用容差比较（<1e-6算一致）；提供串行严格复现模式兜底 |
| 去重率不达预期 | 缓存收益打折 | 每代统计去重率并写入generations表，前端展示；持续<40%提示用户减少字段范围或降低AI创新度；基础计算（字段引用、常用均线/排名）总是预计算缓存，去重率100% |
| 并行效率不达标 | 只快3~4倍而非6倍 | 进程池复用避免启动开销；贪心负载均衡按复杂度分配；大任务优先；子进程只返回结果矩阵（共享内存零拷贝）；每代监控并行效率，持续<50%自动减少进程数 |
| 进程崩溃/异常 | 任务卡死 | 子进程每5秒发心跳，主进程超30秒杀了重试；同任务重试3次失败标记跳过不阻塞；崩溃不影响主进程和其他子进程；错误汇总任务结束统一展示 |
| 断点续跑复杂 | 恢复不全或结果不一致 | 代级断点（每代结束写checkpoint），不做进程内细粒度断点；缓存可选恢复（快照没变加载缓存省时间，快照变了重算保正确）；恢复后第一代抽5个因子校验一致性 |
| 缓存版本不兼容 | 升级后结果错或崩溃 | 缓存带schema_version和hash_algorithm_version，恢复时校验不匹配自动废弃；算子定义带版本号，逻辑变了版本+1缓存失效；版本升级写迁移脚本 |

**核心原则：先保证结果对，再追求跑得快。** 正确性兜底（抽样校验+结果合法性检查+心跳重试+串行模式）是硬要求，不能为了速度跳过。

## 7. 提交时资源确认与最终校验

Step 4 点击“开始挖掘实验”后，先执行秒级最终准备校验，再弹出资源确认框。最终校验只检查跨步骤一致性、快照存在性、Step 3 校验有效期（默认 24 小时，可配置）、参数边界、资源就绪和挖掘任务锁，不重复执行 Step 3 的深度字段扫描。

提交前显示资源卡片（首期简化版）：

- 预计耗时区间（基于数据量 × 候选数的规则估算，标明"估算"）。
- 当前可用磁盘/内存是否达标（磁盘≥20%、内存≥2GB）。
- 资源不足时展开详情并给出调整建议（减少候选数/缩短时间范围/缩小股票池）。

资源充足时默认折叠，显示预计耗时和确认按钮；资源不足时阻断。用户可以"保存暂存"，但不能绕过资源确认直接执行。最终校验通过后才冻结不可变实验快照并获取挖掘任务锁。后续有历史任务数据后，再细化为精确的中间结果行数、峰值内存和磁盘空间估算。

## 8. Step 5：进化执行与结果

### 8.1 状态

`暂存 draft → 待执行 queued → 进化中 running → 最终验证中 validating → 已完成 succeeded`。

异常状态补充：`失败 failed`、`取消中 cancel_requested`、`已取消 cancelled`、`已暂停 paused`、`已收敛 converged`、`需重新校验 invalidated`。

"待执行"定义为 5 个步骤全部通过、最终校验和资源确认完成、尚未获得执行锁；"暂存"允许配置不完整。草稿还可标记"待修复后重校验"和"需重新校验"。

每代保存 checkpoint。失败或用户请求取消后，保留 checkpoint 和已落库结果一段可配置时间，用户可选择"从 checkpoint 继续"或"完全放弃并清理"。"取消"本身不立即删除可续跑数据；只有用户选择完全放弃或清理策略到期时才删除。**中断（暂停）**后任务状态变为`paused`，释放计算资源但保留全部现场和已进化结果，用户可随时"继续"从断点恢复。**提前停止**后保留当前已进化的所有代的结果，直接进入最终验证。

### 8.2 单任务锁

- 同一因子仓库同一时间只允许一个挖掘任务进入 `running`。
- 锁由后端数据库/任务服务持有，带 owner、心跳、过期时间和任务 ID。
- 页面关闭、浏览器断开不释放锁；任务取消或服务恢复流程才释放。
- 系统全局最多保留一个训练任务（状态为 `queued` 或 `running`）。任何其他"运行/提交"请求都必须先由后端原子检查任务状态；发现已有任务时直接拒绝，不创建第二个排队任务，并返回当前任务 ID、状态、当前代数和预计剩余时间。前端按钮禁用只是体验优化，不能代替服务端校验。
- 不允许排队等待多个训练任务；用户必须等待当前任务结束、取消或失败后，才能提交下一次训练。
- 训练任务只能使用受控并行池，设置最大内存和磁盘临时目录。

### 8.3 进化执行界面

运行后可离开页面，任务中心持续显示进度。页面关闭、刷新或网络断开不影响后台任务。

#### 8.3.1 进化进度总览

顶部展示进化进度：

```
进化进度：第 8 代 / 共 20 代（预计还剩 6 分钟）
收敛状态：未收敛（最近3代最优ICIR提升0.03）
种群多样性：●●●●●○○○○○ 52%（高）
```

- 当前代数/最大代数，每代进度条
- 收敛状态：已收敛/未收敛/接近收敛
- 种群多样性：度量种群内个体差异度，低于30%时警告"种群早熟，建议增加随机注入率"

#### 8.3.2 进化曲线

实时展示ICIR进化曲线，每代评估完更新：

```
ICIR进化曲线：
  0.45 │          ●
  0.40 │        ●   ●
  0.35 │      ●       ●
  0.30 │    ●           ●
  0.25 │  ●
       └──────────────────
         1  2  3  4  5  6  7  8  代

  — 最优ICIR  ··· 平均ICIR
```

- 最优ICIR曲线（实线）和平均ICIR曲线（虚线）
- 收敛参考线来自训练配置的收敛阈值，不得把`0.08`写成固定合格门禁
- 连续3代最优ICIR提升小于收敛阈值时，提示"已收敛，建议停止"，是否停止由用户确认

#### 8.3.3 当前种群Top因子

每代评估完实时更新当前种群Top 10（新进Top10的高亮闪烁）：

```
📊 当前种群Top因子（第8代，100个个体）
  1. std(close,5)/mean(std(close,20),60)  ICIR=0.42  覆盖率=95%  [变异自第6代#3]
  2. delta(roe_ttm,4)/std(delta(roe_ttm,4),8)  ICIR=0.39  覆盖率=88%  [精英保留]
  3. rank(turnover)-rank(mean(close,10)/mean(close,60))  ICIR=0.36  [交叉自第7代#2和#5]
  ...
[查看全部种群] [查看血缘追溯]
```

用户可点击候选查看详情（公式、IC时序、分层收益、父代信息、变异/交叉来源）。

#### 8.3.4 每代操作说明

每代执行以下操作：

1. **评估**：短样本（默认1年）计算每个体的ICIR/覆盖率/换手率
2. **选择**：Top 30%作为种子，精英保留Top 5直接进入下一代
3. **变异**（60%新个体）：参数微调（窗口期5→10）、算子替换（mean→ema）、字段替换（close→open）、结构变异（增删算子）
4. **交叉**（30%新个体）：随机选两个种子，交换子表达式
5. **随机注入**（10%新个体）：全新受约束随机公式，防早熟
6. **校验**：复杂度校验+去重+字段依赖检查，不合法的丢弃用随机个体补
7. 生成下一代，重复

#### 8.3.5 中断、暂停与取消

| 操作 | 行为 | 后续 |
|------|------|------|
| **中断** | 暂停当前代进化，保存checkpoint，任务状态`paused`，释放计算资源 | 可随时"继续"从断点恢复 |
| **提前停止** | 终止进化，保留所有已进化代的结果，进入最终验证 | 任务状态`converged`，直接验证当前种群Top因子 |
| **完全放弃** | 终止任务，清理所有中间数据 | 任务状态`cancelled`，不可恢复 |

点击"中断"后立即暂停当前代，按钮变为"继续"；点击"提前停止"时弹出确认框，提示"将保留前X代的进化结果，对当前种群Top 50做最终验证"。

#### 8.3.6 资源与日志

- **资源占用可视化：** CPU/内存/磁盘三个小进度条，悬停显示具体数值
- **速度指标：** 显示当前进化速度（X代/分钟）和已用/预计总资源
- **日志增强：** 按代分组可折叠，支持级别筛选、关键字高亮、自动滚动，ERROR红色可展开堆栈

#### 8.3.7 最终验证

进化结束（收敛/达到最大代数/用户提前停止）后，对最终种群Top 50（可配置）做全量评估（完整训练区间）：

- 计算精确的IC/Rank IC、ICIR、覆盖率、分层收益、多空收益、换手率
- 样本外（OOS）验证
- 与已有因子库比对去重
- 通过门禁（ICIR≥0.3、覆盖率≥80%、OOS方向一致、相关性<0.95）的为有效因子

最终验证进度单独展示，完成后进入结果总览。

### 8.4 结果总览

进化完成后展示：

**概览区：** 进化代数、收敛状态、最优ICIR、平均ICIR、种群多样性、有效因子数、总耗时

**进化曲线：** 完整的ICIR进化曲线（最优/平均），可悬停查看每代详情

**有效因子列表：** 通过门禁的因子，按ICIR排序，展示公式、ICIR、覆盖率、分层收益、换手率、来源（哪一代、变异/交叉/精英）、血缘追溯

**历史最优因子：** 整个进化过程中所有代的Top因子（不只是最终代），按全量验证ICIR排序

**种群分析：** 因子类型分布、复杂度分布、血缘关系图

**被淘汰个体：** 按代展示被淘汰的个体及原因（ICIR不足/相似/复杂度超标）

**操作：** 导出、批量回测、加入FactorSet、提交为正式因子版本、保存配置为模板、保存优质骨架为个人模板

AI评价功能标注"后续开放（P3）"，首期灰掉。结果页不自动激活因子或模型。

**后续操作说明：**
- **回测入口：** 选中因子后点"回测验证"，跳转到现有回测模块并自动携带因子公式、股票池快照和调仓频率；若现有回测暂不支持单因子回测，首期提供"导出因子公式"功能。
- **加入 FactorSet：** 批量加入前按 formula_hash 检查目标 draft FactorSet 中是否已存在相同公式，已存在的跳过并提示"X 个因子已存在，已跳过"；必须选择一个 draft 状态的 FactorSet，frozen 集合不可写。
- **候选转正式因子：** 用户在结果页点"提交评估"后，系统在现有 factors 表创建 draft 状态的因子版本并触发正式评估，评估通过门禁后进入 testing 状态；后续 shadow、active 流程属于因子模型页。
- **保存为模板：** 优质公式骨架可保存为个人模板，下次作为初始种群的高质量起点。

### 8.4 草稿与模板

向导顶部显示当前草稿名称和“切换草稿”入口。草稿列表展示名称、最后修改时间、完成步骤、状态和继续/复制/删除操作；配置不完整也允许保存，删除需要二次确认。字段校验失败进入数据修复时，草稿状态为“待修复后重校验”，不影响其他草稿。

模板分为系统预设和个人模板两类（单用户场景，不涉及团队共享）。系统预设不可删除，只能复制。**首期模板管理在向导的"快速开始"区域内完成（新建/编辑/删除），不做独立管理页面；模板数量超过 20 个后再独立成页。** 模板保存规则和参数，不保存具体股票代码、候选结果或计算结果；可在任意步骤保存部分模板，但未完成的模板只能生成草稿，不能直接执行。**模板完整性校验：** 使用前检查配置完整性，不完整的模板在卡片上标注"配置不完整"并禁用"直接开始"按钮，仅允许"查看并修改"。

模板编辑采用版本化保存，修改必须填写版本说明；历史训练任务保存 `template_id`、`template_version` 和解析后的 `rule_hash`，不受模板后续修改影响。模板使用 `end_date=latest` 时，在本次使用前显示当前数据截止日和预计候选数，确认后解析并冻结。

### 8.5 清理策略

“清理之前训练数据”不能删除仍被模型、评分、回测引用的数据。采用引用计数/快照引用保护：

- 失败、取消任务的临时目录在确认无运行进程后清理。
- 已完成任务保留配置快照、指标、模型权重、审计和最小复现元数据；删除候选明细、临时宽表和中间缓存。
- 被活动模型、回测或审计引用的原始数据不可自动删除。
- 已完成任务的未引用中间数据默认保留 7 天；超过保留期由清理任务删除，删除前生成清单和审计记录。
- 任务详情提供“立即清理临时数据”按钮，允许用户在 7 天前手动清理；清理前必须确认任务已结束且中间数据未被模型、评分、回测或审计引用。
- 提供“立即清理临时数据”与“保留结果并压缩归档”两个动作，默认不删除业务数据。

## 9. 关键验收标准

1. 任意训练都能回溯股票池、过滤规则、数据字段、进化参数、初始种群来源、目标、时间切分、随机种子和资源配置。
2. 修改上游配置后，下游校验状态自动失效。
3. 公式字段缺数据、PIT 失败或有效截面不足时，不能进入进化参数和提交。
4. 同一因子仓库不会出现两个并行训练任务；已有 `queued/running` 任务时再次点击运行必须被后端拒绝；异常退出后锁可恢复。
5. 任务完成后不删除仍被引用的模型/回测数据，临时数据可清理且有审计。
6. 前端关闭、刷新或离开页面后，任务仍可在任务中心跟踪。
7. 默认 5 步流程与模板快速开始均不能绕过字段深度校验、最终准备校验、资源确认和服务端训练锁。
8. 用户勾选的字段全部通过才可继续；系统不得自动剔除问题字段或以零值替代。
9. 取消/失败任务可在保留期内从 checkpoint 继续；完全放弃后才清理可续跑结果；提前停止保留已进化结果并进入最终验证。
10. **遗传算法专项：** 每代必须执行选择→变异→交叉→评估完整流程；精英保留个体不得变异；随机注入率不得为0（防早熟）；每代ICIR和种群多样性必须记录；收敛判断必须基于连续N代提升阈值，不得用固定ICIR值作为门禁；最终验证必须用全量样本，不得用短样本结果代替。
11. **初始种群专项：** AI生成、枚举、随机三部分比例可配置；AI生成的骨架必须经过系统校验（AST/字段/算子/复杂度），不合法的丢弃并用随机个体补；API调用失败时自动降级为枚举+随机，不阻塞进化流程。
12. **血缘追溯专项：** 每个候选必须记录代数、父代ID、操作类型（elite/mutation/crossover/random/ai_generated/enumerated）；结果页可追溯任意因子的完整进化路径。
