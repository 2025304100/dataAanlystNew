# G3 验收与 Stage 2-5 执行记录（2026-08-22）

## 本轮完成项

### G3 固定验收夹具

- `scripts/seed_ui_acceptance.py` 已生成隔离 SQLite 固定数据：3 个历史回测、126 笔交易流水、165 条决策证据、54 条拒绝或数据阻断证据、1 条部分成交。
- 固定夹具同时写入 168 条 `BacktestExecutionFill` 和估值快照；其中首个运行的每日五维守恒报告已验证 `PASSED`，未成交的卖出计划以显式 `REJECTED` 记录保留，避免把合法拒单误判为缺失撮合。
- 部分成交计划：请求 1000，已成交 600，剩余 400，未成交原因为 `VOLUME_LIMIT`。
- 夹具验证：`tests/test_seed_ui_acceptance_fixture.py` 通过。
- 隔离后端可由 `DATABASE_URL=sqlite:///.../tmp/g3_ui_acceptance_20260822.db` 启动；前端验收代理使用 `frontend/vite.g3.config.ts`，仅把 `/api` 指向隔离后端 `127.0.0.1:18000`，不改变常规开发代理。

### Stage 2：历史估值不漂移

- 新增 `backtest_valuation_snapshots`，按 `run_id + trade_date + symbol_id` 持久化完成回测的日终估值、权益、权重及来源行情元数据。
- 新回测在与成交事件相同事务内写入快照；`GET /backtest/runs/{id}/positions` 优先读取该快照。
- 后续供应商修订 `daily_bars.close` 不会改变已完成回测的 `mark_price` 与 `market_value`。
- 无快照的历史运行保持既有 `LEGACY_TRADE_APPROXIMATION` 兼容读取，不伪称其已可复现。
- 迁移：`wps_0023_038_backtest_valuation_snapshots`，SQLite 与 MySQL 均通过 Alembic DDL 创建表和唯一索引。

### Stage 5：现金守恒与取消终态

- `reconcile_trade_date()` 现在在存在 `cash_ledger` 且有精确 Evidence -> SimOrder 关联时，按 `SimTrade.amount + fee` 核对初始资金到当前现金余额；差异生成 `NAV_BROKEN`。
- 旧库没有 `cash_ledger` 时保留 fail-soft 的 `NOT_CHECKED` 兼容语义，不把缺失历史数据错误标记为对账失败。
- `cancel_task_with_cleanup()` 已在释放本地 executor/锁/session 后将任务原子置为 `cancelled` 并加终态锁，防止已释放资源的运行任务被恢复或继续执行。
- 新增 `backtest_runs.reproducibility_status/reproducibility_reason` 与迁移 `wps_0023_039_backtest_reproducibility_marker`：无策略快照和统一决策链的历史运行统一标记为 `legacy/non_reproducible`，新快照驱动运行标记为 `reproducible`。
- 回测列表/详情 API 透传可复现性状态；`scripts/backtest_reproducibility_report.py` 输出只读 JSON 迁移报告，兼容 SQLite 与 MySQL。
- 新增 `scripts/backtest_conservation_report.py`：按成交首次执行日和后续部分成交日重建 `DecisionEvidence -> OrderPlan -> Fill -> Position -> Cash` 五维日报；跨日 T+1 与手续费均按账本处理，历史旧回测只输出 `legacy/non_reproducible` 标记。

## G3 签署状态

G3 浏览器矩阵已通过：BT-UI 自动化当前收集的 11 项全部 `PASSED`，耗时 50.70 秒。隔离前端 `http://127.0.0.1:5174`、隔离后端 `http://127.0.0.1:18000` 均返回正常响应。

证据已归档到 `test_output/g3/`：11 个请求摘要 JSON、11 个 HAR 和 11 张截图；每个 JSON 均记录前端/后端地址、执行时间、请求路径和响应状态。当前记录覆盖验收矩阵中的 BT-UI-01、02、04、08/09、10、11~15、17、18、20；方案要求的 BT-UI-03、05~07、16、19 如需单独签署，应在同一验收机补充对应用例。

复核命令：

```powershell
$env:PYTHONPATH='.'
$env:E2E_BACKEND_URL='http://127.0.0.1:18000'
$env:E2E_FRONTEND_URL='http://127.0.0.1:5174'
$env:G3_RECORD_HAR='1'
.venv\Scripts\python.exe -m pytest tests/e2e/test_backtest_acceptance_matrix.py -m e2e -q
```

归档位置为 `test_output/g3/`，每例已保留请求摘要 JSON、截图和 HAR；Playwright 依赖来自仓库锁定目录 `.pip_packages`。

## 本轮验证

```text
.venv\Scripts\python.exe -m pytest tests/test_stage2_daily_bar_pit.py \
  tests/test_backtest_execution_linkage_api.py \
  tests/test_backtest_partial_fill_lifecycle.py \
  tests/test_backtest_decision_linkage.py \
  tests/test_backtest_detail_snapshot_contract.py -q
16 passed

.venv\Scripts\python.exe -m pytest tests/test_whitebox_task_state_machine.py \
  tests/test_frp1_2_ac10_fault_injection.py -q
34 passed
```

## 后续优先级

本轮新增验证：迁移链、回测结果投影、五维守恒桥接共 31 项通过；因子输入评分按钮与回测中心前端定向测试共 22 项通过；TypeScript、Vite production build、Python compileall、`git diff --check` 均通过。

本轮追加验证：`tests/test_backtest_conservation_report.py` 2 项通过。

1. 在目标数据库执行 `alembic upgrade head`，运行 `python scripts/backtest_reproducibility_report.py --db-url ...` 归档历史迁移报告。
2. 在目标数据库升级到 `wps_0023_040` 后执行 `python scripts/backtest_conservation_report.py --db-url ... --run-id ...`，归档每日报告并处理 BLOCKED 差异。
3. 完成真实新旧链路连续 10 个交易日 G5 双跑，再进入 G6 单组合灰度和 G7 监控/回滚演练。

### G5 双跑报告入口

- `scripts/g5_dual_run_report.py` 提供只读报告入口：输入真实旧/新链路按交易日导出的决策快照，复用 `g5_dual_run_replay` 的逐证券候选、动作、仓位和拒绝原因比较，并输出可归档 JSON。
- 输入不完整、重复日期或缺少任一链路时直接失败；报告存在漏跑日期时不会准入 G6。脚本退出码为 `0` 表示满足 G6 前置条件，`2` 表示报告生成成功但未满足条件。
- 该入口不创建订单、不写数据库、不把合成 runner 结果当作生产验收结果。
- G5 回放报告的每日链路 ID 现在包含真实 `portfolio_id`；窗口内任意故障/漏跑日期都会阻止 `g5_eligible_for_g6`，即使其余有效天数达到 10 天。
- 自动交易新增 `GET /api/v1/portfolios/{portfolio_id}/auto-trade/switch-readiness` 只读门禁接口；在双跑差异尚未持久化、未提交可审计 G5 汇总前，明确返回 `can_switch=false`，不再使用旧的无条件切换占位。
- 新增 `g5_dual_run_reports` 审计表及 `wps_0023_040_g5_dual_run_reports` 迁移；`scripts/g5_dual_run_report.py --db-url ...` 可按报告哈希幂等归档，切换门禁自动读取组合最新归档。独立 SQLite 迁移已升级至 `wps_0023_040`。
- G5 准入现在要求不可变来源证明：`capture_mode=real_dry_run_export`、导出时间、交易日历、来源清单 SHA-256、完整交易日清单，以及每个交易日旧链 `legacy_dry_run` / 新链 `unified_dry_run` 的 `source_run_id + data_cutoff_at`。缺失任一项会在归档时强制写入 `g5_eligible_for_g6=false`，不能靠外部字典或脚本内存结果绕过。
- `can_switch_to_member_source()` 对外部传入的未证明 G5 字典 fail-closed；带 `--db-url` 的报告脚本以最终审计记录作为输出和退出码依据，来源校验失败不会返回成功。
- 本轮 G5/G6/G7 定向回归 `51 passed`，`compileall` 与 `git diff --check` 通过。旧 `tests/test_whitebox_auto_trade.py` 已按新契约收敛为 `40 passed, 1 xfailed`：买入夹具提升到当前 SignalRule 可执行分数，自动交易 endpoint 增加非模拟/禁用组合的显式 4xx 前置校验；唯一 xfail 是未创建 `save_and_apply` 快照的旧归因测试，当前自动模拟按设计 fail-closed，快照归因由 `tests/test_auto_simulation_decision_plan.py` 覆盖。
- G6 前置已补齐：统一自动模拟执行器现在调用组合治理门禁，`ADMIN_PAUSED`、`RECONCILIATION_BLOCKED`、`DATA_INCOMPLETE_PAUSED` 下的新 BUY 会生成带 `DecisionEvidence` 外键的拒绝订单，不写成交/持仓；已有部分成交重试不被误判为新买单。
- 新增只读 `GET /api/v1/portfolios/{portfolio_id}/auto-trade/g6-readiness`，同时检查组合为 `READY`、决策日已完成对账和最新 G5 归档通过；不执行任何切换或下单。

### G7 运维状态聚合（2026-08-22）

- 新增 `GET /api/v1/portfolios/{portfolio_id}/auto-trade/g7-operational-status`，只读聚合 G6 准入结果、组合当前状态、部分/待确认订单和组合未确认 ACTIVE 告警。
- 响应明确区分 `ready_for_expansion`（可扩大范围）、`new_buys_stopped`（当前状态已由下单门禁停止新买）和 `can_resume`（人工暂停解除前置条件满足），避免把缺 G5 报告误报为已停止交易。
- `ADMIN_PAUSED`、`RECONCILIATION_BLOCKED`、`DATA_INCOMPLETE_PAUSED` 视为新买阻断状态；部分成交重试仍按待处理订单计数，不改变既有幂等执行语义。
- 新增 `tests/test_g7_operational_readiness.py`，覆盖缺 G5、人工暂停、待处理订单、未确认告警和路由响应契约；当前定向测试 `5 passed`。

### Stage 5 对账阻断收敛（2026-08-22）

- 修复 `reconcile_trade_date()`：出现 `UNFILLED_PLAN`、`POSITION_MISMATCH`、`NAV_BROKEN` 或其他实质差异时，在同一事务内将组合状态写为 `RECONCILIATION_BLOCKED`，审计事件同时记录状态前后值。
- `NOT_CHECKED` 仍只表示历史执行链不可重建，不会单独触发暂停；人工暂停 `ADMIN_PAUSED` 不会被对账结果覆盖。
- G7 运维状态的 `new_buys_stopped` 现在复用统一 `order_entry_gate_check()`，可识别 `SCORE_STALE`、`MODEL_INACTIVE` 等四维组合状态，而不只读取旧的组合状态列。
- 新增覆盖部分成交自动阻断和 Score 过期买入门禁的测试；定向验证 `tests/test_g7_operational_readiness.py` 为 `7 passed`，Stage 5 对账桥接与 G3 状态机回归为 `8 passed`。

### Stage 5 任务投递与恢复可靠性（2026-08-22）

- `create_async_task()` 对可识别业务上下文生成的 `idempotency_key` 先查询后复用；同一进程使用创建锁保护 SQLite/非唯一索引场景，数据库唯一键竞争捕获 `IntegrityError` 后回读原任务，兼容 MySQL 并发插入。
- 重复投递不区分任务是否已完成：`queued/running/done/failed/cancelled` 均返回原任务快照；需要强制重跑时必须由上层显式产生新的业务幂等键。
- `resume_task()` 改为带状态和终态锁条件的原子 `UPDATE`，只有唯一成功领取 `interrupted/stalled -> running` 的请求会启动 worker；重复恢复、已取消任务均不会再次启动。
- `batch_recovery_json` 在唯一领取成功后传入 worker，恢复请求失败或竞争失败不会清空断点信息，也不会产生半恢复状态。
- 定向验证：`tests/test_stage5_task_delivery_contract.py` 5 项通过；与既有 `tests/test_whitebox_task_state_machine.py` 合计 30 项通过，包含 worker 崩溃后 `failed + terminal lock` 且不可恢复。

### Stage 5 迁移与五维守恒实库验证（2026-08-22）

- 已在不覆盖原件的前提下，将旧 `test_output/g3_fixture_probe_20260822.db` 复制为隔离验证库并执行 `alembic upgrade head`；迁移链成功到 `wps_0023_040_g5_dual_run_reports`，迁移后的可复现性报告可正常读取旧库回测。
- 该旧 probe 是早期 UI 样本，只有聚合 `backtest_trades`，缺少 `backtest_execution_fills` 和估值快照；五维报告因此返回 `BLOCKED/UNFILLED_PLAN`。这属于历史账本不可重建的真实结果，不能作为 G3 当前夹具或生产通过证明。
- 通过 `scripts/seed_ui_acceptance.py` 新生成 `test_output/g3_fixture_current_20260822.db`：3 个可复现回测、126 笔聚合流水、168 条 immutable fills、54 条拒绝/数据阻断证据和 1 条部分成交。
- 对新夹具运行 `scripts/backtest_reproducibility_report.py` 得到 `reproducible=3`；首个运行执行 `scripts/backtest_conservation_report.py --run-id 1`，56 条 fills 跨 19 个交易日的 `DecisionEvidence -> OrderPlan -> Fill -> Position -> Cash` 报告全部 `PASSED`。
- 迁移链白盒契约同步更新至 G5 head `wps_0023_040_g5_dual_run_reports`；G6 审计约束追加迁移 `wps_0023_041_g6_rollout_audit_actions`。
## G6 单组合灰度动作（2026-08-22）

- 新增 `start_g6_rollout()` / `rollback_g6_rollout()` 服务：先执行 READY、每日对账和真实 G5 报告门禁，再在同一事务内更新 `Portfolio.auto_trade_source_mode` 并写入前后快照审计。
- 新增接口：
  - `POST /api/v1/portfolios/{portfolio_id}/auto-trade/g6-start`
  - `POST /api/v1/portfolios/{portfolio_id}/auto-trade/g6-rollback`
- 旧接口 `rollback-to-old-source` 已接入同一持久化回滚路径；环境变量仅保留为紧急熔断兼容机制。
- G6 审计动作：`G6_ROLLOUT_STARTED`、`G6_ROLLOUT_ROLLED_BACK`，迁移 `wps_0023_041_g6_rollout_audit_actions` 同步更新 SQLite/MySQL 约束。
- 重复启动/重复回滚幂等，不重复写切换审计；审计或数据库异常由调用方回滚，组合来源模式不产生半写。

验证命令：

```text
python -m pytest -q tests/test_g6_rollout_actions.py tests/test_g6_graduated_rollout.py tests/test_g7_operational_readiness.py
13 passed
python -m compileall -q app/services/g6_graduated_rollout.py app/api/routes/auto_trade.py app/services/data_governance_audit.py
```

## G7 运维演练入口（2026-08-22）

- 治理页“运行保障”新增 G6 灰度启动/回滚按钮，按钮状态由 `ready_for_expansion` 和请求进行中状态控制；回滚需二次确认，完成后自动刷新 G7 状态。
- 前端 API client 已接入 `g6-start` / `g6-rollback`，返回状态、来源模式前后值、G5 报告 ID 和审计 ID，避免页面只显示无依据的成功提示。
- 中英文文案已补齐，G7 操作页不直接展示 `members_only`、`legacy_scan` 等内部常量。

验证命令：

```text
frontend: vitest run src/components/portfolio-trading/__tests__/PortfolioGovernanceTab.g7.test.tsx
1 passed
frontend: vite build
5611 modules transformed; built successfully
```

补充验证：回测交易接口精确关联买卖两侧 Evidence 时，`intended_entry_price` 与 `intended_exit_price` 均来自对应订单计划；`tests/test_backtest_execution_linkage_api.py`、`tests/test_decision_order_plan_ledger.py` 共 `8 passed`。

### 固定夹具只读报告复核

- `test_output/g3_fixture_current_20260822.db` 当前包含 3 个回测运行，`backtest_reproducibility_report.py` 报告为 `reproducible=3`。
- `backtest_conservation_report.py --run-id 1` 读取 56 条 immutable fills，覆盖 19 个交易日；每日 `DecisionEvidence -> OrderPlan -> Fill -> Position -> Cash` 五维报告均为 `PASSED`，总状态为 `PASSED`。
- 报告脚本只读，不修改运行状态，也不为历史数据猜测性生成 Evidence 或成交记录。

### G5/G6/G7 自动化门禁回归复核

- G5 双跑报告归档与准入、G6 灰度启动/回滚路由、G7 运维状态聚合及 Stage 5 任务投递恢复契约已重新执行回归。
- 本轮定向测试共 `20 passed`：`test_g5_dual_run_audit.py`、`test_g5_dual_run_report.py`、`test_g6_rollout_actions.py`、`test_g6_route_contract.py`、`test_g7_operational_readiness.py`、`test_stage5_task_delivery_contract.py`。
- 真实 G5 10 个交易日、G6 完整再平衡周期和 G7 生产运维演练仍需在具备真实数据与操作权限的环境执行；本地夹具和自动化测试不替代正式签署。

### G3 重试结果（授权运行）

- 已确认隔离后端 `127.0.0.1:18000` 与 G3 前端 `127.0.0.1:5174` 均可访问，并按验收文档加载 `.pip_packages` 中的 Playwright 依赖。
- G3 矩阵仍为 11 项 `SKIP`，唯一原因是当前执行环境创建 Chromium 子进程返回 `spawn EPERM`；不是业务断言失败，也不是服务或 Playwright 依赖缺失。
- 已尝试申请沙箱外执行以启动 Chromium，但平台自动审批服务拒绝该请求，因此未伪造 BT-UI 通过结果、HAR 或截图。
- G5/G6/G7 自动化回归重新执行共 `31 passed`，覆盖真实双跑准入校验、报告审计、G6 灰度动作和 G7 运维状态契约。

### 治理诊断入口回归

- 今日决策页的“前往治理诊断”现在同时切换顶层 `portfolio` 和组合工作台 `governance` 子 Tab，避免只切换顶层页面而停留在旧子 Tab。
- 新增回归测试验证非 `READY` 状态下按钮同时调用两个导航层级。

验证：`TodayDecision.repair.test.tsx` 共 `12 passed`。

接口与迁移链补充验收：

```text
python -m pytest -q tests/test_migration_alembic_chain.py tests/test_g6_route_contract.py tests/test_g6_rollout_actions.py tests/test_g7_operational_readiness.py
38 passed
```

当前迁移 head 为 `wps_0023_041_g6_rollout_audit_actions`，迁移链验收断言已同步更新。

## Stage 1 独立订单计划账本（2026-08-22）

- 新增 `decision_order_plans` 表，保存不可变订单意图：`decision_run_id`、`evidence_id`、证券、动作、信号日、执行日、数量、方向、意向价、拒绝原因和拒绝轨迹。
- DecisionEngine 持久化 Evidence 时在同一事务写入计划；`evidence_id` 唯一约束保证一条证据恰对应一条计划。
- 旧消费者继续读取 `DecisionEvidence.versions_json`；新对账/治理查询可直接使用独立表，不重复保存成交状态。
- 同一确定性决策重跑会幂等复用 DecisionRun，并仅补齐缺失的订单计划行；不会对无法重建的历史运行猜测性回填。
- 迁移：`wps_0023_042_decision_order_plans`，当前 head 已更新为该 revision。

验证：

```text
python -m pytest -q tests/test_migration_alembic_chain.py tests/test_decision_order_plan_ledger.py tests/test_stage1_cross_entry_consistency.py tests/test_stage1_failure_idempotency.py tests/test_backtest_decision_chain_integration.py tests/test_backtest_partial_fill_lifecycle.py
39 passed
```

### 订单计划查询链路补充

- 新增 `GET /api/v1/decision-runs/{run_id}/order-plans`，支持动作过滤、分页，并直接读取 `decision_order_plans` 关系表。
- 前端 `api.listDecisionRunOrderPlans()` 和 `DecisionOrderPlanRead` 类型已接入，后续证据抽屉/对账页可使用精确计划外键，不必解析 Evidence JSON。
- 接口返回的 `order_plan_id`、`evidence_id` 与持久化决策结果逐条一致。

验证：`tests/test_decision_order_plan_ledger.py`、`tests/test_backtest_decision_chain_integration.py` 共 `6 passed`；TypeScript 编译通过。

### 回测交易消费订单计划表

- `GET /backtest/runs/{run_id}/trades` 现在按精确 `decision_evidence_id/exit_evidence_id` 查询 `decision_order_plans`，优先返回独立表的计划数量、意向价和方向字段。
- 旧回测或迁移前 Evidence 没有关系表记录时，继续回退 `versions_json` 的成交状态和数量字段，保持历史页面可读。
- 回测接口回归已验证关系表计划与成交两条腿的 Evidence 外键不串线。

验证：`tests/test_backtest_execution_linkage_api.py`、`tests/test_decision_order_plan_ledger.py` 共 `8 passed`。

### 回测交易详情展示订单计划意向价

- 回测中心交易流水新增“计划价”列；买入交易显示独立 `DecisionOrderPlan.intended_price`，并通过悬浮提示标明来源。
- 已通过 `exit_evidence_id -> decision_order_plans` 关系投影 `intended_exit_price`，无需新增数据库列；买卖两侧均可展示独立订单计划意向价。
- 空状态和加载状态的表格列数同步调整，前端测试覆盖计划价渲染。

验证：

```text
frontend: vitest run src/components/portfolio-trading/__tests__/PortfolioBacktestCenter.test.tsx
12 passed
frontend: tsc -b
通过
frontend: vite build
5611 modules transformed; built successfully
```
