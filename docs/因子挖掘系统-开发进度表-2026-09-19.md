# 因子挖掘系统 · 开发进度表

> 配套文档：《因子挖掘系统-开发任务计划-2026-09-19.md》（卡的定义/DoD/not_do 以计划为准）
> 创建日期：2026-09-19 ｜ 状态词汇：⬜ 未开始 / 🔄 进行中 / ✅ 完成 / ⏸️ 暂停（三次失败止损）
> 更新约定：每卡收工时更新状态 + 填写 DoD 证据（测试命令 / exit code / 文件路径）；**不排预估日期**，按依赖顺序推进，完成一张更新一张。

---

## 一、总览仪表

| 阶段 | 卡数 | ⬜ 未开始 | 🔄 进行中 | ✅ 完成 | 预算 |
|---|---|---|---|---|---|
| 阶段 0 · 固化 | 1 | 0 | 0 | 1 | S |
| 阶段 A · 真实闭环（P0） | 5 | 0 | 0 | 5 | L+M+M+M+M |
| 阶段 B · M2 落库（P1） | 4 | 0 | 0 | 4 | S+M+M+L |
| 阶段 C · 体验一致性（P2） | 5 | 0 | 0 | 5 | S+M+S+M+S |
| **合计** | **15** | **0** | **0** | **15** | — |

**当前基线**：后端 1134 passed / 0 failed（9 warnings 均为 Python3.12 sqlite3/Deprecation 噪音，SAWarning 已清零）｜ 前端 837 passed / 0 failed（Node 24 实测 exit 0，TD-FE-RED-2 关闭）｜ 排期 47/47 done｜ **验收：通过（翻案复核已写入功能验收报告）**

---

## 二、主进度表

### 阶段 0 · 固化

| 卡 | 内容 | 预算 | 依赖 | 状态 | 完成日期 | DoD 证据 |
|---|---|---|---|---|---|---|
| D0 | git 提交固化（含仓库重建：原 .git 对象库损坏，已换新库并把全部工作区固化为新基线） | S | 无 | ✅ | 2026-09-19 | 基线 c902834→68041ee（1408 纯代码文件，已排除依赖/会话/数据库）；已 force push 至 origin/main_work_v0.8（fa07912→68041ee） |

### 阶段 A · 打通真实挖掘闭环（P0 关键路径）

| 卡 | 内容 | 预算 | 依赖 | 状态 | 完成日期 | DoD 证据 |
|---|---|---|---|---|---|---|
| A1 | 实现真实适应度 `evaluate_short`（FactorExecutor + train 段守卫 + G2 缓存 + 探针落库） | L | D0 | ✅ | 2026-09-20 | `pytest tests/services/factors/mining/test_evaluate_short.py -q` → 4 passed；全量 `tests/services/factors/mining` → 1094 passed（0 failed）。实现：`evaluation_adapter.evaluate_short`/`evaluate_short_probed`（真实 executor + train 段 + G2 缓存 + 抽样校验 + 探针）；`contracts.MiningContext` 增 3 个可选字段；修正 `_as_date` 对 pd.Timestamp 的子类判定 Bug |
| A2 | GA 阶段链接入 task_runner（initial_population → run_ga_loop → finalize_run，接入 AI 约束） | M | A1 | ✅ | 2026-09-20 | `pytest tests/services/factors/mining/test_stage_runner_real.py tests/services/factors/mining/test_task_runner.py -q` → 16 passed；全量 → 1096 passed（0 failed）。实现：`_default_stage_runner`=真实阶段链（target 生成 → 经典底座 → run_ga_loop 真实 evaluate_short → 每代 persist/探针落库 → finalize 可跳过）；`skeleton_stage_runner` 供基础设施测试；`persist_candidates` 幂等改 run 级（对齐 (run_id, formula_hash) 唯一约束）；`_release_all_locks` 透传 runner 唤醒队首 |
| A3 | 清零 factor_mining.py 19 处路由占位（runs/结果/evaluations/AI/分级族） | M | A2 | ✅ | 2026-09-20 | `pytest tests/services/factors/mining/test_factor_mining_routes.py -q` → 11 passed；全量 → 1107 passed（0 failed）。实现：19 处 `NotImplementedError` 归零（22 路由全注册）；`service.py` 增 runs/结果/evaluations/AI/模板/分级查询操作族（含 build_mining_context/evaluate_all_impl）；POST /runs 走真实 `submit_mining_run`（双锁）；分级族在候选未提交时结构化 409（CANDIDATE_NOT_SUBMITTED） |
| A4 | 前端提交接线（Settings→MiningShell onSubmitted；Step4 提交调用；Step5/结果页真实数据） | M | A3 | ✅ | 2026-09-20 | `npx vitest run src/components/factors/mining/wizard/MiningWizardFlow.test.tsx` → 9 passed（含 A4 新增 3 条提交回调断言）；全量 → 810 passed / 0 failed / 5 skipped。实现：`MiningShell` wizardConfig 状态上提（step1 onSnapshot → 快照 id/数据截止日；step2 onChange → 起止/频率/horizon/train+val 比例；step3 onChange → selected_fields；step4 onConfig → evolution_params）；handleSubmit 组装 payload 调真实 `POST /runs`，成功后流转第 5 步 + 5s 轮询 `GET /runs/{id}`；step5 三操作接真实接口；新增 i18n miningSubmitPending/miningSubmitError（zh/en 成对） |
| A5 | **门禁 G8**：真实挖掘闭环 E2E（提交→20 代真实 GA→候选入库→结果页→F1 沉淀→G2/探针落库） | M | A1~A4 | ✅ | 2026-09-20 | `.venv/Scripts/python.exe -m pytest tests/services/factors/mining/test_m1_closure.py -q` → 3 passed；全量 → 1110 passed（0 failed）。修复路由断点：POST /runs 提交前补建 `factor_mining_runs` 行（此前缺行 → 真实 worker 「run 不存在」）；AI 骨架接入初始种群（ai_enabled 显式开时尽力填充剩余名额，失败不阻断，关闭/缺省不调用）；前端 Step5 结果区接真实 candidates（#20）；关闭验收报告 P0-1~4 + §14/15/16/17/18/20。E2E 证词：HTTP POST → 真实 worker → 20 代真实 evaluate_short → 代际/候选/探针（G2 命中率+cache_validation）落库；人工核验可通过批次列表/结果区直接看到候选 |

### 阶段 B · M2 特性落库（P1）

| 卡 | 内容 | 预算 | 依赖 | 状态 | 完成日期 | DoD 证据 |
|---|---|---|---|---|---|---|
| B1 | `factor_versions` 分级 4 列迁移（quality_grade 等；InnoDB + verify_schema_drift） | S | A3 | ✅ | 2026-09-20 | `pytest tests/services/factors/mining/test_grade_columns_b1.py -q` → 5 passed（迁移升/降级 roundtrip + verify_schema_drift factor_versions drift=0 + persist_grade_to_version/apply_manual_grade/restore-auto/季度重评同步落库）。实现：`factor_model.FactorVersion` + 4 列（nullable 无 server_default 无索引）；迁移 `0061_wps_0023_060_factor_version_grade_columns`（幂等逐列/逐索引，并补 historic 缺失的 `ix_factor_versions_effective_from` 使纯迁移链 drift=0）；`service.persist_grade_to_version`（grade() 结果可落库，老库列缺失时静默跳过）；顺带修 2 处 latent：`apply_manual_grade` 的 `metrics_json/trigger` 错误字段名 → `metrics_snapshot_json/source="manual"`，`set_manual_grade_auto` 的 FactorVersion 导入错模块（曾在 A3 仅测 409/422 路径未被暴露） |
| B2 | 统计层写库（metrics_json.stats 8 方法）+ 分级消费真实数据（D 级写 F1 负样本/移出 FactorSet） | M | A3、B1 | ✅ | 2026-09-20 | `pytest tests/services/factors/mining/test_stats_grading_b2.py -q` → 6 passed；相关批（wp5/mining_e2e/grading/final_eval/grade_columns_b1）→ 101 passed（wp5 6 条 `insufficient_dates` 为**既有失败**，stash 验证与 B2 无关、不在挖掘域门禁内）。实现：`run_evaluation` 加可选 `stats_provider` 钩子（缺省零行为变化）→ 挖掘域注入 `_stats_provider_for`（compute_stats 8 方法 + icir_raw/icir_adjusted，train_icir 取候选 generation_icir、test 取验证段 ICIR、total_trials 取 run 行）；`finalize_run` 循环内 `_grade_and_govern_outcome`（grade()→persist_grade_to_version 落库 + D 级 `_remove_factor_from_all_sets` 移出 FactorSet）；季度重评改读 `factor_versions.grade_manual_adjusted` 真实列。**决策记录：D 级写 F1 负样本留 B3**（与正向样本共用 formula→AST + C5 网关机制，本卡完成 stats 写库 + 分级落库 + FactorSet 移出） |
| B3 | F1 内生链路（submit/finalize 自动沉淀）+ list/get/archive 服务与路由 + 前端经验页挂载 | M | A2 | ✅ | 2026-09-20 | `pytest tests/services/factors/mining/test_experience_b3.py -q` → 5 passed（submit 正样本沉淀 + 三层指纹去重 + D 级负样本 + list/get/archive service 与 HTTP 契约）；前端 `vitest experience/F1ExperiencePage.test.tsx` → 3 passed。实现：`experience.service` 新增 `list_experiences/get_experience/archive_experience`（`_experience_dict` 序列化）；路由 `GET /factor-experience`、`GET /{id}`、`POST /{id}/archive`；mining 内生链路 `_store_f1_experience`（唯一入口，AST 取自编译后的 `formula_ast_json`，类别白名单守卫，三次方指纹去重/负样本豁免语义走 F1 服务层；submit 正样本 + 分级 D 级负样本接线）；前端 `factorExperienceApi` + `F1ExperiencePage` 挂到 MiningShell 第三子页签「经验库」（active 门控，三态容错）。**红线 C5 决策记录**：挖掘 worker 与 API 同进程，HTTP 自调不可靠 → mining 仅经 `_store_f1_experience` 单点写 F1，不直写表，对外契约仍以 HTTP 面为准 |
| B4 | drafts（含 prepare）/ templates（25 模板入表 + CRUD）/ cleanup（7 天保留）路由族 | L | A3 | ✅ | 2026-09-20 | `pytest tests/services/factors/mining/test_templates_cleanup_b4.py -q` → 8 passed。实现：草稿路由族（GET/POST /factor-mining/drafts、GET /{id}、POST /{id}/prepare 最终准备校验=Step1~4+快照+字段校验有效、POST /{id}/status，多份并存复用 draft_service）；模板服务 `template_service`（`seed_system_templates` 25 经典模板幂等入 `factor_mining_templates`+版本、CRUD/复制/启停）+ 路由族 + 种子脚本 `scripts/seed_mining_templates.py`；清理 `cleanup_run_data`（删候选/代际/检查点+run，幂等 404）与 `purge_stale_mining_drafts`（7 天保留，只清 draft/waiting_data_recheck 超期草稿）+ `POST /runs/{id}/cleanup`、`POST /cleanup/stale` 路由 |

### 阶段 C · 体验与一致性（P2）

| 卡 | 内容 | 预算 | 依赖 | 状态 | 完成日期 | DoD 证据 |
|---|---|---|---|---|---|---|
| C1 | 结果页 → 因子模型页 4 上下文跳转按钮 | S | A5 | ✅ | 2026-09-20 | `vitest result/ResultGotoFactorModelButton.test.tsx` → 3 passed（4 上下文齐备可点→跳转 / 缺 factor_set_id 禁用不跳 / 无上下文禁用）；向导+结果回归 13 passed。实现：`ResultGotoFactorModelButton`（contextComplete 门控 + 复用 `utils/navigate` 派发 `settings:navigate` detail=factor-center），挂到 MiningShell Step5 结果区表头（context 取 run：snapshot/cutoff/frequency + 入组后注入的 factor_set_id）；i18n miningResultGotoDisabledHint 成对 |
| C2 | 前端缺壳页面：数据镜像管理页、因子模板配置页、「因子基础配置」设置栏 | M | A3 | ✅ | 2026-09-20 | 子代理实现并全量验证：`vitest` 全量 → 829 passed / 0 failed / 5 skipped（+12 新用例，skipped 不变）；3 新增 12 例复核通过。新增：`api/dataMirror.ts` + `config/DataMirrorPage`（挂 DataCenter mirror 子页签）、`api/factorTemplates.ts`（list/seed/copy/setEnabled）+ `config/TemplateConfigPage`（挂 MiningShell 第四子页签「模板配置」，active 门控）、`settings/FactorBasicSettings`（Settings 新 section `factor-basic` + SettingsNav 导航项）；i18n 31 key 成对 |
| C3 | 文档一致性：snapshot 命名回填、prev_close 口径统一、monthly=41 有据偏离确认 | S | 无 | ✅ | 2026-09-20 | 裁决记录（C3-1~3）已追加 `docs/因子挖掘-决策记录-v3.md` 第六节：snapshot 三件套以代码为准并不加兼容别名；prev_close 保留执行器投影虚字段/不注册（D-D 撤销维持）；`SPLIT_MINIMUMS.monthly=41` 确认有据偏离（>=36 满足下限、不回退）。向导 §3.7 已回填「创建/查看/重选快照」措辞 |
| C4 | 前端测试重写收尾（TD-FE-RED-2 余 5 项：FactorModelSettings 3 + FactorEvaluationLab 2） | M | 无 | ✅ | 2026-09-20 | 子代理改写 5 项 skip 为真实断言并全量验证：**834 passed / 0 skipped / 0 failed**（基线 829/5 → +5 全转绿）。要点：FactorModelSettings 3 项换喂 `scoring*` 真实接口 mock（shadow 激活/流水线启动/启用开关/仓库初始化断言）；FactorEvaluationLab 2 项（嵌套指标拍平断言、run 自动选中改为 done 态直返避免轮询挂死）。另清掉 `F1ExperiencePage.test.tsx` 既有类型错误（vi.mocked）。全量 tsc 剩余 5 条均为既有 FactorModelPage 测试债（超本卡范围） |
| C5 | task_lock 并发测试 9 条 SAWarning 清理 | S | 无 | ✅ | 2026-09-20 | 子代理修复：`test_task_lock_concurrency.py` 增 `_acquire_write_slot`（expunge_all 后走 `TL.acquire_write_slot`），5 个排队用例消除同 session 重复身份键冲突；`-W error::sqlalchemy.exc.SAWarning` 下 concurrency+runner 两文件 **35 passed 无 SAWarning**；抽样 grade_columns_b1 5 passed 无回归。业务零改动 |

> **C4 既有进展备注**：TD-FE-RED-2 原始 22 项已真实修复 **17 项**（批次 1~4），余 5 项 skip + TODO；
> 每项的「下一步从哪查」已留痕于 `PROGRESS.debt.TD-FE-RED-2.rewrite_progress`（batch1~batch7）。

---

## 三、门禁进度

| 门禁 | 达成判定 | 依赖卡 | 状态 |
|---|---|---|---|
| **G8 · M1b 闭环达成** | A5 DoD 全过；真实挖掘一次跑通 | A1~A4 | ✅ |
| **G9 · M2 落库达成** | B1~B3 DoD 全过；分级/统计/F1 消费真实数据 | B1~B3 | ✅ |
| **G10 · 体验补齐** | B4 + C1~C3 + C5 DoD 全过 | B4、C1~C3、C5 | ✅ |
| **最终验收翻案** | 重跑《功能验收报告》：M1 ✅、M2 ✅、M3 按 G1 决策 | G8、G9、G10 | ✅ |
| **真实浏览器交互验收修复（2026-09-21）** | 《前端交互验收报告-2026-09-20》4 P0 + P1-5~P1-9 + P1-1~P1-4 全部关闭 | 最终验收翻案 | ✅ |

---

## 四、技术债联动

| 技术债 | 当前状态 | 关闭条件 | 联动卡 |
|---|---|---|---|
| TD-FE-RED-2（C 类前端红 22 项） | mitigated (plan B)，17/22 已修 | C4 完成（5 项重写 + skip 归零） | C4 |
| TD-FE-RED（A 类 api mock 未同步） | partially_fixed，A 类已清零 | TD-FE-RED-2 关闭后一并关闭 | C4 |
| FK30/CHECK19 补齐（P1）+ usage 双轨整合（P3） | 未开始（历史遗留，未进排期） | 待排期 | — |
| build_time_split min_val_days=50 小样本冲突 | 待裁决 | C3 一并裁决记录 | C3 |

---

## 四.5、2026-09-21 验收修复批次（《前端交互验收报告-2026-09-20》）

> 背景：真实浏览器验收发现「前端测试全绿但交互不可用」，向导第 1 步即断（Step1 生成物料 405、
> Step3 无字段可选、Step4 提交无请求、预览统计恒空）。本次批次修复 4 P0 + 9 P1 后，**向导 5 步全链路真实可走通**。

| 项 | 内容 | 证据 / 实现 |
|---|---|---|
| **P0-1** | Step1 建池接线：`onGenerateClick` 无池时先 `createPoolFromFilter`（命中<50/覆盖不足 → 4xx 结构化阻断展示）再 `createSnapshot(analyze=true)`；MiningShell 持有 poolId 下发，消除 `/candidate-pools//snapshot` 405 | `MiningPoolStep.tsx` / `MiningShell.tsx`；`MiningPoolStep.test.tsx` 12 passed |
| **P0-2** | Step3 接真实 `filter-fields`：MiningShell 进入 Step2 拉取字段目录映射为 `MiningField[]` 注入，MiningFieldStep 分组兼容筛选分类 | `MiningShell.tsx` / `MiningFieldStep.tsx` / `fieldTypes.ts` |
| **P0-3** | Step4 提交闭环：弹窗**先关闭再提交**；缺快照时明确提示 `miningSubmitNeedSnapshot` 并跳回 Step1；错误可关闭（`data-mining-submit-dismiss`） | `MiningShell.tsx` / `ResourceConfirmModal.tsx`；`mining.css` 新增 dismiss 样式 |
| **P0-4** | PoolPreview 契约对齐：前端 `matched/total/excluded` → 后端 `universe_size/hits/excluded_total/min_pool_size/can_generate`；blocked 判定改用 `can_generate` + hits<50 兜底；阻断展示 `firstBlocker.detail_zh` | `poolApi.ts` / `MiningPoolStep.tsx` |
| **P1-5** | tsc 全绿：FactorModelPage 两测试 mock 改 `vi.fn<any>`（消除 `never[]` 严格类型）；evoTypes 模块路径少一级 `../` 修复；Step1 `detail_zh` 窄化 | `npx tsc --noEmit` exit 0 |
| **P1-6** | 导入股票池接真实接口：模板下载（GET import-template）、预览逐行结果（POST import-preview）、确认入池（POST /{pool_id}/import，建池 source_type=import 需带 import_batch_id）、错误明细导出（POST import-errors）；PoolImportPanel 重写渲染统计+逐行表+错误下载 | `poolApi.ts` / `PoolImportPanel.tsx` / `MiningPoolStep.tsx`；10 个 `miningPoolImport*` i18n key 成对 |
| **P1-7** | Step4 高级参数 onChange 接入 `evolution_params`：ClassicalBase/Exploration/Selection/Reproduction 四折叠区 patch 汇总上报；EvoConfig 扩展高级字段；类别名不再直显英文 key（6 个 `miningEvoCat*` i18n） | `MiningEvoParamStep.tsx` / `evoTypes.ts` / `ClassicalBaseConfig.tsx` |
| **P1-8** | 双锁/ETA/资源卡片接线 + **契约修正**：后端 `GET /locks/status` 返回 **camelCase**（`miningDomain/duckdbWrite/taskId/queue`），前端原用 snake_case 全错 → types/mining.ts 对齐 + ResourceConfirmModal 读取修正 + 测试 fixtures 更新 | `types/mining.ts` / `evoTypes.ts` / `ResourceConfirmModal.tsx` / `MiningEvoParamStep.test.tsx` 14 passed |
| **P1-9** | Step2 预算/镜像引导接线：**新增后端 `POST /factor-mining/split-budget`**（数仓交易日 → 频率重采样调仓点 → `compute_split_budget` 纯函数；`available=False` 降级）；MiningShell 拉 budget + `data-mirror/status` 镜像区间传入；`onGoMirror` 跳转数据中心·数据镜像 | `service.compute_split_budget_preview` / `factor_mining.py` / `MiningShell.tsx` |
| **P1-1** | `portfolioId ?? 1` 在 id=0 时保留 0（无效）→ 改 `>0` 判定兜底 1（TodayDecision / PortfolioWorkbench） | 两组件同修 |
| **P1-2** | 数据镜像页 `available:false` 时不再直甩英文底层错误：中文兜底文案 + 错误详情 `<details>` 折叠 + **禁用「创建镜像任务」** | `DataMirrorPage.tsx`；`dataMirrorUnavailable*` i18n 成对；测试 +1（5 passed） |
| **P1-3** | 回测预检无限「预检中」：`/backtest/precheck` 请求加 **15s 超时**（AbortController），超时中止并落失败态（`PRECHECK_500` + toast），不再无限转圈 | `PortfolioBacktestCenter.tsx`；测试 16 passed |
| **P1-4** | custom-indicators 列表被测试脏数据击穿 500（`__p22_smoke_*` 不合规 key 触发 ResponseValidationError）→ 列表接口按 `^[a-zA-Z][a-zA-Z0-9_]*$` **容错过滤脏行**，单行脏数据不再让整页 500 | `custom_indicators.py` |
| **P2-2** | Step3 无字段时渲染「暂无可用字段目录」空态提示（`data-field-empty` + `miningFieldEmpty` i18n 成对；空态下开始校验按钮禁用） | `MiningFieldStep.tsx`；测试 +1（10 passed） |
| **P2-3** | antd 废弃 API 清理：`destroyOnClose→destroyOnHidden`（FactorModelSettings/FactorEvaluationLab×2/FactorLibrary/FactorModelPage×3/ThemeManagementModal）、`Descriptions labelStyle/contentStyle→styles`（5 处）、`Spin tip` 独立用法改嵌套模式（FactorDetail）、`Popover overlayInnerStyle→styles.body`。静态 `message.*`（60+ 文件）不动：AppContext.showToast 已收敛到 `App.useApp()` messageApi，静态调用 antd v5 仅告警不破坏功能，SK setup.ts 已抑制测试噪声 | 机械替换 7 文件；相关测试全绿 |

| **P2-4** | **Step1 条件筛选完整分层布局**：左栏「常用区间预设」（4 组 Tab：市值/估值/流动性/上市时间，服务端分位边界一键应用，blocked 预设禁点+原因提示）+ 右栏「分类条件 Accordion」（7 分类；数值字段 min/max 双输入+缺失处理、市场/板块多选、ST/退市布尔；blocked 字段置灰+阻断原因）。修正旧面板写顶层 `min_market_cap` 而后端不消费的交互断裂根因（现按契约写入 `valuation.total_market_cap` 等槽位） | `poolApi.ts`（FilterField/FilterPreset 强类型）、`PoolFilterPanel.tsx` 重写；i18n 20 key 成对；`MiningPoolStep.test.tsx` +4（双栏/预设落库/布局）→ **18 passed** |
| **§8.3.6** | **性能探针前端调试面板**：`?debug=perf` 开启（Step5 资源区下方，details 折叠）；内容=①最近一代六阶段耗时条形图（data_load/ast_eval/subexpr_compute/factor_assemble/metric_calc/db_write）②G2 命中率折线（随代数）③子表达式总数/去重唯一数 ④每代缓存校验 ✅/❌（passed=0 行标红 + 警示文案）。后端 `GET /runs/{id}/generations` 已返回探针全列，前端每 5s 轮询 | `PerfProbePanel.tsx`（新）+ `runTypes.ts`（PerfProbeGeneration）+ `MiningShell` 接线 + `factorMiningApi.listGenerations` 对齐数组契约；i18n 17 key 成对；`PerfProbePanel.test.tsx` 4 passed；tsc 全绿 |
| **B2-ui** | **Step1 预设「自定义」标记**（设计 §3.2）：应用预设 → 按钮 `type=primary` 高亮 + 绿 Tag「已应用」；用户手工改动该字段 min/max/缺失处理 → 按钮复位为默认 + 橙 Tag「自定义」（`data-filter-preset-state=applied/custom`），Tooltip 提示「值被手工修改」；清除分类同步复位该分类字段的预设标记 | `PoolFilterPanel.tsx` + `appliedPreset`/`customizedPreset` state；i18n `miningPoolPresetApplied/Custom/CustomTip` 成对；`MiningPoolStep.test.tsx` +1（应用→已应用 Tag；改值→自定义 Tag 且已应用消失）→ **15 passed** |
| **S2-ui** | **Step2 控件统一 antd**：起止日期原生 `input[type=date]` → `DatePicker`（格式 YYYY-MM-DD、结束日 `disabledDate` 不得晚于数据截止日）；调仓频率/预测目标原生 `<select>` → `Select`；预测持有期原生 `input[type=number]` → `InputNumber`（1~20）；切分面板三段比例 → `InputNumber`（0~100）、自定义边界日期 → `DatePicker`。`data-time-*`/`data-split-*` 选择器保留供走查/测试 | `MiningTimeTargetStep.tsx` / `MiningSplitPanel.tsx` 重写控件；`dayjs` 显式引入；`MiningSplitPanel.test.tsx` 选择器适配（antd 将 `data-*` 转发至内层 input）→ 全绿；走查脚本 Step2 段改 antd 交互（DatePicker 填值+回车 / Select 开下拉选周频） |
| **S3-ui** | **Step3 控件统一 antd**：字段勾选原生 `<input type=checkbox>` → `Checkbox`（`data-field-checkbox` 转发至内层 input，保留 disabled/checked 语义；blocked 字段仍禁勾选） | `MiningFieldStep.tsx` 控件替换；`MiningFieldStep.test.tsx` 选择器适配 → 全绿；走查脚本 Step3 段直接勾选 input |
| **F2-ui** | **Step5 当前种群 Top 因子（设计 §8.3.3）**：新面板 `data-run-top-list` 按 §8.3.3 示例格式渲染排名/公式/ICIR/覆盖率/来源五列；**新进 Top10 条目高亮闪烁**（`mining-top-flash` 关键帧 ×3 次，首轮仅建基线不闪）；上一代已在该榜者不算新进；「N 个新进 Top10」计数提示 | `FactorMiningRunTrack.tsx`（`TopFactor` 类型 + `topCandidates` prop + `topNew` diff 逻辑）；`MiningShell` 用真实 candidates 按 `generation_rank` 升序取前 10 下发；`mining.css` 新增 flash 动画 + 表格样式；i18n 6 key 成对；`FactorMiningRunTrack.test.tsx` +3 → 全绿 |
| **§8.3.7** | **最终验证面板细化**：进度条（antd `Progress`，percent=done/total）+ 状态文字（验证中…/已完成）+ 完成时提示「最终验证完成，已进入结果总览」 | `FactorMiningRunTrack.tsx` 面板增强（`data-run-final-progress/status/overview-hint`）；i18n `miningRunFinalRunning/Done/Units/ToOverview` 成对；`FactorMiningRunTrack.test.tsx` +1 → 全绿 |

**回归（最终）**：前端全量 vitest **846 passed / 0 failed**（82 文件，含探针面板 +4）；后端挖掘域全量 **1200 passed / exit 0**；tsc exit 0；i18n translations.test 3 passed。向导 v3.3 全部功能点闭环（4 P0 + 10 P1 + 4 P2 + §8.3.6）。
**后端补充回归（执行轮）**：`test_whitebox_custom_indicators.py` + `test_factor_mining_routes.py` **33 passed**（+5 新用例：P1-4 脏 key 过滤、split-budget 日/周/月重采样 + 数仓空降级）；新增用例捕获并修复 `compute_split_budget_preview._as_date` 的 **datetime/date 比较 bug**（datetime 是 date 子类，先判 datatime 再 date）；M1 闭环 E2E `test_m1_closure.py` 3 passed 无回归。

**回归（遗留项批次：B2-ui/S2-ui/S3-ui/F2-ui/§8.3.7，2026-09-21 晚轮）**：前端全量 vitest **851 passed / 0 failed**（82 文件，+5：B2 +1、F2 +3、§8.3.7 +1）；tsc exit 0；i18n translations.test 3 passed；**真实浏览器走查复验 21/21 PASS**（antd Step2/3 交互随走查脚本同步，报告与截图于 `frontend/.workbuddy/mining/walkthrough/`）。至此向导 5 步控件全部 antd 化、遗留低优交互项清零。

---

## 五、更新约定（硬规则）

1. **每卡收工时**：本表更新状态 + 完成日期 + DoD 证据；同步 PROGRESS.json 双登记；跑 `update_progress_doc.py` 再生看板；
2. **全量验证硬规则**：前端 `npm test` failed 恒为 0（skipped 只减不增）；后端挖掘域 1090+ 全绿；批量回归**串行**跑（DuckDB 独占锁）；
3. **止损纪律**：同一问题 3 次修复失败 → 状态置 ⏸️ 暂停，写明已试方案与下一步指引，请求协助；
4. **关键路径优先**：A1 → A2 → A3 → A4 → A5；A1 是全链第一断点；G1 多进程在探针真实数据可用前**不做**；
5. **状态词汇**：⬜ 未开始 / 🔄 进行中 / ✅ 完成 / ⏸️ 暂停。
