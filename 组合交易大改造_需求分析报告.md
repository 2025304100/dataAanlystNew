# 组合交易大改造 — 需求分析报告

> 版本：v1.0  
> 日期：2026-08-05  
> 基于原型：[组合交易改造需求清单.md](file:///d:/ai_project/dataAanlystNew/portfolio-trading-redesign/组合交易改造需求清单.md)

---

## 1. 项目概述

### 1.1 改造目标
将当前「组合交易」一级页面从零重新设计，构建 **组合工作台 + 组合交易** 双核心视图，实现信息密度优先、操作路径最短的专业金融终端体验。

### 1.2 改造范围
- 页面重构：单页 Trading → 双核心视图（Workbench + Trading）
- 新增页面：无组合状态页、新建组合弹窗、标的详情抽屉
- UI 风格：深蓝灰专业金融终端主题
- 功能整合：账户汇总、持仓、成员、暴露、规则、下单、绩效、自动交易、回测

---

## 2. 当前系统现状分析

### 2.1 前端当前结构
当前组合交易页（[Trading.tsx](file:///d:/ai_project/dataAanlystNew/frontend/src/components/Trading.tsx)）为单页布局，包含：
1. **账户汇总带**（6 个 KPI：权益、现金、市值、已投入、已实现盈亏、未实现盈亏）
2. **组合绩效面板**（PortfolioPerformancePanel）
3. **自动交易面板**（AutoTradePanel）
4. **组合回测面板**（PortfolioBacktestPanel）
5. **双栏布局**：左侧下单面板 + 右侧持仓表
6. **成交历史** 卡片网格

### 2.2 后端数据模型现状

| 模型 | 文件 | 核心字段 | 现状评估 |
| --- | --- | --- | --- |
| Portfolio | [portfolio.py](file:///d:/ai_project/dataAanlystNew/app/models/portfolio.py#L11-L31) | name, account_type, total_capital, investable_ratio, auto_trade_enabled | ✅ 基础完整，需扩展 UI 简化字段映射 |
| PortfolioRule | [portfolio.py](file:///d:/ai_project/dataAanlystNew/app/models/portfolio.py#L33-L49) | max_single/max_sector/max_stock/max_etf, max_loss, max_open, stage_limits | ✅ 字段完整，UI 只暴露子集 |
| Position | [portfolio.py](file:///d:/ai_project/dataAanlystNew/app/models/portfolio.py#L52-L69) | quantity, avg_cost, latest_price, market_value, position_pct, asset_type, theme | ✅ 完整，可直接映射持仓表 |
| PortfolioMember | [portfolio_member.py](file:///d:/ai_project/dataAanlystNew/app/models/portfolio_member.py#L39-L144) | status, execution_mode, source_type, priority, effective_from/to | ✅ 成员与候选区分有 status/source_type，信号分需额外计算 |
| Symbol | [symbol.py](file:///d:/ai_project/dataAanlystNew/app/models/symbol.py#L11-L29) | symbol, name, asset_type, industry, theme | ⚠️ 缺基本面指标（PE/PB/市值/52周高低） |

### 2.3 后端 API 现状

| API 端点 | 当前状态 | 原型需求 | 匹配度 |
| --- | --- | --- | --- |
| `GET /portfolios` | 列表 | 组合选择器 | ✅ 完全匹配 |
| `POST /portfolios` | 创建 | 新建组合弹窗 | ⚠️ 字段需简化（当前 7 字段 → UI 3 字段） |
| `GET /portfolios/{id}` | 详情+规则+分配 | 组合详情 | ✅ 数据超额，需前端子集映射 |
| `GET /portfolios/{id}/allocation` | 暴露计算（sector_exposure） | FR-WB-04 暴露分析 | ✅ 完全匹配，数据结构一致 |
| `GET /portfolios/{id}/positions` | 持仓列表 | FR-WB-02 / FR-TR-02 持仓表 | ✅ 完全匹配 |
| `POST /portfolios/{id}/rules` | 规则新增 | FR-WB-05 保存规则 | ⚠️ 当前是新增（非 upsert），UI 期望更新 |
| `GET /portfolios/{id}/members` | 成员列表+最近信号 | FR-WB-03 成员与候选表 | ⚠️ 缺信号分（signal_score）和候选状态 UI 标签 |
| `GET /portfolios/{id}/performance` | 绩效指标+曲线 | FR-TR-03 绩效面板 | ✅ 完全匹配（含 benchmark 曲线） |
| `POST /portfolios/{id}/auto-trade/execute` | 手动触发执行 | FR-TR-04 自动交易策略开关 | ❌ 粒度不对：当前是一次性执行，UI 需按策略开关 |
| `POST /orders` (sim) | 模拟下单 | FR-TR-01 下单面板 | ✅ 已有 submitSimOrder 上下文 |
| `GET /symbols/{code}/quote` | — | 标的详情实时行情 | ⚠️ 需通过 Symbol + DailyBar 组合查询 |
| `GET /symbols/{code}/fundamentals` | — | 标的详情 PE/PB/市值等 | ❌ 当前缺失，Symbol 表无对应列 |

---

## 3. 功能变更清单

### 3.1 🔴 删除功能

| 编号 | 删除项 | 当前位置 | 影响评估 | 替代方案 |
| --- | --- | --- | --- | --- |
| D-01 | 成交历史卡片网格 | Trading.tsx L371-L416 | 低（当前仅最近成交，原型无此区域） | 建议迁移到「历史成交」二级页或标签页扩展 |
| D-02 | 账户汇总 6 卡布局 | Trading.tsx L126-L155 | 中（减少 2 个：已投入%、已实现盈亏） | 保留 4 卡：总资产、可用现金、累计收益、今日收益（对齐原型 FR-WB-01） |
| D-03 | 自动交易面板 dry_run 模式 | AutoTradePanel | 中（原型是策略开关列表，非 dry_run 预览） | 策略开关列表（FR-TR-04）替代 |
| D-04 | 下单面板快捷比例按钮 25%/33%/50%/All | Trading.tsx L245-L258 | 低（原型无此 UI） | 直接删除，保留数量/价格输入框 |
| D-05 | 下单面板持仓信息区（持有量/均价/权重/当前价） | Trading.tsx L184-L219 | 中（原型将此信息移至持仓表和标的抽屉） | 删除，点击持仓行打开标的详情抽屉查看 |

### 3.2 🟡 变更功能

| 编号 | 变更项 | 原型需求 | 当前实现 | 改造要点 |
| --- | --- | --- | --- | --- |
| C-01 | 全局 UI 主题 | 深蓝灰 `#0B1120` + 青色信号 | Ant Design 默认浅色主题 | 全局 CSS 变量覆盖 / ConfigProvider 定制；新增 `--pt-*` 设计令牌（对齐 colors_and_type.css） |
| C-02 | 页面导航架构 | 子标签栏「组合工作台 / 组合交易」切换 | 无子标签，单页 Trading | 新增 Portfolio 容器组件，管理子 tab 切换 + 共享外壳（组合选择器、刷新、新建） |
| C-03 | 账户汇总 KPI | FR-WB-01：4 卡（总资产、可用现金、累计收益、今日收益），正负色区分 | 6 卡布局，无今日收益 | 今日收益 = `equity_snapshot.today - equity_snapshot.yesterday`（需新增计算字段或前端计算） |
| C-04 | 持仓明细表列定义 | FR-WB-02：代码、名称、持仓量、现价、市值、盈亏额、盈亏%、权重%、操作 | 多列：均价、观察状态、机会状态徽章 | 删均价列；将「机会状态徽章」移至操作列下拉；增加盈亏额独立列 |
| C-05 | 组合规则编辑 | FR-WB-05：再平衡阈值、最大个股权重、止损线、日内调频开关 | PortfolioRule 8 字段全量编辑 | 做字段映射：<br>• 再平衡阈值 → 新增字段（当前规则无此字段）<br>• 最大个股权重 → `max_single_position_pct`<br>• 止损线 → `max_loss_per_trade_pct`<br>• 日内调频开关 → `portfolio.auto_trade_enabled` + 新增 `intraday_enabled` |
| C-06 | 下单面板布局 | FR-TR-01：双栏（左 35% 下单，右 65% 持仓） | 当前左 35% 下单 + 右 65% 持仓，顺序反了且顶部多了绩效面板 | 调换：绩效/自动交易/回测 移到下方 tab 区；下单面板移到上方左栏，持仓移至上方右栏 |
| C-07 | 绩效面板 UI | FR-TR-03：折线图（组合 vs 基准）+ 5 KPI（累计/年化/回撤/夏普/胜率） | PortfolioPerformancePanel 结构类似 | 调整 KPI 顺序和命名；图表使用 Chart.js（原型）或 ECharts（当前）均可，注意 chartReady 懒加载避免 DOM 警告 |
| C-08 | 创建组合弹窗字段 | FR-7.1：名称、初始资金、账户类型（3 选 1：普通/信用/模拟） | 当前 7 字段（含 investable_ratio, cash_reserve_ratio 等） | 默认值填充隐藏字段：<br>• investable_ratio = 0.95<br>• cash_reserve_ratio = 0.05<br>• is_default = 首个组合设为 true |
| C-09 | 组合选择器 | 顶部导航组合下拉，切换当前组合 | 通过设置页管理，无顶栏选择器 | 新增共享外壳组件，调用 `GET /portfolios` 列表，选中项写入 AppContext |

### 3.3 🟢 新增功能

| 编号 | 新增项 | 原型需求 | 数据/API 支持情况 | 实现建议 |
| --- | --- | --- | --- | --- |
| A-01 | **组合工作台页面** | FR-WB 全量 | ⚠️ 部分支持 | 新组件 `PortfolioWorkbench.tsx`，组合账户汇总 + 持仓表 + 成员候选表 + 暴露分析 + 组合规则 |
| A-02 | **成员与候选表** | FR-WB-03：代码、名称、信号分、候选状态、加入时间、操作 | ⚠️ 缺信号分和状态标签 | 信号分从 Score 表取最新 `action_score`；状态标签映射：<br>• status=active + source=manual/candidate → 已加入（绿）<br>• status=paused → 候选中（青） |
| A-03 | **暴露分析（行业权重进度条）** | FR-WB-04：消费/科技/医药/金融/其他 水平进度条 | ✅ 完全支持 | 直接用 `GET /allocation` 返回的 `sector_exposure`；`_sector_key` 当前按 theme 分组，建议新增映射：theme → 5 大行业（如白酒/食品 → 消费） |
| A-04 | **无组合状态页** | FR-6 | ✅ 数据支持（portfolios 空判断） | 新组件 `PortfolioEmpty.tsx`，居中空态插画 + CTA「创建首个组合」 |
| A-05 | **新建组合弹窗（Modal）** | FR-7 | ⚠️ 字段需简化 | 新组件 `CreatePortfolioModal.tsx`，字段：name + total_capital + account_type；account_type 选项：普通账户(normal) / 信用账户(margin) / 模拟账户(simulated) |
| A-06 | **标的详情抽屉（Drawer）** | FR-8：480px 右抽屉，行情 + 迷你图 + 基本面网格 + 操作按钮 | ⚠️ 基本面指标缺失 | 新组件 `SymbolDetailDrawer.tsx`；数据：<br>• 最新价/涨跌幅 → DailyBar 最新 bar 计算<br>• 迷你图 → 近 30 日 close<br>• PE/PB/市值/52 周高低 → **需新增 Symbol 字段或临时从 universe_symbols 联表** |
| A-07 | **自动交易策略开关列表** | FR-TR-04：均线跟踪/动量突破/网格交易/止盈止损 4 策略，各有开关+状态+最近运行 | ❌ 无策略级开关模型 | 新增模型 `AutoTradeStrategy`（portfolio_id + strategy_key + enabled + last_run_at + params_json）；或使用 portfolio 扩展 JSON 字段暂存；最近运行时间复用 `portfolio.auto_trade_last_run_at` |
| A-08 | **回测面板参数简化** | FR-TR-05：回测区间 + 初始资金 + 运行按钮 + 结果表 | ⚠️ 当前 PortfolioBacktestPanel 复杂 | 保留现有回测引擎，UI 简化为输入框 + 结果表；运行回测按钮走异步任务（已有并发保护机制） |
| A-09 | **共享外壳** | FR-3.4：顶栏品牌 + 组合选择器 + 刷新 + 新建；子标签切换 | ❌ 缺失 | 新组件 `PortfolioShell.tsx`；刷新按钮重新拉取 workbench 上下文 |
| A-10 | **组合汇总专用 API** | `GET /portfolios/{id}/summary` 返回 4 KPI + 今日收益 | ⚠️ 分散在多个端点 | 新增聚合接口：equity_snapshot 最新两条 → 总资产/今日收益；持仓汇总 → 可用现金（total - market_value）；累计收益 = total_equity - total_capital |
| A-11 | **暴露分析专用 API 包装** | `GET /portfolios/{id}/exposure` 返回 5 行业权重 | ✅ allocation 已有 sector_exposure | 包装 allocation 接口，增加行业名称映射（theme 名 → 消费/科技/医药/金融/其他），未匹配归为「其他」 |
| A-12 | **标的搜索联想** | FR-TR-01：标的代码/名称输入框支持搜索联想 | ⚠️ 有 Symbol 表 | `GET /symbols/search?q=` 模糊查询 symbol + name 字段，limit 10，前端做 debounce 300ms |

---

## 4. 数据模型扩展需求

### 4.1 必须新增字段

| 模型 | 新增字段 | 类型 | 用途 | 风险 |
| --- | --- | --- | --- | --- |
| Portfolio | intraday_enabled | int (0/1) default 0 | FR-WB-05 日内调频开关 | 低，启动时 schema 自动对齐 |
| Portfolio | rebalance_threshold_pct | float default 0.05 | FR-WB-05 再平衡阈值（如偏离 5% 触发） | 低 |
| Symbol | pe_ratio | float nullable | FR-8 PE 市盈率 | 中：需补全回填逻辑；或从 universe_symbols 实时联表 |
| Symbol | pb_ratio | float nullable | FR-8 PB 市净率 | 中 |
| Symbol | market_cap | float nullable | FR-8 总市值（亿元） | 中 |
| Symbol | week52_high | float nullable | FR-8 52 周最高 | 中 |
| Symbol | week52_low | float nullable | FR-8 52 周最低 | 中 |

### 4.2 建议新增模型

| 模型 | 字段 | 用途 | 优先级 |
| --- | --- | --- | --- |
| AutoTradeStrategy | id, portfolio_id(FK), strategy_key(enum: ma/momentum/grid/profit_loss), enabled(0/1), params_json, last_run_at, last_status, created_at/updated_at | FR-TR-04 策略级开关；唯一约束 (portfolio_id, strategy_key) | P0（否则策略开关无法持久化） |

### 4.3 当前数据可直接复用的部分

✅ **无需修改即可使用：**
- Position 全部字段 → 持仓明细、当前持仓表
- PortfolioMember status + source_type + created_at → 成员/候选表状态与加入时间
- PortfolioRule max_single_position_pct → 最大个股权重
- PortfolioRule max_loss_per_trade_pct → 止损线
- allocation.sector_exposure → 暴露分析数据
- compute_portfolio_performance 返回值（cumulative_return, annualized_return, max_drawdown, sharpe_ratio, win_rate, equity_curve, benchmark_curve）→ 绩效面板 5 KPI + 对比曲线
- Portfolio.auto_trade_enabled + auto_trade_last_run_at → 自动交易总开关 + 最近执行时间

---

## 5. API 端点改造清单

### 5.1 新增端点（P0）

| 方法 | 路径 | 用途 | 请求 | 响应 | 依赖服务 |
| --- | --- | --- | --- | --- | --- |
| GET | `/api/portfolios/{id}/summary` | 组合汇总 4 KPI | — | `{total_assets, available_cash, cumulative_pnl, cumulative_pnl_pct, today_pnl, today_pnl_pct}` | PortfolioEquitySnapshot + Position 聚合 |
| GET | `/api/portfolios/{id}/exposure` | 行业暴露分析（5 类映射） | — | `[{name: "消费", pct: 0.25, amount: 250000}, ...]` | compute_allocation + 行业映射表 |
| GET | `/api/symbols/search` | 标的搜索联想 | `?q=茅台&limit=10` | `[{id, symbol, name, asset_type}]` | Symbol 模糊查询 |
| GET | `/api/symbols/{id}/overview` | 标的详情抽屉全量数据 | — | `{quote: {price, change_pct}, chart_mini: [{date, close}], fundamentals: {pe, pb, market_cap, week52_high, week52_low, volume}}` | DailyBar + Symbol（需扩展列） |
| GET | `/api/portfolios/{id}/auto-trade/strategies` | 获取策略开关列表 | — | `[{key, name, enabled, status, last_run_at}]` | 新增 AutoTradeStrategy 表 |
| PATCH | `/api/portfolios/{id}/auto-trade/strategies/{key}` | 切换单策略开关 | `{enabled: true/false}` | `{key, enabled, updated_at}` | AutoTradeStrategy upsert |

### 5.2 需修改端点

| 方法 | 路径 | 改动点 |
| --- | --- | --- |
| POST | `/api/portfolios` | 允许只传 3 字段（name, total_capital, account_type），其余用默认值；返回新创建组合 |
| POST/ PUT | `/api/portfolios/{id}/rules` | 改为 upsert 语义（按 portfolio_id + is_active=1 找到则更新，找不到则新增）；返回更新后的规则对象 |
| GET | `/api/portfolios/{id}/members` | 扩展响应：增加 `signal_score`（从 Score 联表最新 action_score），增加 `status_label`（已加入/候选中 枚举） |

### 5.3 可直接复用端点（无需修改）

- `GET /portfolios`
- `GET /portfolios/{id}`
- `GET /portfolios/{id}/positions`
- `GET /portfolios/{id}/allocation`（暴露分析底层）
- `GET /portfolios/{id}/performance`（绩效）
- `POST /portfolios/{id}/auto-trade/execute`（手动触发执行）
- `POST /orders`（模拟下单）
- `GET /portfolios/{id}/equity-snapshots`

---

## 6. 前端组件改造拆分

```
当前：
  App.tsx → Tab "portfolio-trading" → Trading.tsx

改造后：
  App.tsx → Tab "portfolio-trading" → PortfolioPage.tsx（新增）
                                  ├── PortfolioShell.tsx（新增：顶栏+子标签）
                                  ├── 子标签：
                                  │   ├── PortfolioWorkbench.tsx（新增：工作台）
                                  │   │   ├── AccountSummaryBand.tsx（新增/拆分：4 KPI）
                                  │   │   ├── PositionTable.tsx（改造：列精简）
                                  │   │   ├── MemberCandidateTable.tsx（新增）
                                  │   │   ├── ExposureAnalysis.tsx（新增：进度条）
                                  │   │   └── PortfolioRuleEditor.tsx（改造：字段子集）
                                  │   └── PortfolioTrading.tsx（重命名/改造：交易页）
                                  │       ├── OrderPanel.tsx（改造：精简）
                                  │       ├── CurrentPositionTable.tsx（复用 PositionTable）
                                  │       └── Tabs（绩效/自动交易/回测）
                                  │           ├── PerformanceTab.tsx（改造布局）
                                  │           ├── AutoTradeStrategies.tsx（新增：策略开关）
                                  │           └── BacktestTab.tsx（UI 简化）
                                  ├── PortfolioEmpty.tsx（新增：无组合）
                                  ├── CreatePortfolioModal.tsx（新增）
                                  └── SymbolDetailDrawer.tsx（新增）
```

---

## 7. 风险与依赖评估

| 风险编号 | 风险描述 | 影响 | 概率 | 缓解措施 |
| --- | --- | --- | --- | --- |
| R-01 | Symbol 表缺 PE/PB/市值/52 周高低 → 标的详情抽屉缺数据 | 中：用户体验不一致 | 高 | **方案 A**（推荐 P0）：从 universe_symbols 联表查询，不修改 Symbol 表；**方案 B**（P1）：Schema 对齐新增 Symbol 列 + 回填脚本 |
| R-02 | 自动交易策略开关无持久化模型 → 开关状态刷新丢失 | 高：核心功能不可用 | 高 | 新增 AutoTradeStrategy 模型（见 4.2），启动 schema 自动对齐建表 |
| R-03 | 组合规则当前是 insert-only，UI 期望更新语义 → 规则越存越多 | 中：数据冗余 + 读取逻辑复杂 | 中 | 修改 POST /rules 为 upsert：先找 portfolio_id + is_active=1，找到则 update 该行的字段，找不到再 insert |
| R-04 | 主题切换（浅色 → 深蓝灰）可能影响其他页面样式 | 中：全局 CSS 变量污染 | 中 | 主题变量限定在 `[data-page="portfolio"]` 作用域内，不影响今日决策/机会中心等其他一级页 |
| R-05 | 创建组合字段简化后，investable_ratio 默认值可能不满足用户预期 | 低：可在设置页调整 | 低 | 弹窗底部增加「高级设置可在 设置→组合管理 中修改」提示文案 |
| R-06 | 暴露分析 theme → 5 大行业映射不完整 | 低：未匹配归为「其他」 | 中 | 维护硬编码映射表（约 30-50 条常见 theme），剩余归为其他；后续可在设置页配置 |

---

## 8. 实施优先级建议

### P0（必须完成，否则改造不可用）
1. [后端] 新增 AutoTradeStrategy 模型 + schema 对齐
2. [后端] 新增 5 个 P0 API 端点（summary/exposure/symbols/search/symbols/overview/strategies 列表+切换）
3. [后端] 修改 POST /rules 为 upsert；扩展 members 响应
4. [前端] PortfolioShell + 子标签切换容器
5. [前端] PortfolioWorkbench：AccountSummaryBand（4 KPI）、PositionTable、MemberCandidateTable、ExposureAnalysis
6. [前端] CreatePortfolioModal + PortfolioEmpty 空态
7. [前端] PortfolioTrading：OrderPanel（精简）+ CurrentPositionTable + 下方三 Tab 容器
8. [前端] SymbolDetailDrawer 基础版（行情+迷你图，先不展示基本面或展示占位）

### P1（体验完善）
1. [后端] Symbol 表新增 PE/PB/市值/52 周高低 + 回填
2. [前端] 自动交易策略开关列表 UI + 状态/最近运行
3. [前端] 回测面板 UI 简化 + 异步执行进度
4. [前端] 主题变量作用域隔离 + 深蓝灰完整适配
5. [前端] 标的搜索联想 debounce + 下拉建议

### P2（后续迭代）
1. 暴露分析行业映射配置化
2. 组合对比/批量调仓
3. 移动端适配
4. 历史成交二级页
5. i18n 翻译键补全

---

## 9. 结论

### 9.1 数据支持度评估

| 维度 | 支持度 | 说明 |
| --- | --- | --- |
| **核心组合数据** | ✅ 95% | Portfolio / Position / PortfolioRule 完整，仅需扩展 2 个 Portfolio 字段 |
| **成员与候选** | ✅ 85% | PortfolioMember 完整，缺信号分（Score 联表可得）和状态标签（前端映射可得） |
| **暴露分析** | ✅ 100% | allocation.sector_exposure 直接可用，仅需行业名称重映射 |
| **绩效计算** | ✅ 100% | compute_portfolio_performance 完全覆盖原型 5 KPI + 基准对比曲线 |
| **下单交易** | ✅ 90% | 模拟下单链路完整，仅 UI 需精简（删快捷按钮/持仓信息） |
| **自动交易** | ⚠️ 50% | 有总开关和一次性执行，缺策略级独立开关模型（P0 必加） |
| **标的详情** | ⚠️ 60% | 行情和走势可得，基本面指标 PE/PB 等缺失（P1 补列） |
| **回测** | ⚠️ 75% | 引擎完整，UI 需从复杂面板简化为区间+资金+结果表 |

### 9.2 总体判断

> **当前数据与基础功能可支持约 80% 的原型需求落地。**  
> 关键缺失集中在 3 处：**自动交易策略级开关模型**、**标的基本面指标列**、**组合规则 upsert 语义**。  
> 这 3 处均在 P0/P1 范围内有明确缓解方案，不存在阻塞性技术风险。  
> 前端改造工作量约为「3 个新页面容器 + 5 个新组件 + 4 个组件重构 + 1 套主题变量」，建议按 P0→P1→P2 分批次上线，每批均保证核心链路可闭环使用。
