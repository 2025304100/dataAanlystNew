{
 "id": "T26",
 "phase": "PH4",
 "title": "F1 存回/抽取接口",
 "deps": [
  "T16",
  "TD4"
 ],
 "gate": null,
 "budget": "L",
 "writes": [
  "app/services/factors/experience/service.py",
  "app/services/factors/experience/fingerprint.py",
  "app/services/factors/experience/generalization.py",
  "app/models/factor_experience.py",
  "app/api/routes/factor_experience.py",
  "app/schemas/factor_experience.py",
  "app/api/router.py",
  "alembic/versions/2026_*_0058_*.py"
 ],
 "reads": [
  "docs/因子挖掘系统-系统设计文档-v2.0.md#6.14",
  "docs/因子挖掘系统-开发需求文档-落地版.md#6.7 F1 历史经验库",
  "docs/因子挖掘系统-开发文档-落地版.md#3.12 F1 历史经验库"
 ],
 "artifacts": [
  "4 表 + 3 接口"
 ],
 "dod": [
  {
   "cmd": "{PY} -m pytest tests/services/factors/experience/ -q",
   "expect": "exit 0"
  }
 ],
 "not_do": [
  "不做 4 个 Tab / 导入导出 / 季度清理 / 共享库"
 ],
 "pitfalls": [
  "mining/** 不得 import F1 model（红线 C5，静态扫描）",
  "is_negative_sample=1 抽取时自动规避",
  "★ **原任务卡漏了迁移写权限**：本任务建 `factor_experience` / `factor_grade_history` 等 4 张表，必须新建 alembic 修订（编号 **0055**；T07 已用 0054、T36 用 0056）。`high_conflict_files` 早已把 T26 列在 `alembic/versions/` 下，但 `writes` 里没有 —— 已补。三条硬要求见 R13：显式 `mysql_engine=\"InnoDB\"`、索引名从 ORM metadata 反推、列集合与 ORM 完全对齐；收工前跑 `verify_schema_drift.py` 确认无漂移（含外键）。",
  "🚨 **写 MySQL 数值列前必须过 `app.core.db_numeric`**（P0，2026-09-17 定）：\n  · `to_db_float(value, column=...)` —— **NaN/±Inf → None**（项目语义里 NaN = 缺失/无效，**不可归 0**：IC=0 是「无预测力」不是「没算出来」）；超界按列型夹取。\n  · 批量落指标用 `clean_numeric_fields(metrics, columns={...})`（不改原 dict；超界默认视为不可用 → None）。\n  · **禁止**把 pandas/numpy 算出的原始值直接赋给 ORM 数值属性 —— PyMySQL 对 `NaN`/`inf` 在**客户端**就抛 `ProgrammingError`，会让整条 INSERT/UPDATE 失败。\n  · 单测跑绿**不能**证明写入安全（SQLite `REAL` 是 DOUBLE 且不做范围检查）→ 本任务收工前必须做一次真实库写入试探（临时表或真实列 + 回滚）。",
  "★ 迁移编号已重排为 0058（原声明的 0055 已被 D-H pool_id 迁移实际占用，tasks.json 声明与磁盘漂移）；开工时先 alembic heads 取当前链尾接上，勿硬编码信任卡片编号。"
 ]
}