==========================================================================
需求 §6.1 逐代进化
==========================================================================
### 6.1 候选生成（遗传算法）

输入：`universe_id`/股票池快照、`data_cutoff_at`、调仓频率、目标周期、种群大小、最大代数、选择比例、变异率、交叉率、随机注入率、收敛阈值、随机种子、初始种群来源配置。

**两层结构**

- **第一层 经典底座**：25 个系统预设模板（6 类），参数网格展开；可勾选类别（默认全选）、数量上限（默认 40，硬上限种群 60%）、覆盖策略（均衡覆盖默认 / 优先级覆盖）。字段联动：估值/质量类未选对应字段则该类置灰。
- **第二层 AI/随机探索**：剩余名额 = 种群大小 − 经典实际入库数。AI 生成（默认剩余 57%，受 §3.3 约束）、受约束随机（43%，算子 ≤4、嵌套 ≤2、字段引用 ≤3）。

**数量链路**：各来源按目标 × 冗余系数（默认 2.0）超额生成 → 校验（损耗 10~20%）→ 来源内去重（15~25%）→ 跨来源去重（约 10%）→ 按「锁定经典 → AI 取 Top → 随机补足」入库。

**四层去重**：①生成时规避 ②`formula_hash` 精确去重（交换律归一）③AST 结构相似度聚类（**M1 简化相似度，M2 树编辑距离**）④运行时因子值相关性去重（三层指纹预筛，≥0.95 标记 `eliminated_similar`）。

**逐代进化**：评估（短样本，**只用 train 段**）→ 观测 → 选择 → 繁殖 → 校验 → 停止。**M1 朴素选择（按 ICIR 排序）+ 固定三率；M2 换完整机制。**

**候选记录字段**：公式哈希、规范化公式、表达式树、字段依赖、代数、父代 ID、操作类型、随机种子、生成时间，及 §3.3 新增的 `economic_logic` / `expected_direction` / `interpretability_score` / `logic_source`。

#### 6.1.1 选择机制（M2）

- **A1 赛道竞争**：6 类赛道，归类优先级 `quality > valuation > volatility > volume_price > reversal > trend`；配额「保底 60% + 动态 40%」，弱赛道（连续 3 代最优个体处于最差层）保底减半转出
- **B1 多目标 NSGA-II**：默认 4 目标（ICIR↑、覆盖率↑、换手率↓、复杂度↓）；非支配分层得 rank + 拥挤度 CD，比较「先比 rank、同 rank 比 CD」；目标数限 3~5
- **B2 锦标赛**：赛道内随机抽 K=3（可调 2~7）取最优；精英不参与
- 偏好档位（均衡/稳健/进攻/简洁）**只作用于结果排序**

#### 6.1.2 繁殖与自适应（M2）

观测(D3/D1) → 决策(C1) → 执行(C2/C3/D2) 单向信号流：

| 机制 | 唯一职责 |
|------|---------|
| D3 多样性监控 | 只产健康分信号，**不改任何率** |
| C1 自适应调度 | **唯一调参者** |
| C2 四种变异 | 参数微调/算子替换/字段替换/结构变异，比例由 C1 定 |
| C3 子树交叉 | 同/跨赛道比例由 C1 定（基准 80/20） |
| D2 随机注入 | 注入率由 C1 定（0.05~0.20） |
| D1 收敛早停 | 只判断停止；**读 val 段信号，先自救（stall≥2），再连续 3 代无效才停** |

安全护栏：变异 [0.40,0.80] / 交叉 [0.10,0.50] / 注入 [0.05,0.20]，三率和为 1；单次调幅 ≤0.10；精英不参与变异交叉。

#### 6.1.3 岛屿模型（远期）

仅预留可空字段 `island_id`、`parent_island_id`，本期恒空。

#### 6.1.4 F2 AI 引导变异（延后二期）

本期不实现。F1 `sample` 接口保留 `related_experiences` 可选入参，`related` 接口本期实现但不调用。

#### 6.1.5 F3 超参数元学习（不做）

#### 6.1.6 性能：G2（M1）/ G1（M3）+ 性能探针（v3.1 遗漏 10）

**G2 子表达式缓存（M1）**：AST 遍历提取 → SHA-256 强哈希去重（算子+参数+字段 ID+数据快照版本）→ 向量化批量计算 → **主进程内存缓存**（不写 DuckDB，规避单写锁）→ 组装时递归查缓存。

- 每代随机抽 5 个因子，缓存结果与直接全量计算对比，差异 >1e-6 报警并重算
- NaN/Inf 检查，异常不写入缓存并标记 failed
- 缓存带 `schema_version` + `hash_algorithm_version`
- 预筛与全量验证分别建缓存

**性能探针（M1 必埋，为 M3 提供依据）**：逐代记录并写入 `generations` 表：

| 探针 | 说明 |
|------|------|
| `data_load` | 数据加载 |
| `ast_eval` | AST 求值 |
| `subexpr_compute` | 子表达式计算 |
| `factor_assemble` | 因子组装 |
| `metric_calc` | 指标计算（IC/ICIR/覆盖率/换手） |
| `db_write` | 落库写入 |
| `g2_hit_rate` | 子表达式缓存命中率 |

**G1 多进程并行（M3，压测后决定）**：依据 M1/M2 探针数据定位瓶颈再决定并行哪一段。已识别风险：无 ProcessPool 先例、Windows 无 fork、`shared_memory` 在 Windows 有 resource_tracker 泄漏坑。



==========================================================================
开发文档 §3.9 繁殖与自适应实现（V3.16 切分）
==========================================================================
### 3.9 繁殖与自适应实现（M2）

九机制单向信号流：观测(D3/D1) → 决策(C1) → 执行(C2/C3/D2)。

| 机制 | 职责 | 约束 |
|------|------|------|
| D3 多样性监控 | 只产 health（类型均衡度 + 基因型 + 表现型三层合成） | **不改任何率** |
| C1 自适应调度 | 唯一调参者，输出三率 + 变异类型分布 + 跨赛道比例 | 安全区间 + 滞回 2 代 + 单步 ≤0.10 |
| C2 四种变异 | 参数微调/算子替换/字段替换/结构变异 | 比例由 C1 定，字段替换不越出已选字段 |
| C3 子树交叉 | 同/跨赛道（基准 80/20） | 比例由 C1 定 |
| D2 随机注入 | 注入率 0.05~0.20 | 由 C1 定 |
| D1 收敛早停 | 只判断停止 | **先自救**（stall=2 交 C1），**后停止**（stall=3 才 converged） |

每代 `actual_*` 三率与策略落库，固定种子可复现。



==========================================================================
SD §7.2（进化/繁殖相关）
==========================================================================
### 7.2 三段切分接入与 purge/embargo 单位归一化（★v2.0 核心修正）

#### 7.2.1 问题陈述

`build_time_split` 的实现（`factor_evaluator.py:266-273`）把 `purge_days`/`embargo_days` 当作**传入 `all_dates` 序列的索引间隔**：

```python
train_end_idx  = max(0, min(n - 1, int(n * train_ratio)))
val_end_idx    = max(train_end_idx, min(n - 1, int(n * (train_ratio + validation_ratio))))
val_start_idx  = min(n - 1, train_end_idx + int(purge_days))      # ← 索引偏移
test_start_idx = min(n - 1, val_end_idx   + int(embargo_days))    # ← 索引偏移
```

而挖掘传入的 `all_dates` 是**有效调仓点**（非交易日）。因此：

- 日频：1 个调仓点 = 1 个交易日 → `5` 语义正确
- 周频：1 个调仓点 = 5 个交易日 → `5` 变成"隔离 5 周（25 个交易日）"
- 月频：1 个调仓点 = 约 21 个交易日 → `5` 变成"隔离 5 个月（105 个交易日）"

**后果**：月频/周频的 val 与 test 段被 purge/embargo 吃光，且与"避免持有期标签（T+5）跨界"的本意完全不符。

**第二个问题**：`min_validation_days` 默认 50 推出 `derived_min_total = max(20, ceil((50 + purge + embargo) / validation_ratio))`。

实测（本机复算 `factor_evaluator.py:259-263` 口径）：

| 频率 | n | purge/embargo | `derived_min_total` | `hard_fail` |
|------|---|---------------|--------------------|------------|
| daily 5y | 1,215 | 5 / 5 | **300** | False ✅ |
| weekly 5y | 243 | **1 / 1**（已归一化） | **260** | **True** ❌ |
| monthly 5y | 60 | **1 / 1**（已归一化） | **260** | **True** ❌ |
| monthly 10y | 120 | **1 / 1** | **260** | **True** ❌ |

> **关键结论**：**即使先做了 §7.2.2 的单位归一化，只要仍依赖默认 `min_validation_days=50`，周频与月频依然全部 `hard_fail`。**
> 两个问题必须**同时修**（C11 + C12），只修单位归一化不足以让周/月频跑通。周频 243 点需要 `derived_min_total ≤ 243`，即 `min_validation_days ≤ 0.2×243 − 2 = 46.6`，故 `SPLIT_MINIMUMS["weekly"]` 取 `min_val=20` 留足余量。

#### 7.2.2 归一化规则（C11/C12）

```python
# 调仓频率 → 每期含交易日数
REBALANCE_INTERVAL_DAYS = {"daily": 1, "weekly": 5, "monthly": 21}

def normalize_purge_points(*, target_horizon_days: int, frequency: str) -> int:
    """把「日」为单位的隔离期换算为「调仓点」计数，最少 1 期。"""
    interval = REBALANCE_INTERVAL_DAYS[frequency]
    return max(1, math.ceil(target_horizon_days / interval))

# 频率标定的样本量地板（与需求 §3.2 最小样本量硬门槛对齐）
SPLIT_MINIMUMS = {
    # frequency: (min_total_points, min_val_points, min_test_points)
    "daily":   (252, 100, 50),
    "weekly":  (104,  20, 10),
    "monthly": ( 36,   6,  3),
}
```

**换算结果表**

| 频率 | `purge_points` | `embargo_points` | 相对旧写的语义变化 |
|------|---------------|------------------|------------------|
| daily | 5 | 5 | 不变 |
| weekly | **1** | **1** | 从"5 周"修正为"1 周"（覆盖 T+5 持有期） |
| monthly | **1** | **1** | 从"5 个月"修正为"1 个月"（覆盖 T+5 持有期） |

> 换算依据：持有期为 T+1 买入 → T+5 卖出，即约 5 个交易日。要阻止标签跨段越界，只需在边界处丢弃 `ceil(5 / 每期交易日数)` 个调仓点。周频与月频各丢 1 期即足够。

#### 7.2.3 样本量口径重算（**取代需求 §2.1 / §4.1 的乐观值**）

以 `train_ratio=0.6, validation_ratio=0.2, purge_points/embargo_points` 如上、`tail_loss = target_horizon = 5` 实算：

| 频率 | 总点数 n | purge/embargo | train | val | test | 频率地板 | 结论 |
|------|---------|--------------|-------|-----|------|---------|------|
| daily（5 年） | 1,215 | 5 / 5 | 729 | 238 | 233 | 252 ✅ | 健康 |
| weekly（5 年） | 243 | 1 / 1 | 145 | 48 | 43 | 104 ✅ | 健康（**修正后**） |
| monthly（5 年） | 60 | 1 / 1 | 36 | **11** | **6** | 36 ✅（压线） | 统计降级（见 §7.9.4） |
| monthly（10 年镜像） | ~120 | 1 / 1 | 72 | **23** | **18** | — | 仍**达不到需求 §4.1 的「test ≥24」** |

> **对需求文档的勘误**：需求 §2.1 表格给出月频 `val 12 / test 12`，是按 `n × ratio` 直算、未扣 purge/embargo 的结果；实算为 **val 11 / test 6**。需求 §4.1 声称"镜像 10 年

==========================================================================
contracts.py 已有契约（Individual/Fitness/Strategy/GenerationStat）
==========================================================================
class Fitness:
    """个体适应度（**只来自 train 段**，设计文档 §7.2 / C8）。"""

    icir: float
    coverage: float
    turnover: float
    ic_mean: float
    complexity: int
    valid_cross_sections: int
    sample_count: int


@dataclass(frozen=True)

class GenerationStat:
    """每代汇总（对应 factor_mining_generations 一行）。"""

    run_id: str
    generation: int
    population_size: int
    best_icir: float
    avg_icir: float
    median_icir: float
    diversity_score: float
    category_distribution_json: dict[str, int] = field(default_factory=dict)
    category_evenness: float = 0.0
    pareto_front_count: int = 0
    diversity_genotype: float = 0.0
    diversity_phenotype: float = 0.0
    diversity_health: float = 0.0
    stall_count: int = 0
    actual_mutation_rate: float = 0.0
    actual_crossover_rate: float = 0.0
    actual_random_rate: float = 0.0
    mutation_type_distribution_json: dict[str, float] = field(default_factory=dict)
    cross_category_ratio: float = 0.2
    adaptive_state: str = ""
    elite_count: int = 0
    mutation_count: int = 0
    crossover_count: int = 0
    random_count: int = 0
    eliminated_count: int = 0
    evaluation_duration_ms: int = 0
    convergence_delta: float = 0.0
    # ── 性能探针（M1 必埋，设计文档 §7.4） ──
    probe_data_load_ms: int = 0
    probe_ast_eval_ms: int = 0
    probe_subexpr_compute_ms: int = 0
    probe_factor_assemble_ms: int = 0
    probe_metric_calc_ms: int = 0
    probe_db_write_ms: int = 0
    probe_subexpr_total: int = 0
    probe_subexpr_unique: int = 0
    probe_g2_hit_rate: float = 0.0
    # ── G2 抽样校验（每代必写，设计文档 §7.3） ──
    cache_validation_passed: int | None = None
    cache_validation_max_diff: float | None = None


@dataclass(frozen=True)
class StatResult:
    """统计层 8 方法输出（M2，写 factor_evaluation_runs.metrics_json.stats）。"""

    p_value: float
    p_adj_bonferron
