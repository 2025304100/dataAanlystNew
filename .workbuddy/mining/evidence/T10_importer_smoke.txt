==========================================================================
1. 模板生成（CSV / XLSX）
==========================================================================
  csv   -> candidate_pool_template.csv          92B  text/csv; charset=utf-8
         模板可反向解析：[('000001', '示例：平安银行'), ('600519', '示例：贵州茅台')]
  xlsx  -> candidate_pool_template.xlsx       5082B  application/vnd.openxmlformats-officedocument.spread
         模板可反向解析：[('000001', '示例：平安银行'), ('600519', '示例：贵州茅台')]

==========================================================================
2. 解析：真实场景文件（含空行 / 大小写列名 / 额外列）
==========================================================================
  解析出 8 行（已跳过空行）
    row  2  symbol='000001'   name=平安银行           market=sz note=银行龙头
    row  3  symbol='600519'   name=贵州茅台           market=sh note=白酒
    row  5  symbol='000002'   name=万  科Ａ          market=sz note=None
    row  6  symbol='1'        name=前导零丢失          market=sz note=Excel 打开过
    row  7  symbol='000001'   name=平安银行           market=sz note=文件内重复
    row  8  symbol='999999'   name=不存在            market=sz note=未匹配
    row  9  symbol='159919'   name=沪深300ETF       market=sz note=ETF
    row 10  symbol='600193'   name=退市创兴           market=sh note=已退市

==========================================================================
3. 逐行匹配（对照真实 symbols 主数据）
==========================================================================
  row  2 [入池] success      000001   sys_name=平安银行         id=1
          匹配成功。
  row  3 [入池] success      600519   sys_name=贵州茅台         id=3276
          匹配成功。
  row  5 [入池] success      000002   sys_name=万  科Ａ        id=6
          匹配成功。
  row  6 [剔除] format_error 1        sys_name=None         id=None
          股票代码 '1' 不是 6 位。若为 Excel 打开导致**前导零丢失**，请将该列设为「文本」格式后重新保存（例如 1 → 000001）。
  row  7 [剔除] duplicate    000001   sys_name=None         id=None
          该代码已在第 2 行出现过，本行忽略（保留首次）。
  row  8 [剔除] unmatched    999999   sys_name=None         id=None
          代码 999999 在 symbols 主数据中不存在，未匹配。
  row  9 [剔除] non_stock    159919   sys_name=沪深300ETF嘉实   id=6482
          代码 159919 的资产类型是 etf（非股票），候选池只支持股票资产。
          ⚠ 文件中的名称 '沪深300ETF' 与系统主数据 '沪深300ETF嘉实' 不一致 —— **以系统主数据为准，不回写**。
  row 10 [剔除] suspended    600193   sys_name=退市创兴         id=3043
          代码 600193 在主数据中 `is_active=0`（已退市 / 不可交易），不入池。注：本项目**无真正的停牌字段**（`security_status_daily` 仅为 e2e fixture 数据），该状态

  计数: {'success': 3, 'unmatched': 1, 'duplicate': 1, 'format_error': 1, 'non_stock': 1, 'suspended': 1}

==========================================================================
4. 名称/市场不一致只提示、不回写
==========================================================================
  ⚠ 文件中的名称 '错误的名字' 与系统主数据 '平安银行' 不一致 —— **以系统主数据为准，不回写**。
  ⚠ 文件中的市场 'sh' 与系统主数据 'sz' 不一致 —— **以系统主数据为准，不回写**。
  状态仍为 success（名称和市场的差异**不影响匹配**）: success

==========================================================================
5. 错误明细 CSV（可下载、可改后重导）
==========================================================================
row_no,status,status_label_zh,symbol,name_in_file,market_in_file,note,reason_zh
6,format_error,格式错误,1,前导零丢失,sz,Excel 打开过,股票代码 '1' 不是 6 位。若为 Excel 打开导致**前导零丢失**，请将该列设为「文本」格式后重新保存（例如 1 → 000001）。
7,duplicate,文件内重复,000001,平安银行,sz,文件内重复,该代码已在第 2 行出现过，本行忽略（保留首次）。
8,unmatched,未匹配,999999,不存在,sz,未匹配,代码 999999 在 symbols 主数据中不存在，未匹配。
9,non_stock,非股票资产,159919,沪深300ETF,sz,ETF,代码 159919 的资产类型是 etf（非股票），候选池只支持股票资产。
10,suspended,已停牌/不可交易,600193,退市创兴,sh,已退市,代码 600193 在主数据中 `is_active=0`（已退市 / 不可交易），不入池。注：本项目**无真正的停牌字段**（`security_status_daily` 仅为 e2e fixture 数据），该状态实际表达的是「已退市/不可交易」。


==========================================================================
6. 格式判定与异常
==========================================================================
  ('a.csv', None) -> csv
  ('a.xlsx', None) -> xlsx
  ('a.xls', None) -> xls
  ('A.XLSX', None) -> xlsx
  ('a.txt', None) -> FactorSevenError: [VALIDATION_ERROR] 无法判定导入文件格式。支持 ['csv', 'xlsx', 'xls']（文件名 'a.txt' 未带可识别后缀）。
  ('a.csv', 'xlsx') -> xlsx

  --- 缺 symbol 列 ---
  FAIL 未拦截

  --- 空文件 ---
  OK FactorSevenError: 导入文件为空。

  --- GBK 编码 CSV ---
  OK 解析 1 行 -> [('000001', '平安银行')]

  --- Excel 前导零（openpyxl 写入数值 1 而不是文本 000001）---
  解析 2 行 -> [('1', '数值型代码'), ('000002', '文本型代码')]
    row2 format_error '1'  股票代码 '1' 不是 6 位。若为 Excel 打开导致**前导零丢失**，请将该列设为「文本」格式后重新保存（例如 1 → 000001）。
    row3 success      '000002'  匹配成功。

==========================================================================
7. 预览（commit=False）不写任何东西
==========================================================================
  total=8 admitted=3 errors=5 committed=False
  counts={'success': 3, 'unmatched': 1, 'duplicate': 1, 'format_error': 1, 'non_stock': 1, 'suspended': 1}
